---
phase: 07-admin-panel-overhaul
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - server/api/internal/repository/admin_repo.go
  - server/api/internal/handler/admin.go
  - server/api/internal/handler/admin_kpis_test.go
autonomous: true
requirements: [ADMIN-01]
must_haves:
  truths:
    - "GET /admin/stats returns paid_users, mrr, active_connections, signups_today/week/month, churn_30d, failed_payments_30d in addition to existing fields"
    - "MRR is cached in Redis (cache:admin:mrr:<currency>) for 5 minutes, computed from active paid users via plans+plan_offers"
    - "Existing total_users/active_subscriptions/server_count/active_server_count fields are preserved (no regression)"
  artifacts:
    - path: server/api/internal/repository/admin_repo.go
      provides: "GetDashboardKPIs extending GetGlobalStats with the ADMIN-01 metrics + GetMRR revenue query"
      contains: "func GetDashboardKPIs"
    - path: server/api/internal/handler/admin.go
      provides: "AdminGetStats wired to the extended KPI repo + 5-min MRR cache"
      contains: "cache:admin:mrr:"
  key_links:
    - from: server/api/internal/handler/admin.go
      to: server/api/internal/repository/admin_repo.go
      via: "AdminGetStats calls GetDashboardKPIs"
      pattern: "GetDashboardKPIs"
---

<objective>
Extend the existing read-only dashboard stats endpoint (ADMIN-01) so it returns live KPIs: paid users this period, MRR estimate, active concurrent connections, signups today/week/month, 30-day churn, and 30-day failed payments — all from the existing DB. MRR (the only expensive multi-join) is cached 5 minutes in Redis. No new route is added; GET /admin/stats already exists and is wired in main.go.

Purpose: This is the dashboard's KPI bar — the operator's day-to-day at-a-glance view. Pure read-only, zero mutation risk.
Output: extended repository.GetDashboardKPIs + GetMRR, AdminGetStats handler returning the new fields with MRR cache.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-panel-overhaul/07-RESEARCH.md

<interfaces>
<!-- Confirmed from codebase this session. -->

Existing repo (internal/repository/admin_repo.go):
  func GetGlobalStats(ctx, db) (map[string]interface{}, error)  // returns total_users, active_subscriptions, server_count, active_server_count
  func GetTimeseries(ctx, db, days int) (signups, connections []TimeseriesBucket, error)  // per-day signups already computed

Existing handler (internal/handler/admin.go):
  func AdminGetStats(logger, db) fiber.Handler  // currently calls GetGlobalStats; route admin.Get("/stats", ...) already wired in cmd/main.go.
  NOTE: AdminGetStats does NOT currently receive a *redis.Client. You must change its signature to AdminGetStats(logger, db, redisClient) and update the single call site in cmd/main.go (admin.Get("/stats", handler.AdminGetStats(logger, db, redisClient))). This is the ONLY main.go edit in this plan — a one-line argument addition, not a new route.

Schema facts for the KPI SQL:
  - users: subscription_tier (string), subscription_expires_at (timestamptz nullable), created_at, plan_id (uuid).
  - connections: disconnected_at (nullable), last_heartbeat_at (nullable). active concurrent = disconnected_at IS NULL AND last_heartbeat_at > now() - interval '2 minutes'.
  - plans: id, code, is_system (bool). System plan = the free tier; paid = NOT is_system.
  - plan_offers: plan_id, periodicity (MONTHLY/PERIOD_YEAR/etc.), price/amount, currency. (Confirm exact amount/price column names by reading plan_repo.go + model/plan.go before writing the MRR query.)
  - lava_contracts: cancelled_at (timestamptz nullable) — churn source (A2: count cancelled_at in last 30d, NO new column).
  - lava_webhook_events: event_type, received_at. failed_payments_30d = count WHERE event_type LIKE '%failed%' AND received_at > now() - interval '30 days'.

Cache helpers (internal/cache/): pattern is Get/Set/Bust per namespace. Add MRR cache inline in the handler using redisClient.Get/Set with key cache:admin:mrr:<currency> TTL 5*time.Minute (no bust needed — 5-min staleness on an estimate is acceptable per RESEARCH).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GetDashboardKPIs + GetMRR to admin_repo.go</name>
  <read_first>
    - server/api/internal/repository/admin_repo.go (GetGlobalStats + GetTimeseries patterns; ctx threading via db.WithContext(ctx))
    - server/api/internal/repository/plan_repo.go (FindSystemPlanID, plan/offer column names)
    - server/api/internal/model/plan.go (PlanOffer amount/price + periodicity + currency column names — MUST confirm before writing MRR SQL)
  </read_first>
  <behavior>
    - GetDashboardKPIs returns all GetGlobalStats keys PLUS: paid_users (int64), active_connections (int64), signups_today/signups_week/signups_month (int64), churn_30d (int64), failed_payments_30d (int64).
    - paid_users = count(users WHERE subscription_tier != <system plan code> AND (subscription_expires_at IS NULL OR subscription_expires_at > now())).
    - active_connections = count(connections WHERE disconnected_at IS NULL AND last_heartbeat_at > now() - interval '2 minutes').
    - churn_30d = count(lava_contracts WHERE cancelled_at > now() - interval '30 days').
    - failed_payments_30d = count(lava_webhook_events WHERE event_type LIKE '%failed%' AND received_at > now() - interval '30 days').
    - GetMRR(ctx, db, currency) returns a float64: sum over active paid users of (monthly offer amount, or yearly offer amount / 12) for that currency. Yearly contributes amount/12.
  </behavior>
  <action>
Add two functions to internal/repository/admin_repo.go. Use db.WithContext(ctx).Raw(...).Scan(...) (match the ctx-propagation rule PERF-07 — every query threads ctx). Resolve the system plan code via FindSystemPlanID + FindPlanByID (or a direct subquery `subscription_tier != (SELECT code FROM plans WHERE is_system = true LIMIT 1)`).

GetDashboardKPIs(ctx, db) (map[string]interface{}, error): start from GetGlobalStats's map (call it, then add keys), then run the additional COUNT queries above and merge. Keep the existing four keys intact.

GetMRR(ctx, db, currency string) (float64, error): join users → plan_offers on the user's active paid plan for the given currency; MONTHLY offers add their amount, PERIOD_YEAR offers add amount/12.0. Use the actual amount column name confirmed from model/plan.go. Return 0 with nil error when no paid users (not an error).
  </action>
  <verify>
    <automated>cd server/api && go build ./internal/repository/ && go vet ./internal/repository/</automated>
  </verify>
  <done>admin_repo.go compiles; GetDashboardKPIs and GetMRR exist with the signatures above; every query uses db.WithContext(ctx).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire AdminGetStats to KPIs + 5-min MRR cache; turn the RED KPI test GREEN</name>
  <read_first>
    - server/api/internal/handler/admin.go (AdminGetStats current body)
    - server/api/cmd/main.go (the admin.Get("/stats", ...) call site — line ~359)
    - server/api/internal/handler/admin_kpis_test.go (RED test TestAdminGetStatsKPIs from plan 07-01 — remove its t.Skip, assert the new keys)
    - server/api/internal/cache/redis.go (redis client usage style)
  </read_first>
  <behavior>
    - GET /admin/stats body.data contains paid_users, mrr, active_connections, signups_today, signups_week, signups_month, churn_30d, failed_payments_30d plus the four existing keys.
    - mrr is read from cache:admin:mrr:<currency> when present; on miss, GetMRR is computed and SET with 5-min TTL. Default currency = USD when ?currency absent.
    - Redis outage must not fail the endpoint — on a cache error, compute MRR live and continue (fail-open).
  </behavior>
  <action>
Change AdminGetStats signature to AdminGetStats(logger *zap.Logger, db *gorm.DB, redisClient *redis.Client) fiber.Handler. Update the call site in cmd/main.go to pass redisClient. In the handler: read currency := c.Query("currency", "USD"); call GetDashboardKPIs; for MRR, try redisClient.Get(ctx, "cache:admin:mrr:"+currency) — on hit parse float, on miss call repository.GetMRR then redisClient.Set(ctx, key, value, 5*time.Minute) best-effort. Add mrr into the response map. Return c.JSON(fiber.Map{"data": stats}).

Then edit server/api/internal/handler/admin_kpis_test.go: remove the t.Skip on TestAdminGetStatsKPIs and assert the response body contains every new key (use the SQLite setup pattern from webhook_lava_test.go; seed one paid user + one active connection + one cancelled lava_contract + one failed webhook event so the counts are non-zero where assertable, or assert key presence + types when seeding the join is impractical in SQLite — at minimum assert all eight keys exist).
  </action>
  <verify>
    <automated>cd server/api && go test ./internal/handler/ -run TestAdminGetStatsKPIs -count=1 && go build ./...</automated>
  </verify>
  <done>TestAdminGetStatsKPIs passes (no longer skipped); GET /admin/stats returns the eight new KPI fields; MRR uses the cache:admin:mrr:<currency> key with 5-min TTL; full module builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin SPA → /admin/stats | authenticated admin (AuthRequired + AdminRequired) bearer-token request |
| handler → Redis (MRR cache) | trusted internal cache; fail-open on outage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Elevation | /admin/stats access | mitigate | Route stays on the AdminRequired group (re-reads role from DB per request, HOTFIX-02) — no stale-JWT can reach KPIs |
| T-07-05 | DoS | MRR multi-join on every 60s dashboard poll | mitigate | 5-min Redis cache (cache:admin:mrr:<currency>) collapses repeated polls to one aggregate per window |
| T-07-06 | Info Disclosure | KPI numbers | accept | Aggregate counts only, no PII; admin-only endpoint |
| T-07-07 | Tampering | currency query param into cache key | mitigate | currency is used only as a cache-key suffix and a WHERE filter via parameterized query; no SQL string concatenation |
</threat_model>

<verification>
- `cd server/api && go test ./internal/handler/ -run TestAdminGetStatsKPIs -count=1` green.
- `cd server/api && go build ./...` green (main.go call-site updated).
- Manual: GET /admin/stats returns the eight new KPI fields; a second call within 5 min serves MRR from cache (no repeated aggregate in query log).
</verification>

<success_criteria>
- GET /admin/stats returns paid_users, mrr, active_connections, signups_today/week/month, churn_30d, failed_payments_30d plus all pre-existing fields.
- MRR cached 5 min in cache:admin:mrr:<currency>, fail-open on Redis outage.
- The 07-01 RED KPI test is now GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-panel-overhaul/07-02-SUMMARY.md`.
</output>
