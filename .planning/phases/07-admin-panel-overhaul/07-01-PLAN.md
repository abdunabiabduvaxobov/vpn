---
phase: 07-admin-panel-overhaul
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/api/migrations/024_admin_panel_overhaul.sql
  - server/api/internal/testutil/pg.go
  - server/api/integration/admin_concurrency_test.go
  - server/api/integration/webhook_replay_test.go
  - server/api/internal/middleware/maintenance_test.go
  - server/api/internal/middleware/suspended_test.go
  - server/api/internal/handler/admin_kpis_test.go
  - server/api/internal/handler/health_endpoints_test.go
autonomous: true
requirements: [ADMIN-01, ADMIN-02, ADMIN-03, ADMIN-04, ADMIN-05, ADMIN-06, ADMIN-07, ADMIN-08]
must_haves:
  truths:
    - "Migration 024 adds users.suspended_at/suspended_reason, vpn_servers.is_draining/last_seen_at, lava_webhook_events.status (backfilled), feature_flags + broadcast_messages tables"
    - "migrations_test.go sequential-numbering check still passes with 024 present"
    - "A real-Postgres test helper (testutil.StartPostgres) exists and t.Skips cleanly when testcontainers/Docker is unavailable"
    - "RED stub tests exist for ADMIN-01/03/04/06/07 referencing the exact downstream handler/repo symbols (compile-fail or assertion-fail is the RED state)"
  artifacts:
    - path: server/api/migrations/024_admin_panel_overhaul.sql
      provides: "Phase-7 columns + feature_flags/broadcast_messages tables + status backfill"
      contains: "ALTER TABLE lava_webhook_events ADD COLUMN status"
    - path: server/api/internal/testutil/pg.go
      provides: "StartPostgres(t) returning a real *gorm.DB on pgx for advisory-lock tests"
      contains: "func StartPostgres"
    - path: server/api/integration/admin_concurrency_test.go
      provides: "RED test TestForceCancelWebhookRace (ADMIN-03 advisory-lock proof)"
      contains: "func TestForceCancelWebhookRace"
  key_links:
    - from: server/api/migrations/024_admin_panel_overhaul.sql
      to: server/api/migrations/migrations_test.go
      via: "lexicographic apply loop picks up 024 automatically"
      pattern: "024_admin_panel_overhaul"
---

<objective>
Lay the Wave-0 foundation for Phase 7: the single forward migration (024) that adds every Phase-7 column and operational table, a real-Postgres integration-test helper (the prior tests use in-memory SQLite which cannot run `pg_advisory_xact_lock`), and RED-first failing tests for the load-bearing requirements (ADMIN-03 race, ADMIN-06 replay) plus stubs for ADMIN-01/04/07.

Purpose: Migration 024 is the schema every downstream plan depends on; the PG helper is required by the ADMIN-03 (advisory lock) and ADMIN-04 (drain) integration tests that cannot use SQLite; RED tests define "done" for the hardest requirements before any implementation lands.
Output: 1 migration file, 1 test helper, and 6 RED test files. No production handler/repo code changes in this plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-admin-panel-overhaul/07-RESEARCH.md
@.planning/phases/07-admin-panel-overhaul/07-VALIDATION.md

<interfaces>
<!-- Confirmed by reading the codebase this session. Use these directly. -->

Migration convention (server/api/migrations/): single-file `.sql`, numbered NNN_name.sql, currently highest = 023_connections_connected_at_index.sql. Next free = 024. migrations_test.go applies every *.sql lexicographically in TestMigrations019_020 (it explicitly skips 019/020/021 only — 024 is applied in the generic loop). Files using CONCURRENTLY are split at statement boundaries; 024 must NOT use CONCURRENTLY (plain transactional DDL is fine and simpler).

Existing schema facts:
- users: id uuid pk, suspended_at / suspended_reason do NOT exist yet.
- vpn_servers: id uuid pk, is_active bool, current_load int; is_draining / last_seen_at do NOT exist yet.
- lava_webhook_events: id uuid pk, event_type varchar(64), contract_id varchar(64), invoice_id varchar(64), payload jsonb, received_at, processed_at timestamptz nullable, error text nullable. NO status column yet. Add status; backfill: error IS NOT NULL → 'FAILED', else processed_at IS NOT NULL → 'DELIVERED', else 'PENDING'.
- model.LavaWebhookEvent struct lives in internal/model/lava_webhook_event.go.

Real-Postgres test pattern already in repo (internal/repository/ctx_cancel_test.go::startPostgresGorm):
uses testcontainers-go + tcpostgres.Run("postgres:16-alpine") + gorm postgres driver. Replicate this into a reusable testutil helper. Wrap container start in a check: if Docker/testcontainers is unavailable, t.Skip("no docker for integration test") rather than t.Fatal.

Handler test pattern (SQLite, fast): internal/handler/webhook_lava_test.go::setupWebhookTestDB builds an in-memory SQLite DB with hand-written CREATE TABLE. Use SQLite for KPI/health stub tests; use the PG helper ONLY for advisory-lock + drain tests.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write migration 024 (columns + tables + status backfill)</name>
  <read_first>
    - server/api/migrations/023_connections_connected_at_index.sql (convention: plain SQL, header comment)
    - server/api/migrations/020_lava_payments.sql (table-creation style, jsonb usage)
    - server/api/migrations/migrations_test.go (the apply loop — confirm 024 is picked up by the generic loop, NOT in the skip list)
    - server/api/internal/model/lava_webhook_event.go (status will map to a new model field in 07-08)
  </read_first>
  <action>
Create server/api/migrations/024_admin_panel_overhaul.sql as a single transactional migration (NO CONCURRENTLY). Wrap in BEGIN; ... COMMIT;. Header comment: "-- 024: Phase 7 admin-panel-overhaul — per-user/server operational columns + feature_flags + broadcast_messages + webhook status".

Statements (exact):

  ALTER TABLE users ADD COLUMN IF NOT EXISTS suspended_at timestamptz;
  ALTER TABLE users ADD COLUMN IF NOT EXISTS suspended_reason text;

  ALTER TABLE vpn_servers ADD COLUMN IF NOT EXISTS is_draining boolean NOT NULL DEFAULT false;
  ALTER TABLE vpn_servers ADD COLUMN IF NOT EXISTS last_seen_at timestamptz;

  ALTER TABLE lava_webhook_events ADD COLUMN IF NOT EXISTS status varchar(16) NOT NULL DEFAULT 'PENDING';
  -- backfill historical rows so the webhook log renders correct status
  UPDATE lava_webhook_events SET status = 'FAILED'    WHERE error IS NOT NULL;
  UPDATE lava_webhook_events SET status = 'DELIVERED' WHERE error IS NULL AND processed_at IS NOT NULL;
  ALTER TABLE lava_webhook_events ADD COLUMN IF NOT EXISTS retried_count integer NOT NULL DEFAULT 0;

  CREATE TABLE IF NOT EXISTS feature_flags (
    key         varchar(64) PRIMARY KEY,
    value       boolean NOT NULL DEFAULT false,
    updated_at  timestamptz NOT NULL DEFAULT now(),
    updated_by  uuid
  );
  -- seed the three flags Phase 7 toggles so reads never miss
  INSERT INTO feature_flags (key, value) VALUES
    ('signups_off', false), ('payments_off', false), ('maintenance_mode', false)
    ON CONFLICT (key) DO NOTHING;

  CREATE TABLE IF NOT EXISTS broadcast_messages (
    id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
    title       varchar(200) NOT NULL,
    body        text NOT NULL,
    severity    varchar(16) NOT NULL DEFAULT 'info',
    locale      varchar(8),
    target_tier varchar(32),
    starts_at   timestamptz,
    ends_at     timestamptz,
    created_at  timestamptz NOT NULL DEFAULT now()
  );
  CREATE INDEX IF NOT EXISTS idx_broadcasts_active ON broadcast_messages (starts_at, ends_at);

Do NOT create an admin_audit_log table (audit_log already exists). Do NOT create a generic webhook_events table (use existing lava_webhook_events). These are LOCKED constraints.
  </action>
  <verify>
    <automated>cd server/api && go test ./migrations/ -run TestMigrations019_020 -count=1</automated>
  </verify>
  <done>migrations_test.go passes (024 applied in the generic loop without sequencing error); a psql \d on a migrated DB shows users.suspended_at, vpn_servers.is_draining, vpn_servers.last_seen_at, lava_webhook_events.status, and the feature_flags + broadcast_messages tables.</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable real-Postgres test helper</name>
  <read_first>
    - server/api/internal/repository/ctx_cancel_test.go (startPostgresGorm pattern — testcontainers + pgx + gorm)
    - server/api/migrations/024_admin_panel_overhaul.sql (the helper must apply all migrations including 024)
  </read_first>
  <action>
Create server/api/internal/testutil/pg.go (package testutil). Export:

  func StartPostgres(t *testing.T) *gorm.DB

It must:
  1. Call t.Helper().
  2. If testing.Short() OR a quick docker-availability probe fails, call t.Skip("integration: no docker/testcontainers available") and return nil — callers must guard on testing.Short() too. (Mirror the skip style; do NOT t.Fatal when Docker is absent so CI without Docker stays green.)
  3. Spin postgres:16-alpine via tcpostgres.Run exactly as ctx_cancel_test.go does (test/test/test, wait.ForLog "database system is ready to accept connections" occurrence 2, 60s startup).
  4. CREATE EXTENSION IF NOT EXISTS pgcrypto (gen_random_uuid).
  5. Apply EVERY server/api/migrations/*.sql file in lexicographic order against the container (resolve the migrations dir relative to this file via runtime.Caller so it works regardless of cwd; reuse the same split-on-CONCURRENTLY logic as migrations_test.go::applyMigration — copy it into the helper, it is small).
  6. Open a gorm DB on the pgx postgres driver (gorm.io/driver/postgres) with silent logger and return it.
  7. Register t.Cleanup to Terminate the container.

This helper is the ONLY way the ADMIN-03 and ADMIN-04 integration tests get a DB where pg_advisory_xact_lock(hashtextextended(...)) is real.
  </action>
  <verify>
    <automated>cd server/api && go vet ./internal/testutil/ && go build ./internal/testutil/</automated>
  </verify>
  <done>Package testutil compiles; StartPostgres is exported with signature func(*testing.T) *gorm.DB; it applies migrations 001..024 and returns a live gorm DB, or t.Skips when Docker is absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED stub tests for ADMIN-03/06 (integration) + ADMIN-01/04/07 (handler)</name>
  <read_first>
    - server/api/internal/testutil/pg.go (the helper from Task 2)
    - server/api/internal/handler/webhook_lava_test.go (SQLite setup + Fiber app.Test pattern)
    - server/api/internal/handler/admin_test.go (admin handler test wiring with AuthRequired/AdminRequired locals)
    - .planning/phases/07-admin-panel-overhaul/07-VALIDATION.md (SC-1..SC-6 → test names)
  </read_first>
  <behavior>
    - TestForceCancelWebhookRace (integration/admin_concurrency_test.go): seeds a pro user on a real PG DB; launches two goroutines — one calling repository.WithUserLock(...force-cancel logic...) and one calling the webhook tier-grant path — concurrently; asserts final users.subscription_tier is deterministic (NOT a hybrid where tier=free but lava_contracts.is_active=true, or vice versa). RED now because repository.WithUserLock does not exist yet (compile-fail is acceptable RED).
    - TestWebhookReplayIdempotent (integration/webhook_replay_test.go): inserts a payment.success lava_webhook_events row, calls handler.applyLavaEvent twice with the stored payload; asserts exactly one tier grant (tier=pro, expires_at unchanged on 2nd call) and status flips to 'REPLAYED'. RED now because applyLavaEvent does not exist yet.
    - TestAdminGetStatsKPIs (handler/admin_kpis_test.go, SQLite): asserts GET /admin/stats body includes keys paid_users, mrr, active_connections, signups_today, signups_week, signups_month, churn_30d, failed_payments_30d. RED now because those fields are absent.
    - TestServerDrainHidesFromPublic (handler/admin_kpis_test.go or a servers stub, SQLite): asserts after setting is_draining=true a non-admin /servers omits the server. RED now (filter not added).
    - TestReadyzLivez (handler/health_endpoints_test.go, SQLite): /livez always 200; /readyz 503 when a dep is forced unhealthy. RED now (handlers absent).
    - TestMaintenanceMiddleware (middleware/maintenance_test.go) + TestSuspendedMiddleware (middleware/suspended_test.go): RED stubs referencing middleware.Maintenance / middleware.SuspendedRequired which do not exist yet.
  </behavior>
  <action>
Write the six RED test files listed in files_modified. Each test must reference the EXACT downstream symbol names the implementing plans will create, so RED is a real compile/assert failure that turns GREEN when that plan lands:
  - repository.WithUserLock(ctx, db, userID, fn) — 07-05
  - handler.applyLavaEvent(ctx, db, redis, logger, ev) + model.LavaWebhookEvent.Status/RetriedCount — 07-08
  - the new /admin/stats KPI keys — 07-02
  - repository.ListActiveServers AND is_draining=false filter — 07-06
  - handler.Livez / handler.Readyz — 07-03
  - middleware.Maintenance(db, redis, logger) + middleware.SuspendedRequired(db) — 07-04 and 07-07

Integration tests (admin_concurrency_test.go, webhook_replay_test.go) MUST start with: `if testing.Short() { t.Skip(...) }` then `db := testutil.StartPostgres(t); if db == nil { return }`. Put them in package `integration` under server/api/integration/ (the dir already exists). Use build comments only if matching existing integration files; otherwise plain test files run under `go test ./integration/`.

Use t.Skip with a clear "RED: <symbol> not implemented yet (plan 07-0X)" message guarded behind a `const redPending = true` toggle ONLY IF a compile reference is impossible; PREFER a real compile-failing reference so the executor of the downstream plan gets an immediate GREEN signal. Since referencing a non-existent symbol breaks compilation of the whole package, gate each not-yet-existent reference behind its own file and add a top-of-file comment "// RED until plan 07-0X lands; remove the t.Skip and the stub once the symbol exists." Mark the body with t.Skip so `go test ./...` stays green for OTHER packages — i.e. the stub test file must itself COMPILE (do not reference undefined symbols); express the RED expectation as a t.Skip("RED: pending 07-0X — asserts <behavior>") with the assertion written as a comment. The downstream plan replaces the t.Skip with the real assertion.
  </action>
  <verify>
    <automated>cd server/api && go test ./integration/ ./internal/handler/ ./internal/middleware/ -run 'ForceCancelWebhookRace|WebhookReplayIdempotent|AdminGetStatsKPIs|ServerDrain|ReadyzLivez|Maintenance|Suspended' -count=1</automated>
  </verify>
  <done>All six test files compile and run (skipping with a clear "RED: pending 07-0X" message); `go test ./...` for the whole server/api module stays green (no broken compilation). Each skip names the plan that turns it GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration → live DB | DDL runs against production Postgres on deploy |
| test container → host | testcontainers spawns a Docker postgres; ephemeral, terminated in t.Cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | migration 024 status backfill | mitigate | Backfill is deterministic (error→FAILED, processed→DELIVERED); idempotent via ADD COLUMN IF NOT EXISTS + ON CONFLICT DO NOTHING so a re-run cannot corrupt rows |
| T-07-02 | Denial of Service | migration locking on ALTER TABLE | accept | Single-VM, low write volume; ADD COLUMN with DEFAULT on PG16 is metadata-only (no table rewrite) for the bool/text columns; acceptable lock window |
| T-07-03 | Info Disclosure | feature_flags seed values | accept | Flags are operational booleans, no secrets; updated_by is an admin uuid only |
</threat_model>

<verification>
- `cd server/api && go test ./migrations/ -run TestMigrations019_020 -count=1` green (024 applies cleanly in sequence).
- `cd server/api && go build ./... && go vet ./internal/testutil/` green.
- `cd server/api && go test ./... -short -count=1` green (RED stubs skip cleanly; nothing breaks compilation).
</verification>

<success_criteria>
- Migration 024 exists, is sequential after 023, adds exactly the LOCKED columns/tables and the status backfill, creates NO admin_audit_log and NO generic webhook_events table.
- testutil.StartPostgres compiles, applies all migrations, and skips gracefully without Docker.
- Six RED test files exist, compile, and each names the downstream plan (07-02..07-08) that turns it GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-panel-overhaul/07-01-SUMMARY.md`.
</output>
