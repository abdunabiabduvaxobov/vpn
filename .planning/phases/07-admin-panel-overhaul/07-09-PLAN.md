---
phase: 07-admin-panel-overhaul
plan: 09
type: execute
wave: 8
depends_on: [01, 03, 08]
files_modified:
  - server/api/internal/handler/admin_system.go
  - server/api/internal/repository/server_repo.go
  - server/api/internal/handler/admin_deps_health_test.go
  - server/api/internal/middleware/audit.go
  - server/api/cmd/main.go
autonomous: true
requirements: [ADMIN-08]
must_haves:
  truths:
    - "GET /admin/system/deps-health returns per-dependency live status for postgres, redis, lava, and a per-tunnel-server list with last_seen_at"
    - "Unlike public /readyz, the admin deps-health endpoint MAY include detail (per-server last_seen_at, current_load) because it is admin-authed"
    - "deps-health reuses the same probe logic as readyz (DB ping, Redis ping, cached lava reachability) — no duplicated flaky per-call lava dial"
  artifacts:
    - path: server/api/internal/handler/admin_system.go
      provides: "AdminDepsHealth handler returning the detailed dependency map"
      contains: "func AdminDepsHealth"
    - path: server/api/internal/repository/server_repo.go
      provides: "ListServerHealth returning per-server last_seen_at + current_load + is_active"
      contains: "func ListServerHealth"
  key_links:
    - from: server/api/internal/handler/admin_system.go
      to: server/api/internal/cache/health_cache.go
      via: "deps-health reads cached lava reachability (same source as readyz)"
      pattern: "GetLavaReachable"
---

<objective>
Implement ADMIN-08: the admin-only dependencies-health endpoint. It reuses the readyz probe logic (DB ping, Redis ping, cached lava reachability from 07-03) and adds a per-tunnel-server list with last_seen_at freshness — detail that is appropriate for an authenticated admin but must NOT leak from the public /readyz. The admin System page (07-10) polls this.

Per RESEARCH §ADMIN-08: admin-auth GET /admin/system/deps-health reuses the readyz probe + the vpn_servers table. The lava reachability comes from the same Redis-cached value as readyz (never a fresh per-call dial).

Purpose: ADMIN-08 success criterion — the deps-health page shows live status of DB, Redis, lava reachability, and tunnel-server heartbeat.
Output: AdminDepsHealth handler, ListServerHealth repo query, route on the audited admin group, GREEN deps-health test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-panel-overhaul/07-RESEARCH.md

<interfaces>
<!-- Confirmed from codebase this session. -->

From 07-03 (already landed): handler.Readyz does DB SELECT 1, Redis Ping, cache.GetLavaReachable, repository.CountFreshServers. Reuse the SAME building blocks here — extract the per-dep probe into a small reusable helper if 07-03 did not (read health.go first; if Readyz inlined the checks, factor a checkDeps(ctx, db, redis, lavaClient) returning a per-dep status map and have BOTH Readyz and AdminDepsHealth call it — but Readyz still returns ONLY status words to the public, while AdminDepsHealth returns the detailed version).

cache.GetLavaReachable (internal/cache/health_cache.go, from 07-03): cached ≤60s "ok"/"down".

server_repo.go: add
  type ServerHealthRow struct { ID, Hostname string; IsActive bool; CurrentLoad int; LastSeenAt *time.Time }
  func ListServerHealth(ctx, db) ([]ServerHealthRow, error)  // SELECT id, hostname, is_active, current_load, last_seen_at FROM vpn_servers ORDER BY hostname

Response shape (RESEARCH + PRD §6.6):
  GET /admin/system/deps-health → 200 {data:{
    postgres: "ok"|"down",
    redis:    "ok"|"down",
    lava:     "ok"|"down"|"unknown",
    tunnel_servers: [ {id, hostname, is_active, current_load, last_seen_at, fresh: bool} ]   // fresh = last_seen_at > now()-90s
  }}
This is admin-authed, so per-server detail is allowed (contrast with public /readyz which returns status words only — T-07-09).

cmd/main.go admin group: add admin.Get("/system/deps-health", handler.AdminDepsHealth(logger, db, redisClient, lavaClient)).
audit.go: deps-health is a GET so the AuditLog middleware skips it (GET is not audited) — no describeAction case needed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ListServerHealth repo + AdminDepsHealth handler (GREEN deps-health test)</name>
  <read_first>
    - server/api/internal/handler/health.go (Readyz from 07-03 — reuse its probe building blocks; factor checkDeps if inlined)
    - server/api/internal/cache/health_cache.go (GetLavaReachable from 07-03)
    - server/api/internal/repository/server_repo.go (CountFreshServers from 07-03 — add ListServerHealth alongside)
    - server/api/internal/handler/admin_deps_health_test.go (create the test; or reuse a stub if 07-01 left one)
  </read_first>
  <behavior>
    - AdminDepsHealth(db, redis, lavaClient): probes postgres (DB SELECT 1, 500ms timeout), redis (Ping, 500ms), lava (cache.GetLavaReachable), and lists per-server health via ListServerHealth with fresh = last_seen_at within 90s. Returns 200 with the detailed map above. Never blocks on a hung lava dial (uses the cached value).
  </behavior>
  <action>
Add ServerHealthRow + ListServerHealth to server_repo.go (ctx-threaded, plain SELECT). In internal/handler/admin_system.go (created in 07-07), add AdminDepsHealth(logger, db, redisClient, lavaClient) fiber.Handler returning the response shape above. Reuse the readyz probe helpers (factor a shared checkDeps in health.go if Readyz inlined them, so the DB/Redis/lava checks are not duplicated). Compute fresh per server = LastSeenAt != nil && LastSeenAt.After(now-90s).

Wire admin.Get("/system/deps-health", handler.AdminDepsHealth(logger, db, redisClient, lavaClient)) into cmd/main.go's admin group.

Create internal/handler/admin_deps_health_test.go (SQLite for the server list; the DB/Redis probes can be exercised against the in-memory DB + a nil/closed redis to assert the down-path): assert the response contains postgres/redis/lava keys and a tunnel_servers array, and that a server with a recent last_seen_at reports fresh=true while a stale one reports fresh=false.
  </action>
  <verify>
    <automated>cd server/api && go test ./internal/handler/ -run TestDepsHealth -count=1 && go build ./...</automated>
  </verify>
  <done>TestDepsHealth passes; GET /admin/system/deps-health returns postgres/redis/lava status + a per-server tunnel_servers list with last_seen_at + fresh flag; lava status comes from the cached value (no fresh dial); probe logic is shared with readyz (not duplicated); module builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin SPA → /admin/system/deps-health | authenticated admin read of infrastructure topology |
| handler → DB/Redis/lava-cache | internal probes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-37 | Info Disclosure | infra topology (hostnames, last_seen) exposed | mitigate | deps-health is on the AdminRequired group (admin-only); the PUBLIC /readyz still returns only status words — detail is gated behind admin auth (contrast T-07-09) |
| T-07-38 | DoS | deps-health dialing lava per poll | mitigate | reuses the ≤60s Redis-cached lava reachability (same source as readyz); DB/Redis checks have 500ms timeouts |
| T-07-39 | Elevation | deps-health reachable without admin | mitigate | route on the AdminRequired group (per-request DB role re-read, HOTFIX-02) |
</threat_model>

<verification>
- `cd server/api && go test ./internal/handler/ -run TestDepsHealth -count=1` green.
- `cd server/api && go build ./...` green.
- Manual: GET /admin/system/deps-health (as admin) → detailed dep map + per-server freshness; as non-admin → 403.
</verification>

<success_criteria>
- deps-health returns DB/Redis/lava status + per-tunnel-server last_seen_at + fresh flag, admin-only.
- Reuses the readyz probe (shared checkDeps), cached lava reachability — no flaky per-call dial.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-panel-overhaul/07-09-SUMMARY.md`.
</output>
