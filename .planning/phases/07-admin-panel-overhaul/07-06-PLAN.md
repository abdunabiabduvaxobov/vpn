---
phase: 07-admin-panel-overhaul
plan: 06
type: execute
wave: 5
depends_on: [01, 05]
files_modified:
  - server/api/internal/model/server.go
  - server/api/internal/repository/server_repo.go
  - server/api/internal/repository/connection_repo.go
  - server/api/internal/handler/admin_server_controls.go
  - server/api/internal/handler/admin_server_controls_test.go
  - server/api/internal/middleware/audit.go
  - server/api/cmd/main.go
autonomous: true
requirements: [ADMIN-04]
must_haves:
  truths:
    - "ListActiveServers filters AND is_draining = false so a drained server stops appearing in non-admin GET /servers"
    - "POST /admin/servers/:id/drain sets is_draining=true + busts cache:servers:active so GET /servers drops it within one request"
    - "POST /admin/servers/:id/undrain sets is_draining=false + busts cache"
    - "POST /admin/servers/:id/disconnect marks connections.disconnected_at by server_id (Option-B); live tunnels die on the existing stale sweep; throttled ≤1/server/60s"
    - "GET /admin/servers/:id/health returns concurrent_conns, last_seen_at, current_load"
  artifacts:
    - path: server/api/internal/handler/admin_server_controls.go
      provides: "AdminDrainServer / AdminUndrainServer / AdminDisconnectServer / AdminServerHealth handlers"
      contains: "func AdminDrainServer"
    - path: server/api/internal/repository/server_repo.go
      provides: "ListActiveServers draining filter + DisconnectConnectionsByServer"
      contains: "is_draining = false"
  key_links:
    - from: server/api/internal/handler/admin_server_controls.go
      to: server/api/internal/cache/servers_cache.go
      via: "drain/undrain bust cache:servers:active synchronously"
      pattern: "BustServersCache"
    - from: server/api/internal/repository/server_repo.go
      to: server/api/internal/handler/servers.go
      via: "ListActiveServers (used by GET /servers) gains is_draining=false"
      pattern: "is_draining"
---

<objective>
Implement ADMIN-04 server controls: drain mode (no new connections; existing survive), force-disconnect-all-clients-on-a-server, and a per-server health read. Per Option-B (LOCKED): drain = vpn_servers.is_draining=true + ListActiveServers adds AND is_draining=false + synchronous cache-bust so non-admin GET /servers drops the server within one request. force-disconnect = mark connections disconnected by server_id; live tunnels die on the existing ~3-min STALE_CONNECTION_AFTER sweep (NO Redis tunnel:kill channel — out of scope).

Purpose: ADMIN-04 success criterion — drained server vanishes from non-admin /servers (existing conns survive); force-disconnect-all marks every active client on that server.
Output: is_draining model field + ListActiveServers filter, drain/undrain/disconnect/health handlers, disconnect-by-server repo, throttle, cache-bust.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-admin-panel-overhaul/07-RESEARCH.md

<interfaces>
<!-- Confirmed from codebase this session. -->

migration 024 already added vpn_servers.is_draining (bool NOT NULL DEFAULT false) + vpn_servers.last_seen_at. Add the matching struct field to model.VPNServer:
  IsDraining bool `json:"is_draining" gorm:"column:is_draining;not null;default:false"`
  LastSeenAt *time.Time `json:"last_seen_at" gorm:"column:last_seen_at"`
(last_seen_at field may already be added by 07-03 Task1 if it added the field; if present, do not duplicate — read server.go first.)

repository.ListActiveServers (server_repo.go:13) currently:
  db.WithContext(ctx).Where("is_active = ?", true).Order("current_load ASC").Find(&servers)
Change WHERE to: Where("is_active = ? AND is_draining = ?", true, false). This is the load-bearing change for criterion 4 — a drained server must drop from the non-admin list. The admin path (ListAllServers) is unchanged so admins still see drained servers.

GET /servers (handler.ListServersCached) reads from cache:servers:active (PERF-01, 60s TTL). Therefore the drain/undrain handlers MUST cache.BustServersCache(ctx, redisClient) synchronously after the write (Pitfall 2), mirroring AdminUpdateServer (admin.go:481). The plan filter runs in Go post-cache, so busting the single global key is sufficient.

connection_repo.go: add
  func DisconnectConnectionsByServer(ctx, db, serverID string) (int64, error)
    // UPDATE connections SET disconnected_at=now() WHERE server_id=? AND disconnected_at IS NULL; return RowsAffected.

server health read needs: concurrent_conns = count(connections WHERE server_id=? AND disconnected_at IS NULL), plus current_load + last_seen_at from the server row. Add repository.CountActiveConnectionsByServer(ctx, db, serverID) (int64, error) if absent (read connection_repo.go — CountActiveConnections is per-user; add the per-server variant).

Throttle: cache.IncrRateLimit(ctx, redis, "disconnect:server:"+serverID, 60s); count>1 → 429 (Pitfall 7, ≤1/server/60s).

cmd/main.go admin group routes to add:
  admin.Post("/servers/:id/drain",      handler.AdminDrainServer(logger, db, redisClient))
  admin.Post("/servers/:id/undrain",    handler.AdminUndrainServer(logger, db, redisClient))
  admin.Post("/servers/:id/disconnect", handler.AdminDisconnectServer(logger, db, redisClient))
  admin.Get("/servers/:id/health",      handler.AdminServerHealth(logger, db))

audit.go describeAction: add cases drain_server / undrain_server / disconnect_server (these are POSTs under /admin/servers/:id/... and would otherwise fall to the generic label; order them BEFORE the existing update_server/create_server cases per the switch precedence comments).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: is_draining filter + disconnect-by-server + per-server count</name>
  <read_first>
    - server/api/internal/model/server.go (add IsDraining; check if LastSeenAt already added by 07-03)
    - server/api/internal/repository/server_repo.go (ListActiveServers — add is_draining filter)
    - server/api/internal/repository/connection_repo.go (DisconnectConnection + CountActiveConnections patterns)
  </read_first>
  <behavior>
    - ListActiveServers returns only is_active=true AND is_draining=false rows.
    - DisconnectConnectionsByServer marks all live connections on a server disconnected, returns count.
    - CountActiveConnectionsByServer returns count of live connections on a server.
  </behavior>
  <action>
Add IsDraining (and LastSeenAt if not already present) to model.VPNServer with the exact gorm tags above. Change ListActiveServers' WHERE to `Where("is_active = ? AND is_draining = ?", true, false)`. Add DisconnectConnectionsByServer + CountActiveConnectionsByServer to connection_repo.go (db.WithContext(ctx), parameterized server_id).
  </action>
  <verify>
    <automated>cd server/api && go build ./... && go test ./internal/handler/ -run TestServers -count=1</automated>
  </verify>
  <done>model.VPNServer has IsDraining; ListActiveServers excludes draining servers; DisconnectConnectionsByServer + CountActiveConnectionsByServer exist; existing servers tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: drain/undrain/disconnect/health handlers + cache-bust + throttle (GREEN the drain test)</name>
  <read_first>
    - server/api/internal/handler/admin.go (AdminUpdateServer cache-bust pattern at line 481; ErrNotFound→404; JSON envelope)
    - server/api/internal/cache/servers_cache.go (BustServersCache)
    - server/api/internal/cache/redis.go (IncrRateLimit)
    - server/api/internal/handler/admin_server_controls_test.go (RED TestServerDrainHidesFromPublic from 07-01 — make GREEN)
  </read_first>
  <behavior>
    - AdminDrainServer: body {force bool}. Set is_draining=true via UpdateServer; BustServersCache; if force → also DisconnectConnectionsByServer (subject to the throttle). Return 200 {data:{id, is_draining:true, killed_count:n}}.
    - AdminUndrainServer: set is_draining=false; BustServersCache; 200 {data:{id, is_draining:false}}.
    - AdminDisconnectServer: throttle IncrRateLimit("disconnect:server:"+id, 60s) → 429 if count>1; else DisconnectConnectionsByServer; 200 {data:{killed_count:n}}.
    - AdminServerHealth: 200 {data:{concurrent_conns, last_seen_at, current_load}} from CountActiveConnectionsByServer + the server row.
  </behavior>
  <action>
Create internal/handler/admin_server_controls.go with the four handlers. Use repository.UpdateServer(ctx, db, id, map[string]interface{}{"is_draining": true/false}) for drain toggling. BustServersCache best-effort (log Warn on failure, never fail the write — mirror AdminUpdateServer). Apply the throttle to AdminDisconnectServer and to the force-disconnect branch of AdminDrainServer. Wire the four routes into cmd/main.go's admin group; add describeAction cases. Edit admin_server_controls_test.go: remove the t.Skip on TestServerDrainHidesFromPublic; assert that after drain, repository.ListActiveServers omits the server while ListAllServers still includes it (SQLite is fine for this — the filter is plain SQL); assert disconnect twice within 60s → second 429.
  </action>
  <verify>
    <automated>cd server/api && go test ./internal/handler/ -run 'TestServerDrain|TestServers' -count=1 && go build ./...</automated>
  </verify>
  <done>TestServerDrainHidesFromPublic passes; drain sets is_draining + busts cache so non-admin /servers drops the server within one request; admin still sees it; force-disconnect marks connections by server_id and is throttled ≤1/server/60s; server health endpoint returns concurrent_conns/last_seen_at/current_load; routes audited with readable labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin SPA → /admin/servers/:id/* | authenticated admin mutation |
| drained server state → public GET /servers | the cached server list must reflect drain within one request |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-23 | DoS | force-disconnect blast radius on a busy server | mitigate | IncrRateLimit (atomic Lua) caps ≤1/server/60s → 429; UI confirm dialog echoes the target (07-10) |
| T-07-24 | Tampering | drained server still served from cache | mitigate | drain/undrain bust cache:servers:active synchronously; ListActiveServers also filters is_draining at the DB so a cache miss is correct too |
| T-07-25 | Elevation | server controls reachable without admin | mitigate | all routes on the AdminRequired audited group |
| T-07-26 | Availability | force-disconnect cannot kill live tunnels in real time | accept | Option-B (LOCKED): mark disconnected + drain stops new connects; live tunnels die on the existing ~3-min stale sweep — a documented weaker guarantee, no tunnel:kill channel this phase |
</threat_model>

<verification>
- `cd server/api && go test ./internal/handler/ -run 'TestServerDrain|TestServers' -count=1` green.
- `cd server/api && go build ./...` green.
- Manual: drain a server → non-admin GET /servers no longer lists it within one request; admin GET /admin/servers still lists it (is_draining=true); force-disconnect twice fast → 429.
</verification>

<success_criteria>
- ListActiveServers filters is_draining=false; drain/undrain bust the servers cache synchronously.
- force-disconnect marks connections by server_id (Option-B), throttled ≤1/server/60s.
- per-server health endpoint returns concurrent_conns/last_seen_at/current_load.
- The 07-01 RED drain test is GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-panel-overhaul/07-06-SUMMARY.md`.
</output>
