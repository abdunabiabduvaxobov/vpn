---
phase: 01-hotfix-audit-critical-fixes
plan: 03
type: execute
wave: 3
depends_on: ["01-hotfix-audit-critical-fixes/02"]
files_modified:
  - server/api/internal/handler/health.go
  - server/api/internal/handler/errorhandler_test.go
  - server/api/cmd/main.go
autonomous: true
requirements: [HOTFIX-04]
requirements_addressed: [HOTFIX-04]
threat_refs: [T-1-04]

must_haves:
  truths:
    - "Every 5xx response body equals exactly {error: 'internal server error', request_id: '<uuid>'} (D-05)"
    - "Raw err.Error() text from GORM/bcrypt/internal libs never reaches the client on 5xx (criterion #5)"
    - "4xx responses keep their existing verbose error messages (D-06)"
    - "Every response has an X-Request-ID header; incoming X-Request-ID is echoed verbatim, else UUIDv4 generated"
    - "Each 5xx is paired with one structured zap log line carrying the same request_id"
  artifacts:
    - path: "server/api/internal/handler/health.go"
      provides: "Updated ErrorHandler that scrubs 5xx body and emits structured log with request_id"
      contains: "request_id"
    - path: "server/api/internal/handler/errorhandler_test.go"
      provides: "Integration tests using Fiber app.Test() that prove scrub + header echo + 4xx pass-through"
    - path: "server/api/cmd/main.go"
      provides: "app.Use(requestid.New(...)) wired BEFORE recover.New() in the middleware chain"
      contains: "requestid.New"
  key_links:
    - from: "cmd/main.go"
      to: "github.com/gofiber/fiber/v2/middleware/requestid"
      via: "app.Use(requestid.New(...)) placed BEFORE app.Use(recover.New())"
      pattern: "requestid\\.New"
    - from: "internal/handler/health.go::ErrorHandler"
      to: "c.Locals(requestid)"
      via: "read by the handler to populate both the response body and the log line"
      pattern: "c\\.Locals\\(\"requestid\"\\)"
---

<objective>
Land HOTFIX-04: replace the verbose err.Error() body in Fiber ErrorHandler with the generic shape `{"error":"internal server error","request_id":"<uuid>"}` for 5xx responses (D-05). 4xx responses keep their existing verbose messages (D-06). Add Fiber built-in middleware/requestid to cmd/main.go BEFORE the recover middleware so panic-recovery paths also have a request_id. Use utils.UUIDv4 (not utils.UUID) to avoid leaking request count via monotonic IDs.

Purpose: closes CODE-REVIEW CRIT-04 and SECURITY-AUDIT S9-1. Implements ROADMAP §Phase 1 success criterion #5.

Output: one commit on the working branch with message `hotfix(01): scrub 5xx error bodies + X-Request-ID middleware [HOTFIX-04]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-hotfix-audit-critical-fixes/01-CONTEXT.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-RESEARCH.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-VALIDATION.md
@server/api/internal/handler/health.go
@server/api/cmd/main.go
@docs/audit/CODE-REVIEW.md
@docs/audit/SECURITY-AUDIT.md

<interfaces>
Fiber v2.52.5 ships middleware/requestid out of the box (verified at $GOPATH/pkg/mod/github.com/gofiber/fiber/v2@v2.52.5/middleware/requestid/requestid.go:13-30):
- Reads incoming X-Request-ID if present, else calls Generator()
- Sets the same value on the response header
- Stores in c.Locals(ContextKey), default ContextKey == "requestid"

Config (simplified):
- Header: string, default "X-Request-ID" (use fiber.HeaderXRequestID constant)
- Generator: func() string, default utils.UUID; this plan uses utils.UUIDv4
- ContextKey: string, default "requestid"

Current ErrorHandler in handler/health.go:155-172 returns a fiber.ErrorHandler closure. The leak site is line 168-170 which does `return c.JSON(fiber.Map{"error": err.Error()})`.
</interfaces>
</context>

<threat_model>
  <threat id="T-1-04" severity="high" category="Information Disclosure">
    <description>The current global ErrorHandler returns err.Error() verbatim in the JSON response body on every error path. GORM errors leak DB driver/library names (pq: connection refused, pq: duplicate key value violates unique constraint), bcrypt errors leak bcrypt: hashedPassword is not the hash of the given password, internal panics leak file paths and function names. Anyone hitting an endpoint that 500s receives free fingerprinting of the stack.</description>
    <mitigation>Task 2 rewrites ErrorHandler so 5xx responses return the fixed body {"error":"internal server error","request_id":"<uuid>"} per D-05. Full error chain still goes to the existing zap logger with the same request_id so operators can correlate the scrubbed client response to the structured log line. 4xx responses pass err.Error() through unchanged per D-06 (4xx is client-UX surface, not a leak surface). Fiber middleware/requestid is wired BEFORE recover.New() so even panic-recovery 5xx responses carry a request_id.</mitigation>
    <verification>Acceptance criteria in Task 2: cd server/api and go test ./internal/handler/... -v -run TestErrorHandler passes; tests assert that the response body for an induced 500 contains no pq:/bcrypt:/gorm: substring and exactly the two keys error and request_id. Smoke from plan 09 step 2 verifies on staging.</verification>
  </threat>
</threat_model>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 [BLOCKING Wave 0]: Create internal/handler/errorhandler_test.go test stub</name>
  <files>server/api/internal/handler/errorhandler_test.go</files>
  <read_first>
    - server/api/internal/handler/health.go (lines 155-172 — current ErrorHandler)
    - .planning/phases/01-hotfix-audit-critical-fixes/01-VALIDATION.md (rows 1-W0-03 and 1-04-01)
    - .planning/phases/01-hotfix-audit-critical-fixes/01-RESEARCH.md §HOTFIX-04 Verification recipe
    - server/api/internal/handler/auth_test.go (existing Fiber app.Test() pattern — match the style)
  </read_first>
  <behavior>
    Test stubs to declare (Task 2 implements bodies):
      - TestErrorHandler_Scrubs5xxBody: registers a route that returns a fake `pq: connection refused` error; asserts response body has exactly the two keys error/request_id and does NOT contain `pq:`.
      - TestErrorHandler_EchoesIncomingRequestID: sends request with header `X-Request-ID: smoke-1`; asserts response header AND body request_id both equal `smoke-1`.
      - TestErrorHandler_GeneratesUUIDv4WhenMissing: sends request without header; asserts the generated value matches the UUIDv4 regex `^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$`.
      - TestErrorHandler_PassesThrough4xx: registers a route that returns `fiber.NewError(400, "email required")`; asserts body contains `"error":"email required"` (D-06 invariant).
  </behavior>
  <action>
    Create the file `server/api/internal/handler/errorhandler_test.go` with EXACTLY this content (Task 2 will fill in real bodies and add additional imports — these stubs only need to compile and be discovered):

    File body (Go source — paste verbatim):
    ----
    package handler_test

    import "testing"

    // HOTFIX-04 regression test scaffolding. Real bodies land in plan 03 task 2.

    func TestErrorHandler_Scrubs5xxBody(t *testing.T) {
        t.Skip("HOTFIX-04: implemented in plan 03 task 2 — asserts 5xx body is generic and has request_id, no pq:/bcrypt:/gorm: leakage")
    }

    func TestErrorHandler_EchoesIncomingRequestID(t *testing.T) {
        t.Skip("HOTFIX-04: implemented in plan 03 task 2 — asserts incoming X-Request-ID is echoed in both header and body")
    }

    func TestErrorHandler_GeneratesUUIDv4WhenMissing(t *testing.T) {
        t.Skip("HOTFIX-04: implemented in plan 03 task 2 — asserts generated request_id matches UUIDv4 regex")
    }

    func TestErrorHandler_PassesThrough4xx(t *testing.T) {
        t.Skip("HOTFIX-04: implemented in plan 03 task 2 — asserts 4xx body keeps verbose error (D-06)")
    }
    ----

    Do NOT commit. Task 2 commits the full diff atomically.
  </action>
  <verify>
    <automated>test -f server/api/internal/handler/errorhandler_test.go &amp;&amp; cd server/api &amp;&amp; go vet ./internal/handler/... &amp;&amp; go test ./internal/handler/... -run TestErrorHandler_ -v -count=1 2>&amp;1 | grep -qE 'SKIP|PASS'</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f server/api/internal/handler/errorhandler_test.go` exits 0
    - All four test names declared: `grep -cE '^func TestErrorHandler_' server/api/internal/handler/errorhandler_test.go` returns 4
    - Package compiles: `cd server/api && go vet ./internal/handler/...` exits 0
    - All four stubs discovered as SKIP: `cd server/api && go test ./internal/handler/... -run TestErrorHandler_ -v -count=1 2>&1 | grep -c -- "--- SKIP: TestErrorHandler_"` returns 4
  </acceptance_criteria>
  <done>Test stub file exists with four discoverable test functions. Executor proceeds to Task 2 in the same working tree. If execution is interrupted before Task 2 completes, re-run Task 1 from scratch — do NOT skip ahead to Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite ErrorHandler to scrub 5xx + add requestid middleware to main.go; replace test SKIPs with real assertions; commit</name>
  <files>
    server/api/internal/handler/health.go,
    server/api/internal/handler/errorhandler_test.go,
    server/api/cmd/main.go
  </files>
  <read_first>
    - server/api/internal/handler/health.go (lines 155-172 — the function to replace; preserve everything else in the file)
    - server/api/cmd/main.go (lines 60-80 — the middleware chain where `app.Use(recover.New())` lives; `requestid.New(...)` must go BEFORE it)
    - .planning/phases/01-hotfix-audit-critical-fixes/01-RESEARCH.md §HOTFIX-04 Code change shape AND Wiring in cmd/main.go
    - .planning/phases/01-hotfix-audit-critical-fixes/01-CONTEXT.md D-05 (5xx body shape) and D-06 (4xx pass-through)
  </read_first>
  <behavior>
    After this task:
      - health.go ErrorHandler returns the closure described in RESEARCH §HOTFIX-04 Code change shape — 5xx body is `{"error":"internal server error","request_id":"<uuid>"}`, 4xx body is `{"error":"<err.Error()>"}`, and every error is logged once via `logger.Error` with fields request_id, status, path, method, plus `zap.Error(err)`.
      - cmd/main.go calls `app.Use(requestid.New(requestid.Config{Header: fiber.HeaderXRequestID, Generator: utils.UUIDv4}))` BEFORE `app.Use(recover.New())`.
      - Four test bodies replace the SKIPs and all PASS.
  </behavior>
  <action>
    Before editing the implementation file, verify the Task-1 test stub exists: `test -f server/api/internal/handler/errorhandler_test.go`. If missing, abort and restart Task 1.

    Step A — Rewrite ErrorHandler in `server/api/internal/handler/health.go`. Locate the function around lines 155-172. Replace its body with the implementation described verbatim in 01-RESEARCH.md §HOTFIX-04 "Code change shape" (the `func ErrorHandler(logger *zap.Logger) fiber.ErrorHandler` block). The key invariants:
      - Read `requestID, _ := c.Locals("requestid").(string)`.
      - Log once via `logger.Error("request error", zap.Int("status", code), zap.String("path", c.Path()), zap.String("method", c.Method()), zap.String("request_id", requestID), zap.Error(err))`.
      - On `code >= 500`, return JSON exactly `{"error": "internal server error", "request_id": requestID}` (two keys, no more).
      - On `code < 500`, return JSON `{"error": err.Error()}` — preserves D-06.
      - Keep the function signature `func ErrorHandler(logger *zap.Logger) fiber.ErrorHandler` unchanged so existing wiring in cmd/main.go (`handler.ErrorHandler(logger)`) keeps compiling.
      - Keep all other functions in health.go untouched.

    Step B — Wire requestid middleware in `server/api/cmd/main.go`. Add imports if missing:
      - `"github.com/gofiber/fiber/v2/middleware/requestid"`
      - `"github.com/gofiber/fiber/v2/utils"`
    Find the middleware chain. Insert IMMEDIATELY AFTER `app := fiber.New(...)` and IMMEDIATELY BEFORE `app.Use(recover.New())`:
        app.Use(requestid.New(requestid.Config{
            Header:    fiber.HeaderXRequestID,
            Generator: utils.UUIDv4,
        }))
    Order is critical (RESEARCH §HOTFIX-04 Risks): requestid MUST be before recover so panic-recovery paths have a request_id.

    Step C — Implement the four test bodies in `server/api/internal/handler/errorhandler_test.go`. Replace each `t.Skip(...)` with the real assertion described in `<behavior>`. Build a tiny test app inside the test file via a helper such as `newTestApp() *fiber.App` that:
      - Calls `fiber.New(fiber.Config{ErrorHandler: handler.ErrorHandler(zap.NewNop())})`.
      - Adds `app.Use(requestid.New(requestid.Config{Header: fiber.HeaderXRequestID, Generator: utils.UUIDv4}))`.
      - Registers `app.Get("/500-pq", func(c *fiber.Ctx) error { return fmt.Errorf("pq: connection refused") })` and `app.Get("/400-validate", func(c *fiber.Ctx) error { return fiber.NewError(400, "email required") })`.
    Then each test uses `httptest.NewRequest` + `app.Test(req)` to drive the handler. For TestErrorHandler_Scrubs5xxBody: parse the JSON body, assert exactly two keys (error == "internal server error", request_id non-empty), assert body string does NOT contain "pq:". For TestErrorHandler_EchoesIncomingRequestID: set header `X-Request-ID: smoke-1`, assert both response header and parsed JSON request_id equal "smoke-1". For TestErrorHandler_GeneratesUUIDv4WhenMissing: regex `^[0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$`. For TestErrorHandler_PassesThrough4xx: assert status 400 and body contains the substring `"error":"email required"`.
    Read the module path from the first line of `server/api/go.mod` to use the correct import for the `handler` package (do NOT guess `vpnapp/server/api/internal/handler` — use whatever is in go.mod).

    Step D — Run tests and build:
        cd server/api && go test ./internal/handler/... -v -count=1 -run TestErrorHandler_
        cd server/api && go build ./cmd
    All four TestErrorHandler_ tests MUST PASS (no SKIP).

    Step E — Commit. Single atomic commit. Subject line must match the regex `^hotfix\(01\): .*HOTFIX-04`. Recommended message:
      Subject: `hotfix(01): scrub 5xx error bodies + X-Request-ID middleware [HOTFIX-04]`
      Body (paragraphs):
        - "Closes CODE-REVIEW CRIT-04 and SECURITY-AUDIT S9-1. Implements ROADMAP §Phase 1 success criterion #5."
        - "Rewrite handler/health.go ErrorHandler so 5xx responses return the fixed generic body per D-05; 4xx responses keep their verbose err.Error() text per D-06 (client-UX surface, not a leak surface)."
        - "Every error is logged once via zap.Error with request_id, status, path, method so operators can correlate the scrubbed client response to the structured log line."
        - "Wire Fiber middleware/requestid in cmd/main.go BEFORE recover.New() so panic-recovery paths also carry a request_id. Use utils.UUIDv4 (not utils.UUID) to avoid leaking request count via monotonic IDs."
        - "Integration tests in internal/handler/errorhandler_test.go cover all four behaviors via Fiber app.Test()."
      Stage ONLY the three files in `files_modified` (use `git add <path>` explicitly per file, NOT `git add -A`).
  </action>
  <verify>
    <automated>cd server/api &amp;&amp; go test ./internal/handler/... -v -count=1 -run TestErrorHandler_ &amp;&amp; go build ./cmd &amp;&amp; cd /Users/abdunabi/Desktop/vpn &amp;&amp; git log -1 --format=%s | grep -qE '^hotfix\(01\): .*HOTFIX-04' &amp;&amp; grep -q 'internal server error' server/api/internal/handler/health.go &amp;&amp; grep -q 'requestid\.New' server/api/cmd/main.go &amp;&amp; grep -q 'utils\.UUIDv4' server/api/cmd/main.go</automated>
  </verify>
  <acceptance_criteria>
    - 5xx scrub body present: `grep -q '"internal server error"' server/api/internal/handler/health.go` exits 0
    - request_id field returned to client on 5xx: `grep -q '"request_id":\s*requestID' server/api/internal/handler/health.go` exits 0 (or equivalent — the literal `request_id` key MUST appear in the 5xx branch)
    - 4xx pass-through preserved (D-06 invariant): the function still references `err.Error()` somewhere in the < 500 branch: `grep -q 'err\.Error()' server/api/internal/handler/health.go` exits 0
    - requestid middleware wired: `grep -q 'requestid\.New' server/api/cmd/main.go` exits 0
    - UUIDv4 generator chosen (not UUID): `grep -q 'utils\.UUIDv4' server/api/cmd/main.go` exits 0
    - requestid wired BEFORE recover (ordering invariant — RESEARCH Risks): the line number of `requestid.New(` in cmd/main.go is LESS than the line number of `recover.New(`. Check with: `awk '/requestid\.New\(/{r=NR} /recover\.New\(/{c=NR} END{exit !(r>0 && c>0 && r<c)}' server/api/cmd/main.go` exits 0
    - All four tests PASS (no SKIP): `cd server/api && go test ./internal/handler/... -v -count=1 -run TestErrorHandler_ 2>&1 | grep -c -- "--- PASS: TestErrorHandler_"` returns 4
    - Binary builds: `cd server/api && go build ./cmd` exits 0
    - Commit message matches: `git log -1 --format=%s | grep -qE '^hotfix\(01\): .*HOTFIX-04'` exits 0
    - No regression in existing handler tests: `cd server/api && go test ./internal/handler/... -v -count=1` exits 0
  </acceptance_criteria>
  <done>One atomic commit `hotfix(01): scrub 5xx error bodies + X-Request-ID middleware [HOTFIX-04]` on the working branch. ErrorHandler scrubs 5xx, passes through 4xx, logs every error with request_id. requestid middleware is wired BEFORE recover. All four TestErrorHandler_* tests PASS.</done>
</task>

</tasks>

<verification>
1. `cd server/api && go test ./internal/handler/... -v -count=1 -run TestErrorHandler_` — all four tests PASS.
2. `cd server/api && go build ./cmd` — builds cleanly.
3. `grep -q '"internal server error"' server/api/internal/handler/health.go` — scrub body present.
4. `grep -q 'requestid\.New' server/api/cmd/main.go && grep -q 'utils\.UUIDv4' server/api/cmd/main.go` — middleware wired with UUIDv4 generator.
5. Ordering invariant holds: `requestid.New` appears before `recover.New` in cmd/main.go (awk check above).
6. `git log -1 --format=%s | grep -qE '^hotfix\(01\): .*HOTFIX-04'` — commit landed correctly.
</verification>

<success_criteria>
- The single commit `hotfix(01): scrub 5xx error bodies + X-Request-ID middleware [HOTFIX-04]` exists on the working branch.
- 5xx responses no longer leak GORM/bcrypt/internal error text (plan-09 smoke step 2 will re-verify on staging).
- Incoming `X-Request-ID` headers are echoed back; missing headers get a UUIDv4 (plan-09 smoke step 3).
- 4xx responses (e.g. validation errors) still carry their verbose `err.Error()` body for client UX.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hotfix-audit-critical-fixes/01-03-SUMMARY.md` with:
- Files changed + commit SHA
- Test output excerpt (four PASS lines for TestErrorHandler_*)
- Confirmation that requestid.New appears before recover.New in cmd/main.go (line numbers)
</output>
