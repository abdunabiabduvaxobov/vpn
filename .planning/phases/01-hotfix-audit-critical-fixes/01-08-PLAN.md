---
phase: 01-hotfix-audit-critical-fixes
plan: 08
type: execute
wave: 8
depends_on: ["01-hotfix-audit-critical-fixes/07"]
files_modified:
  - server/api/migrations/017_sessions_refresh_token_hash_unique.sql
  - server/api/scripts/smoke_test_session_index.sh
autonomous: true
requirements: [HOTFIX-07]
requirements_addressed: [HOTFIX-07]
threat_refs: [T-1-07]

must_haves:
  truths:
    - "EXPLAIN of SELECT * FROM sessions WHERE refresh_token_hash=$1 shows Index Scan using idx_sessions_refresh_token_hash_unique (not Seq Scan) — criterion #3"
    - "Duplicate refresh_token_hash inserts are rejected by the UNIQUE constraint"
    - "Migration is idempotent: re-running on a DB where the index already exists is a no-op (IF NOT EXISTS)"
    - "Migration dedupes any pre-existing duplicate hashes (keeps newest by created_at) before creating the UNIQUE index"
    - "Migration uses CREATE UNIQUE INDEX CONCURRENTLY so production writes are not blocked (the runner — Postgres docker-entrypoint-initdb.d, per A1 verification below — applies SQL files outside an auto-wrapped transaction)"
  artifacts:
    - path: "server/api/migrations/017_sessions_refresh_token_hash_unique.sql"
      provides: "Two-step migration: dedupe DELETE, then CREATE UNIQUE INDEX CONCURRENTLY"
      contains: "CREATE UNIQUE INDEX CONCURRENTLY"
    - path: "server/api/scripts/smoke_test_session_index.sh"
      provides: "Smoke test that runs EXPLAIN against staging Postgres and grep-asserts Index Scan"
  key_links:
    - from: "migrations/017_sessions_refresh_token_hash_unique.sql"
      to: "sessions table on Postgres 16"
      via: "psql -f via docker-entrypoint-initdb.d (no auto-tx wrap per A1)"
      pattern: "CREATE UNIQUE INDEX CONCURRENTLY"
    - from: "repository/session_repo.go:20"
      to: "idx_sessions_refresh_token_hash_unique"
      via: "WHERE refresh_token_hash = ? — Postgres planner picks the new UNIQUE index"
      pattern: "refresh_token_hash"
---

<objective>
Land HOTFIX-07: add a UNIQUE index on `sessions.refresh_token_hash` so `/auth/refresh` is an index lookup rather than a sequential scan (the current query in `session_repo.go:20` is a full seq scan). Per CONTEXT.md D-08 the migration is two-step: (1) dedupe any rows sharing the same hash (keep the newest by `created_at`); (2) `CREATE UNIQUE INDEX CONCURRENTLY idx_sessions_refresh_token_hash_unique ON sessions(refresh_token_hash)`. CONCURRENTLY cannot run inside a transaction — RESEARCH §HOTFIX-07 Assumption A1 states the migration runner applies SQL files outside auto-wrapped transactions. **A1 IS NOW VERIFIED:** the runner is Postgres's native `docker-entrypoint-initdb.d` (per `docker-compose.prod.yml:19 - ./migrations:/docker-entrypoint-initdb.d`), which applies each file via `psql -f` without wrapping in BEGIN/COMMIT. Single-file shape is therefore safe. Per D-02 this is the LAST commit of Phase 1 because schema changes are hardest to reverse.

Purpose: closes PERFORMANCE-AUDIT Perf #1. Implements ROADMAP §Phase 1 success criterion #3.

Output: one commit on the working branch with message `hotfix(01): UNIQUE index on sessions.refresh_token_hash + dedupe [HOTFIX-07]`.

This plan is structured as THREE tasks to honor Nyquist's per-VALIDATION row 1-W0-05 requirement that the smoke script is a dedicated BLOCKING Wave 0 stub:
  - Task 1 [Wave 0 — preflight]: verify A1 (the migration runner) and next sequential migration number.
  - Task 2 [BLOCKING Wave 0 — stub]: create `server/api/scripts/smoke_test_session_index.sh` (the test scaffold per VALIDATION row 1-W0-05). FAILS clearly when run against a DB that does not yet have the index.
  - Task 3: create the migration SQL and commit BOTH files atomically per D-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-hotfix-audit-critical-fixes/01-CONTEXT.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-RESEARCH.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-VALIDATION.md
@server/api/migrations/001_initial.sql
@server/api/internal/repository/session_repo.go
@docker-compose.prod.yml
@docs/audit/PERFORMANCE-AUDIT.md

<interfaces>
Migration runner (verified during planning):
  docker-compose.prod.yml:19 — `- ./migrations:/docker-entrypoint-initdb.d`
  This is Postgres's native init mechanism. The Postgres entrypoint script applies *.sql files in lexical order via `psql -v ON_ERROR_STOP=1 -f <file>`. Each file is applied OUTSIDE a wrapping BEGIN/COMMIT block (the entrypoint does not wrap; only the SQL author can put BEGIN inside a file). Therefore `CREATE INDEX CONCURRENTLY` works in a single file.

Existing migrations directory (server/api/migrations/):
  001_initial.sql through 016_add_telegram_profile.sql
  Next sequential number: 017

sessions table (migrations/001_initial.sql:18-28):
  CREATE TABLE sessions (
      id UUID PRIMARY KEY,
      user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
      refresh_token_hash TEXT NOT NULL,
      expires_at TIMESTAMPTZ NOT NULL,
      created_at TIMESTAMPTZ DEFAULT NOW()
  );
  CREATE INDEX idx_sessions_user_id ON sessions(user_id);
  CREATE INDEX idx_sessions_expires_at ON sessions(expires_at);
  -- NO index on refresh_token_hash today.

Query that benefits (server/api/internal/repository/session_repo.go:20):
  db.Where("refresh_token_hash = ? AND expires_at > ?", tokenHash, time.Now()).First(&session)

Postgres 16 EXPLAIN expected after the migration (per RESEARCH §HOTFIX-07 Verification recipe):
  Index Scan using idx_sessions_refresh_token_hash_unique on sessions
    Index Cond: ((refresh_token_hash)::text = '...'::text)
    Filter: (expires_at > now())
</interfaces>
</context>

<threat_model>
  <threat id="T-1-07" severity="high" category="Denial of Service">
    <description>The `/auth/refresh` query at session_repo.go:20 does a full sequential scan on the sessions table today (no index on refresh_token_hash). At ~100k sessions the cold-cache p99 is 10-50ms; at multi-million-row scale it brown-outs the auth hot path under load — a DoS amplifier where increased legitimate traffic degrades into a brownout. The missing index also leaves no UNIQUE constraint, so a future hash collision (theoretically impossible with SHA-256 of 32-byte random refresh tokens, but defense-in-depth) would silently overwrite session state.</description>
    <mitigation>Task 3 adds `migrations/017_sessions_refresh_token_hash_unique.sql` with two steps in order: (a) dedupe any rows sharing the same refresh_token_hash (keep newest by created_at) using `ROW_NUMBER() OVER (PARTITION BY refresh_token_hash ORDER BY created_at DESC)`; (b) `CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS idx_sessions_refresh_token_hash_unique ON sessions(refresh_token_hash)`. CONCURRENTLY does not lock the table for writes, so it is safe to run against a live production DB. UNIQUE also gives the planner a "rows = 0 or 1" hint that improves the EXPLAIN plan vs a plain INDEX. The Wave 0 smoke script in server/api/scripts/smoke_test_session_index.sh (Task 2) runs EXPLAIN and grep-asserts `Index Scan using idx_sessions_refresh_token_hash_unique`.</mitigation>
    <verification>Acceptance criteria in Task 3: the migration file exists at the correct path with the verbatim SQL; the smoke script (created in Task 2) runs `psql -c "EXPLAIN ..."` and greps for the expected Index Scan line. Manual: plan 09 step 8 runs the smoke script against staging Postgres.</verification>
  </threat>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Verify the migration runner (Assumption A1) and confirm next sequential migration number</name>
  <files></files>
  <read_first>
    - docker-compose.prod.yml (line ~19 — the `./migrations:/docker-entrypoint-initdb.d` mount that confirms the runner)
    - .github/workflows/deploy.yml (lines 109, 124, 126 — confirms migrations are shipped via this path)
    - server/api/Dockerfile (line 28 — confirms migrations are baked into the image)
    - server/api/internal/repository/db.go (lines 1-30 — confirms "Schema is managed by SQL migrations, not AutoMigrate")
    - .planning/phases/01-hotfix-audit-critical-fixes/01-RESEARCH.md §HOTFIX-07 "Risks / gotchas" (CONCURRENTLY in tx caveat) and Assumption A1
  </read_first>
  <action>
    Step A — Verify the migration runner is Postgres's native docker-entrypoint-initdb.d (no application-level migrate tool):
        grep -n 'docker-entrypoint-initdb.d' docker-compose.prod.yml
    Expected output: `19:      - ./migrations:/docker-entrypoint-initdb.d`. This confirms A1 — the Postgres entrypoint applies each .sql file via `psql -f` without wrapping in BEGIN/COMMIT, so `CREATE INDEX CONCURRENTLY` works in a single file.

    Step B — Confirm no application-level migrate runner is present:
        find server/api/cmd -maxdepth 2 -type d
        find server/api -name "Makefile" -maxdepth 3
        grep -rln "migrate" server/api/cmd/main.go 2>/dev/null
    Expected: no `golang-migrate`, no `goose`, no `Makefile` migrate target. If any of those ARE found, STOP and split this plan into 017a (DELETE in tx) + 017b (CREATE INDEX outside tx) — but per A1-verification this is not expected.

    Step C — Confirm the next sequential migration number:
        ls server/api/migrations/ | sort | tail -5
    Expected: `016_add_telegram_profile.sql` is the most recent. New file is `017_sessions_refresh_token_hash_unique.sql`.

    Step D — Document findings for the upcoming SUMMARY. Capture the exact command outputs above; they go into the commit body and the SUMMARY so future readers don't have to re-derive the assumption.

    No file is written in this task — it's a pre-flight verification. If any of the above three checks return UNEXPECTED output, STOP and surface the deviation in the SUMMARY before proceeding to Task 2.
  </action>
  <verify>
    <automated>grep -q 'docker-entrypoint-initdb.d' /Users/abdunabi/Desktop/vpn/docker-compose.prod.yml &amp;&amp; ls /Users/abdunabi/Desktop/vpn/server/api/migrations/016_add_telegram_profile.sql &amp;&amp; ! ls /Users/abdunabi/Desktop/vpn/server/api/migrations/017_sessions_refresh_token_hash_unique.sql 2>/dev/null</automated>
  </verify>
  <acceptance_criteria>
    - Migration runner confirmed as docker-entrypoint-initdb.d: `grep -q 'docker-entrypoint-initdb.d' docker-compose.prod.yml` exits 0
    - Existing 016 migration present: `test -f server/api/migrations/016_add_telegram_profile.sql` exits 0
    - 017 file NOT yet present (Task 3 creates it): `! test -f server/api/migrations/017_sessions_refresh_token_hash_unique.sql` exits 0
    - No application-level migrator found that would auto-wrap transactions: `! grep -rln "golang-migrate\|goose" server/api/cmd/ 2>/dev/null` exits 0 (or returns no output)
  </acceptance_criteria>
  <done>A1 verified — migration runner is Postgres docker-entrypoint-initdb.d (no auto-tx wrap). Next sequential number is 017. Single-file shape is safe. Executor proceeds to Task 2.</done>
</task>

<task type="auto">
  <name>Task 2 [BLOCKING Wave 0]: Create scripts/smoke_test_session_index.sh (the test stub per VALIDATION row 1-W0-05)</name>
  <files>server/api/scripts/smoke_test_session_index.sh</files>
  <read_first>
    - .planning/phases/01-hotfix-audit-critical-fixes/01-VALIDATION.md (row 1-W0-05 — this task IS the Wave 0 artifact that satisfies that row)
    - .planning/phases/01-hotfix-audit-critical-fixes/01-RESEARCH.md §HOTFIX-07 "Verification recipe" — the canonical EXPLAIN check shape
    - server/api/migrations/001_initial.sql (lines 18-28 — confirm the column name `refresh_token_hash` for the EXPLAIN query)
  </read_first>
  <behavior>
    The smoke script exists, is executable, and is syntactically valid BEFORE the migration is written in Task 3. When run against a DB that does NOT yet have the UNIQUE index (e.g., right now — pre-migration), it MUST exit with a clear FAIL message so the operator immediately sees the gate is meaningful. When run against a DB that DOES have the index (post-migration, post-deploy), it exits 0.

    This is the Wave 0 contract: the test fails first (no index), then implementation (Task 3) makes it pass. Per VALIDATION row 1-W0-05, the script is the Wave 0 artifact for HOTFIX-07 — it MUST exist BEFORE the migration.
  </behavior>
  <action>
    Create `server/api/scripts/smoke_test_session_index.sh` with EXACTLY this content. After writing, run `chmod +x server/api/scripts/smoke_test_session_index.sh`.

    File body (Bash — paste verbatim):
    ----
    #!/usr/bin/env bash
    # HOTFIX-07 smoke test: confirms the UNIQUE index on sessions.refresh_token_hash
    # is being used by the query planner. Maps to ROADMAP Phase 1 success criterion #3
    # and to VALIDATION.md row 1-W0-05 (Wave 0 artifact for HOTFIX-07).
    #
    # Pre-migration (run right after this script is created in Task 2): exits 1 with a
    # clear "Seq Scan on sessions" or "index not found" message because the UNIQUE index
    # idx_sessions_refresh_token_hash_unique does not yet exist. That is the Wave 0
    # RED state — proves the gate is meaningful.
    #
    # Post-migration (after Task 3's 017_...sql is applied to staging): exits 0 with
    # "OK: query planner is using idx_sessions_refresh_token_hash_unique". That is
    # the Wave 0 GREEN state.
    #
    # Usage:
    #   DATABASE_URL=postgres://... ./scripts/smoke_test_session_index.sh
    #   ./scripts/smoke_test_session_index.sh "postgres://..."
    #
    # Exit codes:
    #   0  EXPLAIN output contains "Index Scan using idx_sessions_refresh_token_hash_unique"
    #   1  Output contains "Seq Scan on sessions" (REGRESSION -- index not being used)
    #      OR the expected Index Scan line is missing
    #   2  psql failed to connect, or sessions table not found
    set -euo pipefail

    DB_URL="${1:-${DATABASE_URL:-}}"
    if [[ -z "$DB_URL" ]]; then
      echo "ERROR: DATABASE_URL not set and no first argument provided" >&2
      exit 2
    fi

    EXPLAIN_OUT=$(psql "$DB_URL" -X -A -t -c \
      "EXPLAIN SELECT * FROM sessions WHERE refresh_token_hash = 'deadbeefdeadbeefdeadbeefdeadbeef' AND expires_at > NOW();" 2>&1) || {
        echo "ERROR: psql failed:" >&2
        echo "$EXPLAIN_OUT" >&2
        exit 2
    }

    echo "EXPLAIN output:"
    echo "$EXPLAIN_OUT"
    echo

    if echo "$EXPLAIN_OUT" | grep -q "Index Scan using idx_sessions_refresh_token_hash_unique"; then
      echo "OK: query planner is using idx_sessions_refresh_token_hash_unique"
      exit 0
    fi

    if echo "$EXPLAIN_OUT" | grep -q "Seq Scan on sessions"; then
      echo "FAIL: query planner is doing a sequential scan -- HOTFIX-07 index not in use" >&2
      echo "      (expected after Task 3 / plan 09 step 8 migration apply; pre-migration this FAIL is intentional)" >&2
      exit 1
    fi

    echo "FAIL: EXPLAIN output did not contain the expected Index Scan line" >&2
    exit 1
    ----

    Do NOT commit this file yet — it is committed alongside the migration in Task 3 (single atomic commit per D-01).

    Confirm the script is syntactically valid:
        bash -n server/api/scripts/smoke_test_session_index.sh
    Exits 0 on success.

    Confirm the script is executable:
        test -x server/api/scripts/smoke_test_session_index.sh
    Exits 0 on success (re-run `chmod +x` if not).
  </action>
  <verify>
    <automated>test -f server/api/scripts/smoke_test_session_index.sh &amp;&amp; test -x server/api/scripts/smoke_test_session_index.sh &amp;&amp; bash -n server/api/scripts/smoke_test_session_index.sh &amp;&amp; grep -q "idx_sessions_refresh_token_hash_unique" server/api/scripts/smoke_test_session_index.sh &amp;&amp; grep -q "Index Scan" server/api/scripts/smoke_test_session_index.sh &amp;&amp; grep -q "set -euo pipefail" server/api/scripts/smoke_test_session_index.sh</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f server/api/scripts/smoke_test_session_index.sh` exits 0
    - File is executable: `test -x server/api/scripts/smoke_test_session_index.sh` exits 0
    - Bash syntax is valid: `bash -n server/api/scripts/smoke_test_session_index.sh` exits 0
    - Script greps for the canonical index name: `grep -q "idx_sessions_refresh_token_hash_unique" server/api/scripts/smoke_test_session_index.sh` exits 0
    - Script greps for "Index Scan" (the EXPLAIN marker per RESEARCH §HOTFIX-07): `grep -q "Index Scan" server/api/scripts/smoke_test_session_index.sh` exits 0
    - Script uses safe bash defaults: `grep -q "set -euo pipefail" server/api/scripts/smoke_test_session_index.sh` exits 0
    - Script emits a clear FAIL message pre-migration: `grep -q "FAIL: query planner is doing a sequential scan" server/api/scripts/smoke_test_session_index.sh` exits 0
  </acceptance_criteria>
  <done>The Wave 0 smoke-script stub for HOTFIX-07 exists in the working tree, is executable, and has valid bash syntax. Pre-migration it would exit 1 with a clear FAIL message (RED state). Per D-01 the file is NOT committed yet — Task 3 commits it atomically with the migration SQL. If execution is interrupted before Task 3 completes, re-run Task 2 from scratch — do NOT skip ahead to Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Write migrations/017_sessions_refresh_token_hash_unique.sql; commit both files atomically</name>
  <files>
    server/api/migrations/017_sessions_refresh_token_hash_unique.sql,
    server/api/scripts/smoke_test_session_index.sh
  </files>
  <read_first>
    - server/api/migrations/001_initial.sql (lines 18-28 — sessions table schema)
    - server/api/migrations/016_add_telegram_profile.sql (so the executor matches the existing SQL header/style)
    - .planning/phases/01-hotfix-audit-critical-fixes/01-RESEARCH.md §HOTFIX-07 (full section — particularly the migration file content + Risks/gotchas + Verification recipe)
    - .planning/phases/01-hotfix-audit-critical-fixes/01-CONTEXT.md D-08 (two-step migration, dedupe by MAX(created_at), then CONCURRENTLY)
    - docs/audit/PERFORMANCE-AUDIT.md (search "Perf #1" — for EXPLAIN expectations)
  </read_first>
  <behavior>
    After this task:
      - File `server/api/migrations/017_sessions_refresh_token_hash_unique.sql` exists containing:
        1. A header comment block explaining the two-step migration AND the CONCURRENTLY-cannot-run-in-tx caveat AND a note pointing at A1 verification (runner is Postgres docker-entrypoint-initdb.d, no auto-tx wrap).
        2. Step 1: a `DELETE FROM sessions WHERE id IN (SELECT id FROM (SELECT id, ROW_NUMBER() OVER (PARTITION BY refresh_token_hash ORDER BY created_at DESC) AS rn FROM sessions) ranked WHERE rn > 1);`
        3. Step 2: `CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS idx_sessions_refresh_token_hash_unique ON sessions (refresh_token_hash);`
      - The smoke script created in Task 2 remains untouched.
      - Both files are committed in a SINGLE atomic commit per D-01.
  </behavior>
  <action>
    Before editing the migration file, verify the Task-2 smoke script exists in the working tree: `test -f server/api/scripts/smoke_test_session_index.sh && test -x server/api/scripts/smoke_test_session_index.sh && bash -n server/api/scripts/smoke_test_session_index.sh`. If missing or invalid, abort and restart Task 2.

    Step A — Create `server/api/migrations/017_sessions_refresh_token_hash_unique.sql` with EXACTLY this content (mirror the comment style from 01-RESEARCH.md §HOTFIX-07 "Migration file"):

    File body (SQL — paste verbatim):
    ----
    -- HOTFIX-07: UNIQUE index on sessions.refresh_token_hash for /auth/refresh O(1) lookup.
    -- Per PERFORMANCE-AUDIT Perf #1 and ROADMAP Phase 1 success criterion #3.
    --
    -- The query at repository/session_repo.go:20 does a full sequential scan today;
    -- this index makes it an index lookup. The hash is SHA-256 of a 30-day-lived
    -- refresh token, so uniqueness is statistically guaranteed -- UNIQUE also gives
    -- the planner a "row count = 0 or 1" hint that improves the EXPLAIN plan.
    --
    -- Step 1: deduplicate any rows sharing the same refresh_token_hash. In v2.1.0
    -- with no paying users this should find zero rows in production, but staging
    -- and dev DBs may have stale guest sessions from testing. Keep the newest row
    -- per hash (by created_at), delete the rest. The sessions.user_id FK is
    -- ON DELETE CASCADE on the users side; deleting sessions themselves does not
    -- cascade.
    --
    -- Step 2: CREATE UNIQUE INDEX CONCURRENTLY -- does NOT lock the table for
    -- writes, so safe to run against a live production database.
    --
    -- IMPORTANT: CREATE INDEX CONCURRENTLY cannot run inside a transaction block.
    -- This file is applied by Postgres's native docker-entrypoint-initdb.d
    -- (mounted at docker-compose.prod.yml:19 - ./migrations:/docker-entrypoint-initdb.d),
    -- which runs `psql -f` per file WITHOUT wrapping in BEGIN/COMMIT. Verified
    -- during planning (see .planning/phases/01-hotfix-audit-critical-fixes/01-RESEARCH.md
    -- Assumption A1). If a future deploy moves to golang-migrate or goose with
    -- auto-tx wrap, this file MUST be split into 017a (DELETE inside tx) and
    -- 017b (CREATE INDEX CONCURRENTLY outside tx) -- and the file will need
    -- the runner-specific marker to disable wrapping (e.g. `-- migrate:no-tx`
    -- for golang-migrate).

    -- Step 1: dedupe -- keep the newest session per refresh_token_hash.
    DELETE FROM sessions
    WHERE id IN (
        SELECT id FROM (
            SELECT id,
                   ROW_NUMBER() OVER (PARTITION BY refresh_token_hash ORDER BY created_at DESC) AS rn
            FROM sessions
        ) ranked
        WHERE rn > 1
    );

    -- Step 2: create the UNIQUE index without locking writes.
    CREATE UNIQUE INDEX CONCURRENTLY IF NOT EXISTS idx_sessions_refresh_token_hash_unique
        ON sessions (refresh_token_hash);
    ----

    Step B — Lint the migration. Confirm the SQL parses by running `psql --dry-run`-equivalent if available; otherwise visually inspect against `migrations/001_initial.sql:18-28` to confirm the column name `refresh_token_hash` matches. Confirm no semicolons inside the comment block.

    Step C — Re-confirm the Task 2 smoke script is still syntactically valid (it was not touched by this task, but verify nothing got corrupted between tasks):
        bash -n server/api/scripts/smoke_test_session_index.sh
    Exits 0.

    Step D — Run the full Go test suite to confirm no regression (the migration is not yet applied, so this is a sanity check that prior commits still hold):
        cd server/api && go test ./... -count=1
    Exits 0.

    Step E — Commit. Subject must match regex `^hotfix\(01\): .*HOTFIX-07`. Recommended message:
      Subject: `hotfix(01): UNIQUE index on sessions.refresh_token_hash + dedupe [HOTFIX-07]`
      Body paragraphs:
        - "Closes PERFORMANCE-AUDIT Perf #1. Implements ROADMAP §Phase 1 success criterion #3."
        - "Adds migrations/017_sessions_refresh_token_hash_unique.sql with the two-step shape from CONTEXT.md D-08: (1) DELETE any rows sharing the same refresh_token_hash, keeping the newest by created_at (defensive — production has no paying users so should be a no-op, but staging/dev DBs may have stale guest sessions from testing); (2) CREATE UNIQUE INDEX CONCURRENTLY idx_sessions_refresh_token_hash_unique ON sessions (refresh_token_hash) -- does not lock writes."
        - "RUNNER VERIFICATION (RESEARCH §HOTFIX-07 Assumption A1 was MARKED as needing verification — now verified during planning): the migration runner is Postgres's native docker-entrypoint-initdb.d, mounted at docker-compose.prod.yml:19. The Postgres entrypoint applies each *.sql file via `psql -f` without wrapping in BEGIN/COMMIT, so CREATE INDEX CONCURRENTLY in a single file is safe. If a future deploy switches to golang-migrate or goose with auto-tx wrap, the migration must be split into 017a/017b -- a comment in the SQL file documents this contingency."
        - "Also commits server/api/scripts/smoke_test_session_index.sh (Wave 0 stub created in Task 2 — satisfies VALIDATION.md row 1-W0-05). The script runs `psql -c \"EXPLAIN SELECT * FROM sessions WHERE refresh_token_hash='deadbeef'\"` and grep-asserts the expected `Index Scan using idx_sessions_refresh_token_hash_unique` line. Pre-migration the script exits 1 with FAIL (Wave 0 RED state); post-migration it exits 0 (Wave 0 GREEN state). Plan 09 step 8 runs this against staging before tag v2.2.0-hotfix."
        - "Per D-02 this is the LAST commit of Phase 1 because schema changes are hardest to reverse. Recommend taking a logical backup of `sessions` before applying."
      Stage ONLY the two files in files_modified (the migration SQL plus the smoke script from Task 2).
  </action>
  <verify>
    <automated>test -f server/api/migrations/017_sessions_refresh_token_hash_unique.sql &amp;&amp; test -x server/api/scripts/smoke_test_session_index.sh &amp;&amp; bash -n server/api/scripts/smoke_test_session_index.sh &amp;&amp; grep -q 'CREATE UNIQUE INDEX CONCURRENTLY' server/api/migrations/017_sessions_refresh_token_hash_unique.sql &amp;&amp; grep -q 'idx_sessions_refresh_token_hash_unique' server/api/migrations/017_sessions_refresh_token_hash_unique.sql &amp;&amp; grep -q 'ROW_NUMBER() OVER (PARTITION BY refresh_token_hash' server/api/migrations/017_sessions_refresh_token_hash_unique.sql &amp;&amp; cd server/api &amp;&amp; go test ./... -count=1 &amp;&amp; cd /Users/abdunabi/Desktop/vpn &amp;&amp; git log -1 --format=%s | grep -qE '^hotfix\(01\): .*HOTFIX-07'</automated>
  </verify>
  <acceptance_criteria>
    - Migration file exists: `test -f server/api/migrations/017_sessions_refresh_token_hash_unique.sql` exits 0
    - Migration name matches pattern enforced by sequential numeric prefix: `ls server/api/migrations/017_*.sql | wc -l` returns 1
    - CONCURRENTLY clause present: `grep -q 'CREATE UNIQUE INDEX CONCURRENTLY' server/api/migrations/017_sessions_refresh_token_hash_unique.sql` exits 0
    - IF NOT EXISTS clause (idempotent re-runs): `grep -q 'IF NOT EXISTS' server/api/migrations/017_sessions_refresh_token_hash_unique.sql` exits 0
    - Exact index name used (matches test/smoke expectations): `grep -q 'idx_sessions_refresh_token_hash_unique' server/api/migrations/017_sessions_refresh_token_hash_unique.sql` exits 0
    - Step 1 dedupe present with ROW_NUMBER: `grep -q 'ROW_NUMBER() OVER (PARTITION BY refresh_token_hash' server/api/migrations/017_sessions_refresh_token_hash_unique.sql` exits 0
    - A1-verification note in SQL file comments: `grep -q 'docker-entrypoint-initdb.d' server/api/migrations/017_sessions_refresh_token_hash_unique.sql` exits 0
    - Smoke script (from Task 2) still exists and is executable: `test -x server/api/scripts/smoke_test_session_index.sh` exits 0
    - Smoke script still has valid bash syntax: `bash -n server/api/scripts/smoke_test_session_index.sh` exits 0
    - Atomic commit touches BOTH files: `git diff-tree --no-commit-id --name-only -r HEAD | sort | tr '\n' ' '` contains BOTH `server/api/migrations/017_sessions_refresh_token_hash_unique.sql` AND `server/api/scripts/smoke_test_session_index.sh`
    - Full Go suite still green (no regression): `cd server/api && go test ./... -count=1` exits 0
    - Commit message matches: `git log -1 --format=%s | grep -qE '^hotfix\(01\): .*HOTFIX-07'` exits 0
  </acceptance_criteria>
  <done>One atomic commit `hotfix(01): UNIQUE index on sessions.refresh_token_hash + dedupe [HOTFIX-07]` containing BOTH the Task 2 smoke script and the Task 3 migration SQL. Bash syntax valid. Full Go suite still green. A1 invariant documented in the SQL comment block. Wave 0 contract honored: smoke script existed BEFORE the migration (Task 2 preceded Task 3), and now both are committed atomically per D-01.</done>
</task>

</tasks>

<verification>
1. `test -f server/api/migrations/017_sessions_refresh_token_hash_unique.sql && grep -q 'CREATE UNIQUE INDEX CONCURRENTLY' server/api/migrations/017_sessions_refresh_token_hash_unique.sql` — migration present and correct.
2. `test -x server/api/scripts/smoke_test_session_index.sh && bash -n server/api/scripts/smoke_test_session_index.sh` — smoke script present (per Task 2 Wave 0), valid bash.
3. `cd server/api && go test ./... -count=1` — full suite still green (no regression from earlier hotfix commits).
4. `git log -1 --format=%s | grep -qE '^hotfix\(01\): .*HOTFIX-07'` — commit landed correctly.
5. Atomic commit confirmed: `git diff-tree --no-commit-id --name-only -r HEAD` lists both files.
6. Plan 09 step 8 will run `bash server/api/scripts/smoke_test_session_index.sh "$STAGING_DATABASE_URL"` against staging Postgres after the migration is applied.
</verification>

<success_criteria>
- The single commit `hotfix(01): UNIQUE index on sessions.refresh_token_hash + dedupe [HOTFIX-07]` exists.
- After applying the migration on staging, `EXPLAIN SELECT * FROM sessions WHERE refresh_token_hash=...` returns `Index Scan using idx_sessions_refresh_token_hash_unique` (plan-09 step 8).
- Migration is idempotent (`IF NOT EXISTS`).
- A1 invariant about the migration runner is documented in the SQL file so future maintainers don't have to re-derive it.
- VALIDATION.md row 1-W0-05 is satisfied: the smoke script existed as a dedicated Wave 0 task (Task 2) BEFORE the implementation (Task 3).
</success_criteria>

<output>
After completion, create `.planning/phases/01-hotfix-audit-critical-fixes/01-08-SUMMARY.md` with:
- Commit SHA + message
- The two new file paths
- `bash -n` output (clean) and `cat` of the smoke script for review
- `cat` of the migration file (so reviewer can see the final shape without git diff)
- The Task 1 A1-verification command outputs (the `grep docker-entrypoint-initdb.d` line + the `ls migrations/` tail)
- Confirmation that Task 2 (Wave 0 smoke script stub) preceded Task 3 (migration SQL) within the same atomic commit, satisfying VALIDATION row 1-W0-05.
- Note: "Migration WILL be applied to staging by plan 09 step 8 (smoke). If A1 turns out wrong after all, split into 017a/017b per the SQL comment."
</output>
