---
phase: 01-hotfix-audit-critical-fixes
plan: 09
type: execute
wave: 9
depends_on: ["01-hotfix-audit-critical-fixes/08"]
files_modified: []
autonomous: false
requirements: [HOTFIX-01, HOTFIX-02, HOTFIX-03, HOTFIX-04, HOTFIX-05, HOTFIX-06, HOTFIX-07, HOTFIX-08]
requirements_addressed: [HOTFIX-01, HOTFIX-02, HOTFIX-03, HOTFIX-04, HOTFIX-05, HOTFIX-06, HOTFIX-07, HOTFIX-08]
threat_refs: [T-1-01, T-1-02, T-1-03, T-1-04, T-1-05, T-1-06, T-1-07, T-1-08]

must_haves:
  truths:
    - "All 8 hotfix commits exist on the working branch with the expected commit messages (per D-01)"
    - "Full Go test suite passes with -race on the entire repo: `cd server/api && go test ./... -race -count=1`"
    - "Staging deploy of the 8 commits succeeds; all 10 smoke steps from VALIDATION.md pass end-to-end"
    - "The `v2.2.0-hotfix` git tag is pushed ONLY AFTER every smoke step is green (operator explicitly appends the `<!-- ALL_SMOKE_STEPS_APPROVED -->` marker to SMOKE-RESULTS.md after every row is ✅)"
    - "ROADMAP success criteria 1-8 are each verifiably TRUE on staging"
  artifacts:
    - path: ".planning/phases/01-hotfix-audit-critical-fixes/01-09-SMOKE-RESULTS.md"
      provides: "Per-step pass/fail record of the 10-step staging smoke checklist (operator-filled during this plan)"
  key_links:
    - from: "git tag v2.2.0-hotfix"
      to: "all 8 hotfix commits + full smoke green"
      via: "tag only created after every checklist item is checked off AND the `<!-- ALL_SMOKE_STEPS_APPROVED -->` marker is present"
      pattern: "v2\\.2\\.0-hotfix"
---

<objective>
Phase 1 final integration plan. After plans 01-08 each land a single atomic hotfix commit on the working branch, this plan: (a) runs the full Go test suite with race detector across the entire repo as the cross-cutting CI gate; (b) deploys the 8 commits to staging via the project's existing deploy pipeline; (c) walks the operator through the 10-step staging smoke checklist from `.planning/phases/01-hotfix-audit-critical-fixes/01-VALIDATION.md` (which mirrors `01-RESEARCH.md §Validation Architecture`); (d) once every step is green AND the operator has explicitly appended the `<!-- ALL_SMOKE_STEPS_APPROVED -->` marker to SMOKE-RESULTS.md, pushes the `v2.2.0-hotfix` git tag.

This plan has ONE autonomous task (Go suite + commit-log verification) plus TEN human-verify checkpoint tasks (one per smoke step) plus ONE final autonomous task (tag creation, gated on every preceding checkpoint being approved AND the explicit marker being present in SMOKE-RESULTS.md).

Per CONTEXT.md D-01: single combined deploy at the end (no per-fix rolling deploys). Per RESEARCH §Recommendations item 9: pre-tag manual smoke is MANDATORY — green CI alone is insufficient because HOTFIX-02 and HOTFIX-05 in particular exercise live DB+Redis behavior that unit tests with sqlite/miniredis cannot fully prove.

Output: the `v2.2.0-hotfix` git tag pushed to the working branch HEAD (after all 8 commits + smoke approval).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-hotfix-audit-critical-fixes/01-CONTEXT.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-RESEARCH.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-VALIDATION.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-01-SUMMARY.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-02-SUMMARY.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-03-SUMMARY.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-04-SUMMARY.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-05-SUMMARY.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-06-SUMMARY.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-07-SUMMARY.md
@.planning/phases/01-hotfix-audit-critical-fixes/01-08-SUMMARY.md

<interfaces>
The 8 expected commit messages on the working branch (from plans 01-08), in landing order per D-02:
  1. hotfix(01): createadmin reads password from stdin + seeds tier=free [HOTFIX-06]
  2. hotfix(01): fail-fast aggregate env validator [HOTFIX-08]
  3. hotfix(01): scrub 5xx error bodies + X-Request-ID middleware [HOTFIX-04]
  4. hotfix(01): AdminRequired re-reads role from DB per request [HOTFIX-02]
  5. hotfix(01): atomic Lua INCR+EXPIRE for rate limiter [HOTFIX-03]
  6. hotfix(01): transactional refresh-token rotation [HOTFIX-05]
  7. hotfix(01): regression test for subscription downgrade (column+scheduler already correct) [HOTFIX-01]
  8. hotfix(01): UNIQUE index on sessions.refresh_token_hash + dedupe [HOTFIX-07]

Deploy mechanism (verified during planning):
  .github/workflows/deploy.yml runs on push and copies docker-compose.prod.yml and server/api/migrations to the staging VM.
  Migrations are applied by Postgres's docker-entrypoint-initdb.d on container init.
  For an EXISTING running stack, the operator may need to manually `psql -f server/api/migrations/017_sessions_refresh_token_hash_unique.sql $STAGING_DATABASE_URL` because docker-entrypoint-initdb.d only fires on a fresh Postgres data dir.

The 10 smoke steps are defined verbatim in 01-VALIDATION.md "Cross-Cutting Staging Smoke Checklist" (§lines 87-101). They are reproduced inline in each checkpoint task below for executor convenience.

**Approval marker convention:** The SMOKE-RESULTS.md template includes a footer line that says (commented out by default):
  `<!-- ALL_SMOKE_STEPS_APPROVED -->`
The operator UNCOMMENTS / appends this marker AFTER every row in the smoke table is ✅. Task 13 greps for the literal marker — this is more robust than counting ✅ characters (avoids accidental tag pushes when one row is ⚠️ while others are ✅; also avoids brittle column-spacing regex).
</interfaces>
</context>

<threat_model>
  <threat id="T-1-01" severity="high" category="Repudiation"><description>HOTFIX-01 — see plan 07</description><mitigation>Smoke step 7 verifies on staging</mitigation><verification>Step 7 acceptance below</verification></threat>
  <threat id="T-1-02" severity="high" category="Elevation of Privilege"><description>HOTFIX-02 — see plan 04</description><mitigation>Smoke step 4 verifies on staging</mitigation><verification>Step 4 acceptance below</verification></threat>
  <threat id="T-1-03" severity="high" category="Denial of Service"><description>HOTFIX-03 — see plan 05</description><mitigation>Smoke step 5 verifies on staging</mitigation><verification>Step 5 acceptance below</verification></threat>
  <threat id="T-1-04" severity="high" category="Information Disclosure"><description>HOTFIX-04 — see plan 03</description><mitigation>Smoke steps 2-3 verify on staging</mitigation><verification>Steps 2 and 3 acceptance below</verification></threat>
  <threat id="T-1-05" severity="high" category="Tampering"><description>HOTFIX-05 — see plan 06</description><mitigation>Smoke step 6 verifies on staging</mitigation><verification>Step 6 acceptance below</verification></threat>
  <threat id="T-1-06" severity="high" category="Information Disclosure"><description>HOTFIX-06 — see plan 01</description><mitigation>Smoke step 9 verifies on staging</mitigation><verification>Step 9 acceptance below</verification></threat>
  <threat id="T-1-07" severity="high" category="Denial of Service"><description>HOTFIX-07 — see plan 08</description><mitigation>Smoke step 8 verifies on staging</mitigation><verification>Step 8 acceptance below</verification></threat>
  <threat id="T-1-08" severity="high" category="Tampering"><description>HOTFIX-08 — see plan 02</description><mitigation>Smoke step 1 verifies on staging</mitigation><verification>Step 1 acceptance below</verification></threat>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Verify all 8 hotfix commits are present and the full Go test suite passes with -race</name>
  <files>.planning/phases/01-hotfix-audit-critical-fixes/01-09-SMOKE-RESULTS.md</files>
  <read_first>
    - All 8 prior SUMMARY files (01-01-SUMMARY.md through 01-08-SUMMARY.md) — confirm each lists a commit SHA
    - .planning/phases/01-hotfix-audit-critical-fixes/01-VALIDATION.md (Sampling Rate section — "Full suite command")
  </read_first>
  <action>
    Step A — Verify the 8 expected commits are present on the working branch in D-02 order. Run:
        git log --oneline -20
    Then for each expected commit (HOTFIX-06, 08, 04, 02, 03, 05, 01, 07 in that order), assert presence:
        git log --format=%s | grep -E '^hotfix\(01\): .*HOTFIX-06' || (echo "MISSING: HOTFIX-06 commit" >&2; exit 1)
        git log --format=%s | grep -E '^hotfix\(01\): .*HOTFIX-08' || (echo "MISSING: HOTFIX-08 commit" >&2; exit 1)
        git log --format=%s | grep -E '^hotfix\(01\): .*HOTFIX-04' || (echo "MISSING: HOTFIX-04 commit" >&2; exit 1)
        git log --format=%s | grep -E '^hotfix\(01\): .*HOTFIX-02' || (echo "MISSING: HOTFIX-02 commit" >&2; exit 1)
        git log --format=%s | grep -E '^hotfix\(01\): .*HOTFIX-03' || (echo "MISSING: HOTFIX-03 commit" >&2; exit 1)
        git log --format=%s | grep -E '^hotfix\(01\): .*HOTFIX-05' || (echo "MISSING: HOTFIX-05 commit" >&2; exit 1)
        git log --format=%s | grep -E '^hotfix\(01\): .*HOTFIX-01' || (echo "MISSING: HOTFIX-01 commit" >&2; exit 1)
        git log --format=%s | grep -E '^hotfix\(01\): .*HOTFIX-07' || (echo "MISSING: HOTFIX-07 commit" >&2; exit 1)
    Also verify D-02 ordering: the order of these commits in `git log --format=%s | grep '^hotfix(01)'` (chronologically oldest first via `--reverse`) should be HOTFIX-06, 08, 04, 02, 03, 05, 01, 07. Document any deviation; minor reordering is acceptable as long as all 8 are present.

    Step B — Confirm exactly 8 hotfix commits (no surprises, no extra commits with `hotfix(01):` prefix):
        [ $(git log --format=%s | grep -cE '^hotfix\(01\):') -eq 8 ] || (echo "Expected exactly 8 hotfix(01) commits, found different count" >&2; exit 1)

    Step C — Run the full Go test suite with race detector:
        cd server/api && go test ./... -race -count=1
    Must exit 0. Capture the output (last 50 lines) for the SMOKE-RESULTS document.

    Step D — Build all binaries to confirm cross-package compile:
        cd server/api && go build ./cmd ./cmd/createadmin
    Must exit 0.

    Step E — Create `.planning/phases/01-hotfix-audit-critical-fixes/01-09-SMOKE-RESULTS.md` with the following template (operator fills in checkpoint results in the upcoming tasks). NOTE: the file MUST end with the `ALL_SMOKE_STEPS_APPROVED` marker COMMENTED OUT — the operator UNCOMMENTS it (or replaces the placeholder block) only after every row is ✅:

        # Phase 1 Smoke Results

        **Started:** <timestamp>
        **Working-branch HEAD before tag:** <commit sha>

        ## CI Gate (Task 1)

        - [x] 8 hotfix commits present (D-02 order verified)
        - [x] `go test ./... -race -count=1` PASS
        - [x] `go build ./cmd ./cmd/createadmin` PASS

        Last 50 lines of test output:
        ```
        <paste>
        ```

        ## Staging Smoke Checklist (Tasks 2-11)

        | # | Hotfix | Step | Result | Operator | Notes |
        |---|--------|------|--------|----------|-------|
        | 1 | HOTFIX-08 | env validator fails fast | ⬜ | | |
        | 2 | HOTFIX-04 | 5xx body scrubbed | ⬜ | | |
        | 3 | HOTFIX-04 | X-Request-ID echoed | ⬜ | | |
        | 4 | HOTFIX-02 | admin demotion takes effect | ⬜ | | |
        | 5 | HOTFIX-03 | rate-limit TTL positive | ⬜ | | |
        | 6 | HOTFIX-05 | refresh leaves single session | ⬜ | | |
        | 7 | HOTFIX-01 | scheduler downgrades expired pro | ⬜ | | |
        | 8 | HOTFIX-07 | EXPLAIN shows Index Scan | ⬜ | | |
        | 9 | HOTFIX-06 | createadmin stdin + free tier | ⬜ | | |
        | 10 | regression | existing endpoints still 200/JSON | ⬜ | | |

        ## Tag Push (Task 13)

        - [ ] `v2.2.0-hotfix` tag created at <commit sha>
        - [ ] Tag pushed to remote

        ---

        <!-- FINAL APPROVAL MARKER -- DO NOT UNCOMMENT UNTIL EVERY SMOKE ROW IS ✅.
             Task 13 greps for the literal text "ALL_SMOKE_STEPS_APPROVED" without the
             surrounding comment delimiters. To approve, REPLACE the four lines of this
             HTML comment block with the single uncommented marker line below: -->
        <!-- ALL_SMOKE_STEPS_APPROVED -->
  </action>
  <verify>
    <automated>[ $(git log --format=%s | grep -cE '^hotfix\(01\):') -eq 8 ] &amp;&amp; cd server/api &amp;&amp; go test ./... -race -count=1 &amp;&amp; go build ./cmd ./cmd/createadmin &amp;&amp; test -f /Users/abdunabi/Desktop/vpn/.planning/phases/01-hotfix-audit-critical-fixes/01-09-SMOKE-RESULTS.md</automated>
  </verify>
  <acceptance_criteria>
    - Exactly 8 hotfix commits exist on the working branch: `git log --format=%s | grep -cE '^hotfix\(01\):'` returns 8
    - Each individual HOTFIX-XX appears in commit log: `for n in 01 02 03 04 05 06 07 08; do git log --format=%s | grep -qE "^hotfix\\(01\\): .*HOTFIX-$n" || exit 1; done` exits 0
    - Full Go suite with race detector PASSES: `cd server/api && go test ./... -race -count=1` exits 0
    - All binaries build: `cd server/api && go build ./cmd ./cmd/createadmin` exits 0
    - SMOKE-RESULTS.md scaffolding created with the 10 unchecked rows AND the commented-out approval marker block
  </acceptance_criteria>
  <done>All 8 hotfix commits verified present; full Go suite green with -race; binaries build; SMOKE-RESULTS.md scaffold initialized with the FINAL APPROVAL MARKER block (commented out — operator uncomments only after all 10 rows are ✅). Ready to deploy + smoke on staging.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2 [HUMAN ACTION]: Deploy the 8 hotfix commits to staging</name>
  <what-built>The working branch carries 8 atomic hotfix commits ready to deploy as a single combined deploy per D-01.</what-built>
  <how-to-verify>
    1. Push the working branch to origin if not already (no force, no skipped hooks):
       `git push origin HEAD`
    2. Trigger the existing deploy workflow OR follow the project's standard staging-deploy runbook. From `.github/workflows/deploy.yml` the workflow runs on push and copies `docker-compose.prod.yml` + `server/api/migrations/` to the staging VM, then `docker compose up -d --build` cycles the API container.
    3. Verify the deploy succeeded:
       - `gh run watch` (or check the Actions tab) shows the deploy job green.
       - SSH to staging or use the project's standard health check: `curl -i https://<staging-host>/api/v1/health` returns 200.
    4. **MIGRATION NOTE (HOTFIX-07):** Postgres `docker-entrypoint-initdb.d` only runs on a FRESH data dir. For an existing running Postgres on staging, the 017 migration MUST be applied manually:
       `psql "$STAGING_DATABASE_URL" -f server/api/migrations/017_sessions_refresh_token_hash_unique.sql`
       Confirm the index landed:
       `psql "$STAGING_DATABASE_URL" -c "\d sessions" | grep idx_sessions_refresh_token_hash_unique`
    5. Update SMOKE-RESULTS.md "Working-branch HEAD before tag" line with the SHA of `git rev-parse HEAD`.
    
    There is no CLI command for "deploy to my staging VM via my preferred runbook" — this is the rare genuinely-human-action case in this phase.
  </how-to-verify>
  <resume-signal>Reply "deployed" once staging is on the new build AND `psql -c "\d sessions"` lists the new index (or describe issues if any step failed).</resume-signal>
  <files>(no files modified — this is a manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per &lt;how-to-verify&gt;"</automated>
  </verify>
  <done>Operator replied with the resume-signal indicating the checkpoint passed (or described the failure for follow-up).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3 [SMOKE 1/10]: HOTFIX-08 — env validator fails fast on missing required env</name>
  <what-built>config.RequireEnv() called from cmd/main.go before config.Load(); logger.Fatal on non-empty slice with single aggregate log line.</what-built>
  <how-to-verify>
    On the staging VM (or in a staging-flavored container where you can override env), run:
        JWT_SECRET= ./vpn-api 2>&1 | head -1 | jq -e '.missing'
    Expected: process exits with code 1; head -1 returns a SINGLE JSON log line containing a `missing` array including `JWT_SECRET`; `jq -e '.missing'` exits 0 (truthy).
    Alternative if jq not installed: `JWT_SECRET= ./vpn-api 2>&1 | head -1 | grep -q '"missing"'` then `echo $?` is 0.
    Negative test: with all required vars set, the api-server starts normally with no `"missing"` line (possibly a `"vars"` WARN line for Stripe placeholders — that's correct per HOTFIX-08 OptionalEnvWarnings).
    Update SMOKE-RESULTS.md row #1 with `✅ <date> <operator-initials>` or `❌ <reason>`.
  </how-to-verify>
  <resume-signal>Reply "approved" or describe the failure.</resume-signal>
  <files>(no files modified — this is a manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per &lt;how-to-verify&gt;"</automated>
  </verify>
  <done>Operator replied with the resume-signal indicating the checkpoint passed (or described the failure for follow-up).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4 [SMOKE 2/10]: HOTFIX-04 — 5xx body is scrubbed (no GORM/bcrypt/pq strings)</name>
  <what-built>handler/health.go ErrorHandler returns generic `{"error":"internal server error","request_id":"<uuid>"}` for 5xx; full error logged via zap with same request_id.</what-built>
  <how-to-verify>
    Trigger a 500 against staging using the canonical malformed-refresh-token endpoint (per RESEARCH §HOTFIX-04 — this endpoint hits the HOTFIX-05 rotation transaction which returns 500 on internal failure once HOTFIX-04 scrubbing lands):
        curl -i -XPOST -H 'Content-Type: application/json' -d '{"refresh_token":"not-a-real-token"}' https://<staging-host>/api/v1/auth/refresh
    Expected response body shape:
        {"error":"internal server error","request_id":"<some-uuid>"}
    Assert the response body contains `request_id`:
        curl -s -XPOST -H 'Content-Type: application/json' -d '{"refresh_token":"not-a-real-token"}' https://<staging-host>/api/v1/auth/refresh | grep -q '"request_id"'
    AND the body contains NONE of the leak substrings:
        curl -s -XPOST -H 'Content-Type: application/json' -d '{"refresh_token":"not-a-real-token"}' https://<staging-host>/api/v1/auth/refresh | grep -vE 'pq:|gorm:|bcrypt:' >/dev/null
    Also confirm the response carries an `X-Request-ID` header:
        curl -i -XPOST -H 'Content-Type: application/json' -d '{"refresh_token":"not-a-real-token"}' https://<staging-host>/api/v1/auth/refresh | grep -i 'x-request-id:'

    **Fallback if the malformed token is rejected with 401 (validation rejects early before hitting the rotation transaction):** Per RESEARCH §HOTFIX-04 alternative trigger paths, two options:
      (a) Pick a different real endpoint known to 500 on bad input (e.g., temporarily break DATABASE_URL mid-request, or use any endpoint where a GORM/bcrypt error path is reachable from public input).
      (b) Add a `/api/v1/__force-500` debug route guarded by `ENV=staging` in a Plan 03 follow-up — but PREFER option (a) because it doesn't add production surface area. The auth/refresh path is the documented canonical trigger and SHOULD reach the 500 path because the rotation transaction begins before signature validation in the current handler code.
    Document which trigger you used in the SMOKE-RESULTS row #2 "Notes" column.

    Update SMOKE-RESULTS.md row #2.
  </how-to-verify>
  <resume-signal>Reply "approved" or describe the failure.</resume-signal>
  <files>(no files modified — this is a manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per &lt;how-to-verify&gt;"</automated>
  </verify>
  <done>Operator replied with the resume-signal indicating the checkpoint passed (or described the failure for follow-up).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5 [SMOKE 3/10]: HOTFIX-04 — incoming X-Request-ID is echoed back (same endpoint as step 2)</name>
  <what-built>middleware/requestid wired in cmd/main.go BEFORE recover; accepts incoming header verbatim or generates UUIDv4.</what-built>
  <how-to-verify>
    Reuse the canonical malformed-refresh-token endpoint from step 2 — it carries an X-Request-ID through the same scrubbed-500 path and lets us verify echo behavior on a 5xx response (the same response shape clients will see in the wild). This keeps the smoke surface minimal (no debug-only routes, no separate health-check coupling):
        curl -i -XPOST -H 'Content-Type: application/json' -H 'X-Request-ID: smoke-2' -d '{"refresh_token":"not-a-real-token"}' https://<staging-host>/api/v1/auth/refresh
    Expected: response header `X-Request-ID: smoke-2` (echoed verbatim) AND the response body's `request_id` field also equals `smoke-2`.
    Assert echo in header:
        curl -i -XPOST -H 'Content-Type: application/json' -H 'X-Request-ID: smoke-2' -d '{"refresh_token":"not-a-real-token"}' https://<staging-host>/api/v1/auth/refresh | grep -qi '^x-request-id: smoke-2'
    Assert echo in body:
        curl -s -XPOST -H 'Content-Type: application/json' -H 'X-Request-ID: smoke-2' -d '{"refresh_token":"not-a-real-token"}' https://<staging-host>/api/v1/auth/refresh | grep -q '"request_id":"smoke-2"'

    Now test the GENERATION path when the header is absent (any endpoint works — `/api/v1/health` is the simplest):
        curl -i https://<staging-host>/api/v1/health | grep -iE '^x-request-id: [0-9a-f]{8}-[0-9a-f]{4}-4[0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}'
    Should match (the UUIDv4 regex).
    Update SMOKE-RESULTS.md row #3.
  </how-to-verify>
  <resume-signal>Reply "approved" or describe the failure.</resume-signal>
  <files>(no files modified — this is a manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per &lt;how-to-verify&gt;"</automated>
  </verify>
  <done>Operator replied with the resume-signal indicating the checkpoint passed (or described the failure for follow-up).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6 [SMOKE 4/10]: HOTFIX-02 — admin demotion takes effect on the very next request</name>
  <what-built>middleware.AdminRequired re-reads role from DB per request via repository.FindUserByID (pure DB, no Redis cache).</what-built>
  <how-to-verify>
    1. Log in to the staging admin panel with the seeded admin account (or use the API directly: `curl -X POST .../admin/login` with admin creds).
    2. Confirm admin access works: `curl -H "Authorization: Bearer $TOKEN" https://<staging-host>/api/v1/admin/users` returns 200.
    3. In a separate terminal, run on staging Postgres:
       `psql "$STAGING_DATABASE_URL" -c "UPDATE users SET role='user' WHERE email='<admin-email>'"`
    4. WITHIN the same access-token lifetime (5 min), re-issue the same admin request from step 2 with the SAME $TOKEN.
    5. Expected: returns 403 (Forbidden). If it returns 200, the JWT-claim path is still active — HOTFIX-02 failed.
    6. Restore: `psql -c "UPDATE users SET role='admin' WHERE email='<admin-email>'"`.
    Update SMOKE-RESULTS.md row #4.
  </how-to-verify>
  <resume-signal>Reply "approved" or describe the failure (e.g., still 200 — indicates HOTFIX-02 regression).</resume-signal>
  <files>(no files modified — this is a manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per &lt;how-to-verify&gt;"</automated>
  </verify>
  <done>Operator replied with the resume-signal indicating the checkpoint passed (or described the failure for follow-up).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7 [SMOKE 5/10]: HOTFIX-03 — rate-limit TTL is always positive after IncrRateLimit</name>
  <what-built>cache/redis.go IncrRateLimit uses Lua EVAL via redis.NewScript().Run() — single round-trip, atomic INCR + EXPIRE.</what-built>
  <how-to-verify>
    From any unauthenticated source, hit a rate-limited endpoint 35 times in <1 min. Recommended: the guest-auth endpoint with its existing 30/min/IP unauth limit (replace with the actual path if different):
        for i in $(seq 1 35); do curl -s -o /dev/null -w "%{http_code}\n" -X POST https://<staging-host>/api/v1/auth/guest; done
    Expected: ~30 200s followed by ~5 429s.
    Then on the staging Redis (via `docker exec` or `redis-cli`), check the TTL:
        redis-cli TTL rate:ip:<your_test_ip>
    MUST return a positive integer (1..60). Never `-1` (no expiry) or `-2` (key gone).
    Update SMOKE-RESULTS.md row #5.
  </how-to-verify>
  <resume-signal>Reply "approved" or describe the failure (e.g., TTL returned -1 — indicates atomicity regression).</resume-signal>
  <files>(no files modified — this is a manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per &lt;how-to-verify&gt;"</automated>
  </verify>
  <done>Operator replied with the resume-signal indicating the checkpoint passed (or described the failure for follow-up).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 8 [SMOKE 6/10]: HOTFIX-05 — refresh leaves exactly one session row for the user</name>
  <what-built>handler/auth.go RefreshToken wraps DeleteSession + FindUserByID + generateTokens + storeRefreshSession in db.Transaction.</what-built>
  <how-to-verify>
    1. Sign in as any user on staging to mint a refresh token. Note `user_id` and the refresh token.
    2. Check session row count before:
       `psql "$STAGING_DATABASE_URL" -t -c "SELECT count(*) FROM sessions WHERE user_id='<user_id>'"`  → expected 1
    3. Hit refresh:
       `curl -X POST https://<staging-host>/api/v1/auth/refresh -H 'Content-Type: application/json' -d '{"refresh_token":"<token>"}'`  → expect 200 with new tokens.
    4. Check session row count after:
       `psql "$STAGING_DATABASE_URL" -t -c "SELECT count(*) FROM sessions WHERE user_id='<user_id>'"`  → expected EXACTLY 1 (the new row; old one was deleted in-tx).
    5. Confirm the new row's hash differs from the original by re-deriving SHA-256 of the new refresh token and comparing to `SELECT refresh_token_hash FROM sessions WHERE user_id='<user_id>'`.
    Update SMOKE-RESULTS.md row #6.
  </how-to-verify>
  <resume-signal>Reply "approved" or describe the failure (e.g., count is 0 or 2 — indicates rollback bug).</resume-signal>
  <files>(no files modified — this is a manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per &lt;how-to-verify&gt;"</automated>
  </verify>
  <done>Operator replied with the resume-signal indicating the checkpoint passed (or described the failure for follow-up).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 9 [SMOKE 7/10]: HOTFIX-01 — scheduler downgrades expired Pro user within ≤90s</name>
  <what-built>This is the regression-test path. Production column + scheduler are already correct; this verifies on staging that the auto-downgrade still works end-to-end.</what-built>
  <how-to-verify>
    1. On staging Postgres, seed a Pro user with past-due expires_at:
       `psql "$STAGING_DATABASE_URL" -c "UPDATE users SET subscription_tier='pro', subscription_expires_at=NOW() - INTERVAL '1 hour' WHERE email='<test-user-email>'"`
    2. Confirm:
       `psql "$STAGING_DATABASE_URL" -t -c "SELECT subscription_tier, subscription_expires_at FROM users WHERE email='<test-user-email>'"` → returns `pro | <past timestamp>`.
    3. Wait ≤90 seconds (scheduler runs every 60s per scheduler.go).
    4. Re-check:
       `psql -t -c "SELECT subscription_tier, subscription_expires_at FROM users WHERE email='<test-user-email>'"` → MUST return `free | <same past timestamp>` (tier downgraded; expires_at preserved as audit trail).
    Update SMOKE-RESULTS.md row #7.
  </how-to-verify>
  <resume-signal>Reply "approved" or describe the failure (e.g., still pro after 2 min — indicates scheduler wire issue).</resume-signal>
  <files>(no files modified — this is a manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per &lt;how-to-verify&gt;"</automated>
  </verify>
  <done>Operator replied with the resume-signal indicating the checkpoint passed (or described the failure for follow-up).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 10 [SMOKE 8/10]: HOTFIX-07 — EXPLAIN shows Index Scan on the new UNIQUE index</name>
  <what-built>migrations/017 + scripts/smoke_test_session_index.sh — adds UNIQUE index idx_sessions_refresh_token_hash_unique on sessions.refresh_token_hash.</what-built>
  <how-to-verify>
    Run the smoke script against staging:
        bash server/api/scripts/smoke_test_session_index.sh "$STAGING_DATABASE_URL"
    Expected: exit 0, and the output contains `OK: query planner is using idx_sessions_refresh_token_hash_unique`.
    If exit 1 with "Seq Scan on sessions": the migration did not apply correctly. Re-check that step 4 of Task 2 ran `psql -f .../017_sessions_refresh_token_hash_unique.sql`.
    Manual cross-check:
        psql "$STAGING_DATABASE_URL" -c "\d sessions" | grep idx_sessions_refresh_token_hash_unique
    Should show the UNIQUE btree index.
    Update SMOKE-RESULTS.md row #8.
  </how-to-verify>
  <resume-signal>Reply "approved" or describe the failure (e.g., "Seq Scan" output — indicates migration not applied).</resume-signal>
  <files>(no files modified — this is a manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per &lt;how-to-verify&gt;"</automated>
  </verify>
  <done>Operator replied with the resume-signal indicating the checkpoint passed (or described the failure for follow-up).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 11 [SMOKE 9/10]: HOTFIX-06 — createadmin rejects -password flag and seeds tier=free</name>
  <what-built>cmd/createadmin/main.go reads password from stdin via term.ReadPassword (TTY) or bufio (piped); seeds SubscriptionTier="free".</what-built>
  <how-to-verify>
    SSH to staging (or `docker exec -it vpn-api sh`) and run:
        ./createadmin -password=anything
    Expected: process errors immediately with stderr `flag provided but not defined: -password`.
        ./createadmin -password=anything 2>&1 | grep -q 'not defined'   # should exit 0
    Then run the actual create flow (use a unique email to avoid collisions):
        ./createadmin -email=smoke-admin-$(date +%s)@example.com
    Expected: prompts "Password (8-72 chars):", echo-off; accept a password; reports success.
    Confirm the seeded row:
        psql "$STAGING_DATABASE_URL" -t -c "SELECT role, subscription_tier FROM users WHERE email LIKE 'smoke-admin-%@example.com' ORDER BY created_at DESC LIMIT 1"
    Expected: `admin | free` (NOT `admin | ultimate`).
    Update SMOKE-RESULTS.md row #9.
  </how-to-verify>
  <resume-signal>Reply "approved" or describe the failure.</resume-signal>
  <files>(no files modified — this is a manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per &lt;how-to-verify&gt;"</automated>
  </verify>
  <done>Operator replied with the resume-signal indicating the checkpoint passed (or described the failure for follow-up).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 12 [SMOKE 10/10]: Regression sanity — existing endpoints still return 200/JSON</name>
  <what-built>Cross-cutting confirmation that no Phase-1 fix introduced a regression on the broader API surface.</what-built>
  <how-to-verify>
    Hit the four critical endpoints and confirm each returns its expected shape:
        curl -i -X POST https://<staging-host>/api/v1/auth/guest    # 200, returns access+refresh JSON
        curl -i -X POST https://<staging-host>/api/v1/auth/admin-login -d '{...}'   # 200 with valid creds; 401 otherwise
        curl -i -H "Authorization: Bearer $TOKEN" https://<staging-host>/api/v1/subscription   # 200 returning tier
        curl -i -H "Authorization: Bearer $TOKEN" https://<staging-host>/api/v1/servers   # 200 returning [] or [{...}]
    None should 500. Each should produce its current expected JSON body shape (compare to a pre-deploy capture if uncertain). Pay extra attention to:
      - `/auth/guest` — touches IncrRateLimit (HOTFIX-03) and storeRefreshSession (NOT inside HOTFIX-05 transaction, but uses the same helper).
      - `/auth/admin-login` — touches AdminRequired's new DB-read path on subsequent admin requests.
      - Any 5xx response should now carry the scrubbed body shape with request_id (HOTFIX-04).
    Update SMOKE-RESULTS.md row #10.

    **AFTER ALL 10 ROWS ARE ✅:** explicitly UNCOMMENT (or replace the placeholder HTML-comment block with) the literal marker line:
        <!-- ALL_SMOKE_STEPS_APPROVED -->
    so it appears un-nested at the bottom of SMOKE-RESULTS.md as:
        <!-- ALL_SMOKE_STEPS_APPROVED -->
    Task 13 greps for this exact literal string. This is the single explicit operator gate that tells the tagging task "yes, every smoke step is green, push the tag".
  </how-to-verify>
  <resume-signal>Reply "approved" or describe any regression observed.</resume-signal>
  <files>(no files modified — this is a manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per &lt;how-to-verify&gt;"</automated>
  </verify>
  <done>Operator replied with the resume-signal indicating the checkpoint passed (or described the failure for follow-up).</done>
</task>

<task type="auto">
  <name>Task 13: Create + push the v2.2.0-hotfix git tag (gated on the explicit ALL_SMOKE_STEPS_APPROVED marker)</name>
  <files></files>
  <read_first>
    - .planning/phases/01-hotfix-audit-critical-fixes/01-09-SMOKE-RESULTS.md (verify the literal `<!-- ALL_SMOKE_STEPS_APPROVED -->` marker is present at the bottom; this is the explicit operator gate that proves all 10 rows are ✅)
    - .planning/phases/01-hotfix-audit-critical-fixes/01-CONTEXT.md specifics (tag is v2.2.0-hotfix per RESEARCH Risk #8 recommendation)
    - .planning/phases/01-hotfix-audit-critical-fixes/01-RESEARCH.md §Risks #8 (v2.2.0-hotfix vs v2.2.0)
  </read_first>
  <action>
    Step A — Verify SMOKE-RESULTS.md carries the explicit operator-applied approval marker. This is the gate that proves the operator reviewed every row and is ready to ship. Run:
        grep -q '^<!-- ALL_SMOKE_STEPS_APPROVED -->$' .planning/phases/01-hotfix-audit-critical-fixes/01-09-SMOKE-RESULTS.md
    Must exit 0. If it doesn't, STOP — the operator has not yet approved (the marker is still inside an HTML-comment instruction block or has not been added at all). The tag MUST NOT land.

    Why a literal marker line instead of counting ✅ characters: counting ✅ via regex like `grep -cE '^\| \d+\s.+✅'` is brittle (column-spacing dependent) AND ambiguous (one row could be ⚠️ while others are ✅ and the count still hits 10 if any cell happens to contain ✅ as part of free-text notes). A single literal marker that the operator MUST explicitly add is unambiguous: "yes, I personally reviewed every row, all 10 are green, push the tag".

    Step B — Confirm working tree is clean and on the expected branch (no uncommitted changes that would silently land in the tag):
        git status --porcelain   # must be empty
        git rev-parse --abbrev-ref HEAD   # the working branch (per branching_strategy: none, this is main or equivalent)

    Step C — Confirm working-branch HEAD is the same SHA we deployed and smoked (capture for the SUMMARY):
        DEPLOY_SHA=$(git rev-parse HEAD)
        echo "Tagging at: $DEPLOY_SHA"

    Step D — Create the annotated tag with a message that lists all 8 hotfixes:
        git tag -a v2.2.0-hotfix -m "Phase 1 hotfix tranche (Tranche 0 of docs/audit/MASTER-PLAN.md)

        Closes 8 audit Critical/High findings (HOTFIX-01..08):
          - HOTFIX-06: createadmin stdin + free tier (S2-1)
          - HOTFIX-08: fail-fast env validator (HIGH-08 / S3-4 / S3-5)
          - HOTFIX-04: scrub 5xx + X-Request-ID (CRIT-04 / S9-1)
          - HOTFIX-02: AdminRequired re-reads role per request (CRIT-03 / S2-2)
          - HOTFIX-03: atomic Lua INCR+EXPIRE (CRIT-02)
          - HOTFIX-05: transactional refresh rotation (S1-1)
          - HOTFIX-01: subscription downgrade regression test (CRIT-01; webhook write deferred to Phase 3)
          - HOTFIX-07: UNIQUE index on sessions.refresh_token_hash (Perf #1)

        All 8 commits + 10-step staging smoke green. See
        .planning/phases/01-hotfix-audit-critical-fixes/01-09-SMOKE-RESULTS.md
        (literal marker ALL_SMOKE_STEPS_APPROVED confirms operator sign-off)
        for operator-recorded checkpoint results."

    Step E — Push the tag (do NOT use --force):
        git push origin v2.2.0-hotfix

    Step F — Update SMOKE-RESULTS.md "Tag Push" section: check both boxes and record the tag SHA.
  </action>
  <verify>
    <automated>grep -q '^&lt;!-- ALL_SMOKE_STEPS_APPROVED --&gt;$' /Users/abdunabi/Desktop/vpn/.planning/phases/01-hotfix-audit-critical-fixes/01-09-SMOKE-RESULTS.md &amp;&amp; cd /Users/abdunabi/Desktop/vpn &amp;&amp; git tag -l v2.2.0-hotfix | grep -q v2.2.0-hotfix &amp;&amp; git ls-remote --tags origin v2.2.0-hotfix | grep -q v2.2.0-hotfix</automated>
  </verify>
  <acceptance_criteria>
    - **Explicit operator approval marker present:** `grep -q '^<!-- ALL_SMOKE_STEPS_APPROVED -->$' .planning/phases/01-hotfix-audit-critical-fixes/01-09-SMOKE-RESULTS.md` exits 0. This single literal marker is the canonical sign-off — robust to column-spacing changes, immune to incidental ✅ characters elsewhere in the file, and requires explicit operator action.
    - Working tree was clean at tag time: `git status --porcelain` returned empty before tagging
    - Tag exists locally: `git tag -l v2.2.0-hotfix` returns `v2.2.0-hotfix`
    - Tag is annotated (not lightweight): `git cat-file -t v2.2.0-hotfix` returns `tag` (not `commit`)
    - Tag pushed to origin: `git ls-remote --tags origin v2.2.0-hotfix | grep -q v2.2.0-hotfix` exits 0
    - Tag points at the working-branch HEAD (the smoked SHA): `git rev-list -n 1 v2.2.0-hotfix` equals the value of `git rev-parse HEAD`
  </acceptance_criteria>
  <done>v2.2.0-hotfix annotated tag exists locally and on origin, pointing at the SHA that was smoked green on staging. The explicit `<!-- ALL_SMOKE_STEPS_APPROVED -->` marker in SMOKE-RESULTS.md confirmed operator sign-off before the tag was created. SMOKE-RESULTS.md is fully filled in. Phase 1 is complete.</done>
</task>

</tasks>

<verification>
1. `git tag -l v2.2.0-hotfix | grep -q v2.2.0-hotfix` — tag exists locally.
2. `git ls-remote --tags origin v2.2.0-hotfix | grep -q v2.2.0-hotfix` — tag pushed.
3. `git cat-file -t v2.2.0-hotfix` returns `tag` — annotated tag.
4. SMOKE-RESULTS.md has the literal `<!-- ALL_SMOKE_STEPS_APPROVED -->` marker on its own line (operator explicit sign-off).
5. `git log --format=%s | grep -cE '^hotfix\(01\):'` returns 8.
</verification>

<success_criteria>
- 8 hotfix commits exist on the working branch in (approximately) D-02 order.
- Full Go test suite passes with -race.
- All 10 staging smoke steps green and recorded in SMOKE-RESULTS.md, plus the explicit `<!-- ALL_SMOKE_STEPS_APPROVED -->` operator marker at the file's bottom.
- `v2.2.0-hotfix` annotated tag exists locally and on origin, pointing at the smoked SHA.
- Each of ROADMAP §Phase 1's 8 success criteria has been verifiably demonstrated on staging.
</success_criteria>

<output>
After completion, create `.planning/phases/01-hotfix-audit-critical-fixes/01-09-SUMMARY.md` with:
- Tag SHA + working-branch HEAD SHA
- The full filled-in SMOKE-RESULTS.md content (or reference + summary), including the explicit `<!-- ALL_SMOKE_STEPS_APPROVED -->` marker line for the record
- Last 50 lines of `go test ./... -race -count=1` output (for the record)
- Any deviations from the 10-step plan (e.g., if a different 500-triggering endpoint was used in step 2 — record the chosen alternative)
- Outbound note for Phase 2: "Phase 1 complete. AdminRequired now takes (db); refresh rotation is transactional; sessions has UNIQUE index on refresh_token_hash. Phase 2 (SSO backend) can rebase on top of v2.2.0-hotfix without contention."

Also create `.planning/phases/01-hotfix-audit-critical-fixes/PHASE-SUMMARY.md` aggregating all 9 plan SUMMARYs into one phase-completion record (this is the artifact /gsd-verify-work / /gsd-next will read when planning Phase 2).
</output>
