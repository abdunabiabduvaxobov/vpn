---
phase: 05-mobile-sso-pro-cta
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - app/__mocks__/@invertase/react-native-apple-authentication.ts
  - app/__mocks__/@react-native-google-signin/google-signin.ts
  - app/src/services/__tests__/appleSignIn.test.ts
  - app/src/services/__tests__/googleSignIn.test.ts
  - app/src/services/__tests__/deepLink.test.ts
  - app/src/services/__tests__/payment.test.ts
  - app/src/services/__tests__/api.test.ts
  - app/src/stores/__tests__/authStore.test.ts
  - app/src/screens/__tests__/LoginScreen.test.tsx
  - app/src/screens/__tests__/PaymentScreen.test.tsx
  - app/src/components/__tests__/ActivatingProModal.test.tsx
  - app/src/config/__tests__/version.test.ts
autonomous: true
requirements: [APP-01, APP-02, APP-03, APP-04, APP-05, APP-06, APP-07]

must_haves:
  truths:
    - "Jest manual mocks for @invertase/react-native-apple-authentication and @react-native-google-signin/google-signin exist and load without throwing"
    - "Every Wave-0 test file exists, is parseable by ts-jest, and contains at least one failing/skipped test describing the future behavior"
    - "Running `cd app && npm test` exits 0 (skipped tests count, no failures) so the suite is green before code lands"
    - "Operator prerequisites checklist is documented in 05-HUMAN-UAT.md including iOS Bundle ID confirmation"
  artifacts:
    - path: "app/__mocks__/@invertase/react-native-apple-authentication.ts"
      provides: "Manual mock exporting appleAuth.performRequest + appleAuth.Error.CANCELED + appleAuth.Operation + appleAuth.Scope"
      contains: "performRequest"
    - path: "app/__mocks__/@react-native-google-signin/google-signin.ts"
      provides: "Manual mock exporting GoogleSignin.configure, hasPlayServices, signIn + statusCodes.SIGN_IN_CANCELLED"
      contains: "GoogleSignin"
    - path: "app/src/services/__tests__/appleSignIn.test.ts"
      provides: "Stub tests for signInWithApple happy path + cancellation"
      contains: "signInWithApple"
    - path: "app/src/services/__tests__/googleSignIn.test.ts"
      provides: "Stub tests for configureGoogleSignIn + signInWithGoogle"
      contains: "configureGoogleSignIn"
    - path: "app/src/services/__tests__/deepLink.test.ts"
      provides: "Stub tests for URL parser edge cases"
      contains: "vpnapp://payment/success"
    - path: "app/src/services/__tests__/payment.test.ts"
      provides: "Stub tests for upgradeUrlForLocale + getInvoice escalate"
      contains: "upgradeUrlForLocale"
    - path: "app/src/services/__tests__/api.test.ts"
      provides: "Stub test for _skipAuthRefresh interceptor short-circuit"
      contains: "_skipAuthRefresh"
    - path: "app/src/stores/__tests__/authStore.test.ts"
      provides: "Stub tests for signInWithApple + signInWithGoogle + startActivatingPro"
      contains: "signInWithApple"
    - path: "app/src/screens/__tests__/LoginScreen.test.tsx"
      provides: "Stub render test for three CTAs (iOS vs Android)"
      contains: "LoginScreen"
    - path: "app/src/screens/__tests__/PaymentScreen.test.tsx"
      provides: "Stub render test for informational layout + locked CTA copy"
      contains: "Upgrade to Pro at risevpn.com"
    - path: "app/src/components/__tests__/ActivatingProModal.test.tsx"
      provides: "Stub polling-loop test with fake timers"
      contains: "ActivatingProModal"
    - path: "app/src/config/__tests__/version.test.ts"
      provides: "Trivial guard: APP_VERSION === '2.2.0'"
      contains: "2.2.0"
    - path: ".planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md"
      provides: "Operator-prerequisites checklist gating Wave 1"
      contains: "Apple Bundle ID"
  key_links:
    - from: "app/jest.config.js"
      to: "app/__mocks__/"
      via: "react-native preset auto-discovers manual mocks under __mocks__/"
      pattern: "preset:\\s*'react-native'"
    - from: "app/src/services/__tests__/*.test.ts"
      to: "app/__mocks__/@invertase/react-native-apple-authentication.ts"
      via: "jest.mock('@invertase/react-native-apple-authentication')"
      pattern: "react-native-apple-authentication"
---

<objective>
Establish the Wave-0 test scaffolding for Phase 5 mobile SSO + Pro CTA work — create Jest manual mocks for both new SSO native libraries and stub test files for every service / store / screen / component that subsequent waves will implement. Document the BLOCKING operator-prerequisites checklist (Apple Bundle ID confirmation, Google OAuth client IDs, Android debug keystore SHA-1) in 05-HUMAN-UAT.md so Wave 1 cannot start until the operator has confirmed the inputs.

Purpose: Per RESEARCH.md §"Wave 0 Gaps" and VALIDATION.md, every Wave-2/Wave-3 task has a `<verify>` clause pointing at a Jest test file that does not yet exist. This plan creates the empty test scaffolds so subsequent tasks can immediately write tests-first against them (Nyquist compliance). The mocks unblock `appleSignIn.ts` / `googleSignIn.ts` from importing the real native modules in a Jest environment that has no native bridge.

Output: 12 new files (2 mocks + 10 test stubs + 1 UAT doc) + a phase-internal release keystore note in 05-HUMAN-UAT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md
@.planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md
@.planning/phases/05-mobile-sso-pro-cta/05-UI-SPEC.md
@.planning/phases/05-mobile-sso-pro-cta/05-VALIDATION.md
@app/jest.config.js
@app/package.json
@app/__tests__/App.test.tsx

<interfaces>
<!-- Existing Jest config is bare: preset 'react-native', no setupFiles, no transformIgnorePatterns overrides. -->

From app/jest.config.js:
```js
module.exports = {
  preset: 'react-native',
};
```

From app/package.json (relevant scripts):
```json
"test": "jest"
```

Mock contract that downstream waves will rely on (Wave 2 services consume these mocks transparently — production code does NOT import from __mocks__ paths):

appleAuth API surface that the mock MUST export (matches `@invertase/react-native-apple-authentication@2.5.1` real shape):
- `appleAuth.performRequest(options)` → returns `{identityToken, authorizationCode, user, fullName, email}` (configurable per test)
- `appleAuth.Operation.LOGIN`, `appleAuth.Operation.LOGOUT`, `appleAuth.Operation.REFRESH`, `appleAuth.Operation.IMPLICIT`
- `appleAuth.Scope.FULL_NAME`, `appleAuth.Scope.EMAIL`
- `appleAuth.Error.CANCELED`, `appleAuth.Error.UNKNOWN`, `appleAuth.Error.INVALID_RESPONSE`, `appleAuth.Error.NOT_HANDLED`, `appleAuth.Error.FAILED`

GoogleSignin API surface that the mock MUST export (matches `@react-native-google-signin/google-signin@16.1.2` v16 shape):
- `GoogleSignin.configure(options)` → void
- `GoogleSignin.hasPlayServices(options)` → Promise<true>
- `GoogleSignin.signIn()` → Promise<{type: 'success', data: {idToken, user: {email, name, id, photo}}}>
- `GoogleSignin.signOut()` → Promise<void>
- `statusCodes.SIGN_IN_CANCELLED`, `statusCodes.IN_PROGRESS`, `statusCodes.PLAY_SERVICES_NOT_AVAILABLE`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create Jest manual mocks for both new SSO native libraries</name>
  <files>app/__mocks__/@invertase/react-native-apple-authentication.ts, app/__mocks__/@react-native-google-signin/google-signin.ts</files>
  <read_first>
    - app/jest.config.js (confirm preset 'react-native' and no transformIgnorePatterns override that excludes __mocks__/)
    - app/package.json (confirm @invertase/react-native-apple-authentication NOT yet installed — these mocks are written against the API spec from RESEARCH.md §Pattern 1 and §Pattern 2)
    - .planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md (§Pattern 1, §Pattern 2 for exact API surfaces)
    - .planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md (D-22 T-6 — pin exact versions, supply-chain mitigation; mocks must mirror the real library shape so Wave 2 services swap from mock to real without changes)
  </read_first>
  <behavior>
    - Test 1: importing the Apple mock yields `appleAuth.Operation.LOGIN === 1`, `appleAuth.Scope.FULL_NAME === 0`, `appleAuth.Scope.EMAIL === 1`, `appleAuth.Error.CANCELED === '1001'`
    - Test 2: `appleAuth.performRequest({requestedOperation: appleAuth.Operation.LOGIN, requestedScopes: [appleAuth.Scope.EMAIL]})` resolves to `{identityToken: 'mock-apple-id-token', authorizationCode: 'mock-apple-auth-code', user: 'mock-apple-sub', fullName: {givenName: 'Test', familyName: 'User'}, email: 'test@example.com'}`
    - Test 3: importing the Google mock yields `statusCodes.SIGN_IN_CANCELLED === 'SIGN_IN_CANCELLED'`
    - Test 4: `GoogleSignin.signIn()` resolves to `{type: 'success', data: {idToken: 'mock-google-id-token', user: {email: 'test@example.com', id: 'mock-google-sub', name: 'Test User', photo: null, familyName: 'User', givenName: 'Test'}}}`
    - Test 5: `GoogleSignin.configure({webClientId: 'x'})` is a no-op jest.fn() that records the call
  </behavior>
  <action>
    Create `app/__mocks__/@invertase/react-native-apple-authentication.ts` with the following EXACT contents:

    ```typescript
    // Manual Jest mock for @invertase/react-native-apple-authentication.
    // Mirrors real library v2.5.1 API surface used by app/src/services/appleSignIn.ts
    // (and consumed in unit tests). DO NOT import from production code paths.

    export const appleAuth = {
      performRequest: jest.fn().mockResolvedValue({
        identityToken: 'mock-apple-id-token',
        authorizationCode: 'mock-apple-auth-code',
        user: 'mock-apple-sub',
        fullName: {givenName: 'Test', familyName: 'User'},
        email: 'test@example.com',
        nonce: 'mock-nonce',
        realUserStatus: 1,
        state: null,
      }),
      Operation: {
        IMPLICIT: 0,
        LOGIN: 1,
        REFRESH: 2,
        LOGOUT: 3,
      },
      Scope: {
        FULL_NAME: 0,
        EMAIL: 1,
      },
      Error: {
        UNKNOWN: '1000',
        CANCELED: '1001',
        INVALID_RESPONSE: '1002',
        NOT_HANDLED: '1003',
        FAILED: '1004',
      },
    };

    export type AppleRequestResponse = {
      identityToken: string | null;
      authorizationCode: string | null;
      user: string;
      fullName: {givenName?: string; familyName?: string} | null;
      email: string | null;
      nonce: string;
      realUserStatus: number;
      state: string | null;
    };

    export default {appleAuth};
    ```

    Create `app/__mocks__/@react-native-google-signin/google-signin.ts` with the following EXACT contents (matches v16 shape: response wrapped in `data`):

    ```typescript
    // Manual Jest mock for @react-native-google-signin/google-signin v16.
    // Mirrors v16 response shape: userInfo.data.idToken (NOT userInfo.idToken pre-v13).

    export const GoogleSignin = {
      configure: jest.fn(),
      hasPlayServices: jest.fn().mockResolvedValue(true),
      signIn: jest.fn().mockResolvedValue({
        type: 'success',
        data: {
          idToken: 'mock-google-id-token',
          serverAuthCode: null,
          scopes: ['email', 'profile'],
          user: {
            id: 'mock-google-sub',
            email: 'test@example.com',
            name: 'Test User',
            familyName: 'User',
            givenName: 'Test',
            photo: null,
          },
        },
      }),
      signOut: jest.fn().mockResolvedValue(undefined),
      revokeAccess: jest.fn().mockResolvedValue(undefined),
      isSignedIn: jest.fn().mockResolvedValue(false),
      getCurrentUser: jest.fn().mockResolvedValue(null),
    };

    export const statusCodes = {
      SIGN_IN_CANCELLED: 'SIGN_IN_CANCELLED',
      IN_PROGRESS: 'IN_PROGRESS',
      PLAY_SERVICES_NOT_AVAILABLE: 'PLAY_SERVICES_NOT_AVAILABLE',
    };

    export default {GoogleSignin, statusCodes};
    ```

    Both files are pure mocks — no production code path imports them. Jest auto-resolves them when modules `@invertase/react-native-apple-authentication` or `@react-native-google-signin/google-signin` are imported under test, because RN preset honors `__mocks__/` directory adjacent to `node_modules`.
  </action>
  <verify>
    <automated>cd app && test -f __mocks__/@invertase/react-native-apple-authentication.ts && test -f __mocks__/@react-native-google-signin/google-signin.ts && grep -q "performRequest" __mocks__/@invertase/react-native-apple-authentication.ts && grep -q "GoogleSignin" __mocks__/@react-native-google-signin/google-signin.ts && grep -q "CANCELED: '1001'" __mocks__/@invertase/react-native-apple-authentication.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/__mocks__/@invertase/react-native-apple-authentication.ts` exists
    - `app/__mocks__/@react-native-google-signin/google-signin.ts` exists
    - `grep -E "performRequest|Operation|Scope|Error" app/__mocks__/@invertase/react-native-apple-authentication.ts` returns ≥4 matches
    - `grep -E "configure|signIn|hasPlayServices|statusCodes" app/__mocks__/@react-native-google-signin/google-signin.ts` returns ≥4 matches
    - `grep -q "CANCELED: '1001'" app/__mocks__/@invertase/react-native-apple-authentication.ts` exits 0
    - `grep -q "data:" app/__mocks__/@react-native-google-signin/google-signin.ts` exits 0 (v16 wrapped shape)
  </acceptance_criteria>
  <done>Both manual mock files exist with the EXACT shapes specified above and are loadable as TypeScript modules.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create stub test files for all Wave-2 / Wave-3 / Wave-4 surfaces with describe.skip() blocks</name>
  <files>app/src/services/__tests__/appleSignIn.test.ts, app/src/services/__tests__/googleSignIn.test.ts, app/src/services/__tests__/deepLink.test.ts, app/src/services/__tests__/payment.test.ts, app/src/services/__tests__/api.test.ts, app/src/stores/__tests__/authStore.test.ts, app/src/screens/__tests__/LoginScreen.test.tsx, app/src/screens/__tests__/PaymentScreen.test.tsx, app/src/components/__tests__/ActivatingProModal.test.tsx, app/src/config/__tests__/version.test.ts</files>
  <read_first>
    - app/__tests__/App.test.tsx (existing single-smoke test — the only existing test file; mirror its import style and react-test-renderer pattern)
    - app/jest.config.js (preset 'react-native')
    - app/tsconfig.json (verify strict mode + test file matching)
    - .planning/phases/05-mobile-sso-pro-cta/05-VALIDATION.md (per-task verification map — Task IDs 5-W0-01..5-VER-02 enumerate the behaviors each test file must eventually cover)
    - .planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md (§"Wave 0 Gaps" lines 748-760 — exact file list)
    - .planning/phases/05-mobile-sso-pro-cta/05-UI-SPEC.md (Copywriting Contract — exact i18n key names and locked CTA copy `Upgrade to Pro at risevpn.com`)
  </read_first>
  <behavior>
    - Test stub 1 (appleSignIn.test.ts): `describe.skip('signInWithApple', () => { it('returns identityToken on success', () => {}); it('throws on Apple sheet cancellation with code 1001', () => {}); })` — Wave 2 fills in
    - Test stub 2 (googleSignIn.test.ts): `describe.skip('configureGoogleSignIn', () => { it('calls GoogleSignin.configure once at app boot with webClientId', () => {}); }); describe.skip('signInWithGoogle', () => { it('returns idToken from userInfo.data.idToken (v16 shape)', () => {}); })` — Wave 2 fills in
    - Test stub 3 (deepLink.test.ts): `describe.skip('parseInvoiceFromUrl', () => { it('extracts invoiceId from vpnapp://payment/success?invoiceId=X', () => {}); it('returns null for wrong scheme', () => {}); it('returns null for missing invoiceId param', () => {}); it('decodeURIComponents the captured value', () => {}); })` — Wave 2 fills in
    - Test stub 4 (payment.test.ts): `describe.skip('upgradeUrlForLocale', () => { it('returns https://risevpn.com/ru/pricing?return=app for locale starting with ru', () => {}); it('returns https://risevpn.com/en/pricing?return=app otherwise', () => {}); }); describe.skip('getInvoice', () => { it('GETs /invoices/:id without escalate by default', () => {}); it('appends ?escalate=true when escalate=true is passed', () => {}); })` — Wave 2 fills in
    - Test stub 5 (api.test.ts): `describe.skip('axios 401 interceptor', () => { it('short-circuits refresh for /auth/* endpoints', () => {}); it('respects _skipAuthRefresh: true on request config', () => {}); })` — Wave 2 fills in
    - Test stub 6 (authStore.test.ts): `describe.skip('signInWithApple action', () => { it('POSTs identity_token+device fingerprint to /auth/apple', () => {}); it('preserves guest JWT in Authorization header (in-place promotion)', () => {}); it('catches CANCELED error silently', () => {}); }); describe.skip('signInWithGoogle action', () => { it('POSTs id_token+device fingerprint to /auth/google', () => {}); }); describe.skip('startActivatingPro / stopActivatingPro', () => { it('sets pendingInvoiceId + isActivatingPro=true', () => {}); it('clears them on stop', () => {}); })` — Wave 2 fills in
    - Test stub 7 (LoginScreen.test.tsx): `describe.skip('LoginScreen renders', () => { it('shows Apple + Google + Guest CTAs on iOS', () => {}); it('hides Apple CTA on Android', () => {}); })` — Wave 3 fills in
    - Test stub 8 (PaymentScreen.test.tsx): `describe.skip('PaymentScreen informational layout', () => { it('renders no $ price text anywhere', () => {}); it('renders exactly one CTA with text Upgrade to Pro at risevpn.com', () => {}); it('opens LeavingAppSheet before Linking.openURL on CTA tap', () => {}); })` — Wave 3 fills in
    - Test stub 9 (ActivatingProModal.test.tsx): `describe.skip('ActivatingProModal polling', () => { it('polls every 2000ms (fake timers)', () => {}); it('escalates after poll #5 (poll 6 url contains ?escalate=true)', () => {}); it('times out at 30s and shows takingLonger state', () => {}); it('closes modal + calls fetchAccount on status=paid', () => {}); it('navigates to Account on status=failed', () => {}); })` — Wave 3 fills in
    - Test stub 10 (version.test.ts): A NON-SKIPPED test: `it('APP_VERSION matches package.json version 2.2.0', () => { expect(APP_VERSION).toBe('2.2.0'); })` — initially RED until Wave 4 bumps both files. Test must compile NOW; Wave 4 makes it pass.
  </behavior>
  <action>
    Create the 10 test files listed in `<files>`. Each file uses `describe.skip(...)` blocks so `npm test` exits 0 immediately with all suites passing-as-skipped. The single exception is `app/src/config/__tests__/version.test.ts`, which is NOT skipped and intentionally RED (will turn GREEN when Wave 4 bumps APP_VERSION to '2.2.0').

    Template for each test file (replace placeholders per behavior section above):

    ```typescript
    // app/src/services/__tests__/appleSignIn.test.ts
    // Phase 5 Wave 0 scaffold — Wave 2 fills in implementations.
    // Tracks: 05-VALIDATION.md task 5-SVC-01, 5-SVC-02.

    describe.skip('signInWithApple', () => {
      it('returns identityToken on success', () => {
        // Wave 2: import signInWithApple from '../appleSignIn';
        // jest.mock('@invertase/react-native-apple-authentication');
        // expect await signInWithApple() to resolve with identityToken: 'mock-apple-id-token'
      });

      it('throws on Apple sheet cancellation with code 1001', () => {
        // Wave 2: override appleAuth.performRequest to reject with {code: '1001'}
        // expect await signInWithApple() to throw with code === appleAuth.Error.CANCELED
      });
    });
    ```

    Apply the same template style to each of the 10 files. Each file MUST:
    1. Begin with a comment naming the corresponding 05-VALIDATION.md task ID(s).
    2. Use `describe.skip` for all stubs except `version.test.ts`.
    3. Contain the EXACT `it(...)` strings listed in the `<behavior>` section above so Wave 2/Wave 3 can grep for them.

    For `app/src/config/__tests__/version.test.ts` use this EXACT content (single not-skipped test):

    ```typescript
    // app/src/config/__tests__/version.test.ts
    // Phase 5 Wave 0 — guards against version drift.
    // Tracks: 05-VALIDATION.md task 5-VER-01.
    // INTENTIONALLY RED until Wave 4 bumps APP_VERSION to '2.2.0'.

    import {APP_VERSION} from '../version';

    describe('APP_VERSION', () => {
      it('matches package.json version 2.2.0', () => {
        expect(APP_VERSION).toBe('2.2.0');
      });
    });
    ```

    Each test stub file MUST compile (`npx tsc --noEmit` exits 0 unchanged) and parse under Jest. Imports of `../appleSignIn`, `../googleSignIn`, `../deepLink`, `../payment` are NOT in the stubs (since those modules don't exist yet) — Wave 2 will add the imports when it fills the `describe.skip` blocks. The only real import is `version.test.ts` importing `APP_VERSION` from `../version` which already exists.

    Mark `version.test.ts` as intentionally red — Wave 4 will turn it green by editing `app/src/config/version.ts` to export `'2.2.0'`. Until Wave 4 runs, `cd app && npm test -- --testPathPattern=version.test` fails — this is expected and documented.

    Suppress this single expected red from blocking Wave 0 completion: Wave 0's verify command is `cd app && npm test -- --testPathIgnorePatterns=version.test` (passes), AND the standalone red on version.test is captured as a "must turn green in Wave 4" gate.
  </action>
  <verify>
    <automated>cd app && for f in src/services/__tests__/appleSignIn.test.ts src/services/__tests__/googleSignIn.test.ts src/services/__tests__/deepLink.test.ts src/services/__tests__/payment.test.ts src/services/__tests__/api.test.ts src/stores/__tests__/authStore.test.ts src/screens/__tests__/LoginScreen.test.tsx src/screens/__tests__/PaymentScreen.test.tsx src/components/__tests__/ActivatingProModal.test.tsx src/config/__tests__/version.test.ts; do test -f "$f" || { echo "MISSING: $f"; exit 1; }; done && grep -l 'describe.skip' src/services/__tests__/appleSignIn.test.ts src/services/__tests__/googleSignIn.test.ts src/services/__tests__/deepLink.test.ts src/services/__tests__/payment.test.ts src/services/__tests__/api.test.ts src/stores/__tests__/authStore.test.ts src/screens/__tests__/LoginScreen.test.tsx src/screens/__tests__/PaymentScreen.test.tsx src/components/__tests__/ActivatingProModal.test.tsx | wc -l | grep -q '^9$' && npm test -- --testPathIgnorePatterns=version.test 2>&1 | tail -5 | grep -E "Tests:.*(passed|skipped)"</automated>
  </verify>
  <acceptance_criteria>
    - All 10 test stub files exist at paths listed in `<files>`
    - 9 of them (all except `version.test.ts`) contain `describe.skip(`
    - `version.test.ts` contains exactly one non-skipped `it('matches package.json version 2.2.0'...)` block
    - `cd app && npm test -- --testPathIgnorePatterns=version.test` exits 0 (Jest reports tests skipped, suite passes)
    - `cd app && npm test -- --testPathPattern=version.test` exits 1 (intentional red until Wave 4)
    - Each test file references its 05-VALIDATION.md task ID in a header comment (grep `5-(SVC|UI|VER|W0)-` returns ≥10 matches across the 10 files)
  </acceptance_criteria>
  <done>All 10 stub files exist and pass `npm test --testPathIgnorePatterns=version.test`; `version.test.ts` is the only intentional red waiting on Wave 4.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Author 05-HUMAN-UAT.md operator-prerequisites checklist and append iOS Bundle ID BLOCKING question</name>
  <files>.planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md (D-21 operator prerequisites; D-19 Android smoke; D-20 iOS deferred)
    - .planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md (§"Risks & Open Questions" item #1 — iOS Bundle ID placeholder is a real BLOCKER; §"Environment Availability" table)
    - app/ios/VpnApp.xcodeproj/project.pbxproj (verify PRODUCT_BUNDLE_IDENTIFIER lines 274 + 303 still read `org.reactjs.native.example.*`)
    - .planning/phases/04-landing-surfaces/04-HUMAN-UAT.md (template style — match the structure used for Phase 4's UAT doc for consistency)
  </read_first>
  <action>
    Create `.planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md` with the following EXACT structure (replace placeholders with project values):

    ```markdown
    ---
    phase: 5
    slug: mobile-sso-pro-cta
    status: pending
    created: 2026-05-26
    ---

    # Phase 5 — Operator Prerequisites + Manual UAT

    > Two-part checklist:
    > 1. **BLOCKING prerequisites** (Wave-1 gate): operator MUST confirm these BEFORE Wave 1 starts. If any item is missing, the plan stops at the prereq gate.
    > 2. **Manual UAT** (post-Wave-4): operator-run smoke tests on real Android device per D-19, plus iOS test-deferred capture per D-20.

    ---

    ## Part 1 — BLOCKING Prerequisites (Wave-1 gate)

    Operator MUST confirm each item below by checking the box AND filling the value. If any "Required this phase" item is missing, ABORT — Wave 1 cannot proceed.

    ### Required this phase

    - [ ] **Apple Bundle ID**: registered in Apple Developer Portal with "Sign in with Apple" capability enabled.
        - Value: `____________________` (likely `com.vpnapp` to match Android `applicationId`)
        - Verified via: Apple Developer Portal → Identifiers → Bundle IDs → search for the value above, confirm "Sign In with Apple" appears in Capabilities list.
        - **Open Q #1 from RESEARCH.md:** Current iOS Bundle ID in `app/ios/VpnApp.xcodeproj/project.pbxproj` is the RN template placeholder (`org.reactjs.native.example.*`). Wave 1 Task 1 MUST replace both occurrences (line 274 + line 303) with the confirmed value above.

    - [ ] **Apple Service ID**: registered (already needed for Phase 2 — re-confirm operator has it).
        - Value: `____________________`

    - [ ] **Google OAuth Web Client ID** (the audience that backend validates — already issued for Phase 2 landing).
        - Value: `____________________.apps.googleusercontent.com`

    - [ ] **Google OAuth iOS Client ID** (new this phase — tied to iOS Bundle ID above).
        - Value: `____________________.apps.googleusercontent.com`
        - Reversed-client-id format for Info.plist: `com.googleusercontent.apps.<IOS_CLIENT_ID>`

    - [ ] **Google OAuth Android Client ID** (new this phase — tied to `com.vpnapp` + debug keystore SHA-1).
        - Value: `____________________.apps.googleusercontent.com`

    - [ ] **Android debug keystore SHA-1** registered with the Android OAuth client.
        - Extract via: `keytool -list -v -keystore app/android/app/debug.keystore -alias androiddebugkey -storepass android -keypass android | grep SHA1`
        - Value: `__:__:__:__:__:__:__:__:__:__:__:__:__:__:__:__:__:__:__:__`

    - [ ] **Server `MIN_APP_VERSION` env value** (so backend doesn't reject 2.2.0 as below-min). Confirm operator has scheduled the bump to `2.2.0` on the production API AT THE SAME TIME as the mobile release (per RESEARCH.md Open Q #3).
        - Current value: `____________________`
        - Will bump to `2.2.0` when: `____________________`

    ### NOT required this phase (deferred to end-of-milestone release phase per D-21)

    - [ ] Apple App Store Connect API key (`.p8` + key ID + issuer)
    - [ ] Play Console service account JSON
    - [ ] Production Android release keystore (a phase-internal keystore is acceptable for Wave 4 smoke — generate and document its location)
    - [ ] Apple external-link entitlement form approval

    ---

    ## Part 2 — Manual UAT (post-Wave-4)

    > Operator runs these on a physical Android device per D-19; iOS items are deferred per D-20.

    ### Android smoke (REQUIRED for phase completion per D-19)

    - [ ] **Build signed `.aab`**: `cd app/android && ./gradlew bundleRelease` → produces `app/android/app/build/outputs/bundle/release/app-release.aab`.
    - [ ] **Install on operator's Android device** (via `bundletool` → APK or `assembleRelease`).
    - [ ] **Google sign-in works**: launch app → Account → Sign in to sync Pro → Google → complete Google sheet → land on Home with `auth_provider: 'google'`.
    - [ ] **Guest → Google upgrade preserves users.id**: verify admin panel `SELECT * FROM users WHERE auth_provider='google'` shows ONE row with the ORIGINAL guest's `users.id` (not a new row).
    - [ ] **PaymentScreen informational layout**: navigate to Payment → confirm (a) no price text anywhere on screen, (b) exactly one primary CTA reading "Upgrade to Pro at risevpn.com", (c) tertiary "Already paid? Refresh" text link below.
    - [ ] **Interstitial → browser handoff**: tap CTA → confirm "You're leaving the app" sheet appears → tap Continue → confirm Chrome opens to `https://risevpn.com/en/pricing?return=app` (or `/ru/` if device language is Russian).
    - [ ] **Deep-link receive**: in Chrome address bar type `vpnapp://payment/success?invoiceId=test123` → confirm app foregrounds and Activating-Pro modal appears. (Polling will fail with 404 since `test123` is not a real invoice — modal stays in polling state until 30s timeout switches to "takingLonger" state.)

    ### iOS smoke (DEFERRED per D-20)

    - [ ] iOS test plan recorded; runtime verification deferred to end-of-milestone release phase.
    - [ ] iOS code compiles cleanly: `cd app/ios && pod install && xcodebuild -workspace VpnApp.xcworkspace -scheme VpnApp -configuration Debug -sdk iphonesimulator build` (CI-style; runs on macOS only).

    ### Release-prep (post-smoke)

    - [ ] `app/package.json` version === `2.2.0` (grep `'"version": "2.2.0"'`)
    - [ ] `app/src/config/version.ts` `APP_VERSION` === `'2.2.0'`
    - [ ] `app/android/app/build.gradle` `versionName "2.2.0"` + `versionCode 13`
    - [ ] `app/ios/VpnApp.xcodeproj/project.pbxproj` `MARKETING_VERSION = 2.2.0;` + `CURRENT_PROJECT_VERSION = 2;`
    - [ ] `cd app && npm test` exits 0 (all suites green including `version.test`)
    - [ ] `cd app && npx tsc --noEmit` exits 0

    ---

    ## Sign-off

    - [ ] Operator confirms Part 1 prereqs filled — date: `____________________`
    - [ ] Operator confirms Part 2 UAT passed — date: `____________________`
    - [ ] Phase 5 complete — date: `____________________`
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md && grep -q "Apple Bundle ID" .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md && grep -q "vpnapp://payment/success?invoiceId=test123" .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md && grep -q "MIN_APP_VERSION" .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md && grep -q "Upgrade to Pro at risevpn.com" .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md`
    - Contains BLOCKING prereq for "Apple Bundle ID" (Open Q #1 from RESEARCH.md)
    - Contains all 6 required-this-phase prereqs from CONTEXT.md D-21 (Apple Bundle ID + Service ID, Google iOS/Android/Web client IDs, Android debug keystore SHA-1)
    - Contains the deep-link smoke test exact URL `vpnapp://payment/success?invoiceId=test123`
    - Contains the locked CTA copy "Upgrade to Pro at risevpn.com" in the UAT verification checklist
    - Contains the upgrade URL exact value `https://risevpn.com/en/pricing?return=app`
    - Contains a section noting which prereqs are NOT required this phase (deferred per D-21 to end-of-milestone)
  </acceptance_criteria>
  <done>05-HUMAN-UAT.md captures the BLOCKING prereqs gate AND the Wave 4 smoke checklist; operator has a single document to fill in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-files → production-files | Wave-0 scaffolding only mocks/tests, no production wiring — boundary is one-way: stubs reference symbols that don't exist yet (Wave 2/3 create them) |
| operator-input → planning-artifacts | The HUMAN-UAT prereqs gate (Apple Bundle ID, Google client IDs, keystore SHA-1) is operator-furnished — Wave 1 trusts these inputs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6 (CONTEXT.md D-22) | Tampering / Elevation | Manual mocks for `@invertase/react-native-apple-authentication` + `@react-native-google-signin/google-signin` | mitigate | Mocks are committed to repo, reviewable in PR; they live in `app/__mocks__/` (Jest-only path, never bundled into production builds — RN packager excludes `__mocks__/` per Metro defaults). Mocks shape EXACTLY mirrors the real library v2.5.1 / v16.1.2 spec from RESEARCH.md so production code swaps in without API drift. |
| T-2 (Apple/Google id-token replay) | Spoofing | Test fixtures with `'mock-apple-id-token'` / `'mock-google-id-token'` strings | accept | Fixture strings are obviously fake and never persisted. No real id-tokens in any test file. |
| Operator-input integrity | Spoofing | HUMAN-UAT prereq fields (Apple Bundle ID, OAuth client IDs, SHA-1) | accept | Operator fills these from authoritative sources (Apple Developer Portal, Google Cloud Console). No client-side validation possible; downstream Wave 1 task verifies via `grep`/`xmllint`/build output. |
| T-1, T-3, T-4, T-5, T-7 | (various from CONTEXT.md D-22) | — | not-applicable | Wave 0 produces NO production code that handles deep-links, tokens, or network requests. These threats apply to Waves 1–4 plans only; this plan's threat surface is zero. |
</threat_model>

<verification>
After all three tasks complete:

1. `cd app && npm test -- --testPathIgnorePatterns=version.test` exits 0 — all 9 skip-suites pass.
2. `cd app && npx tsc --noEmit` exits 0 — no type errors introduced by test stubs.
3. `ls app/__mocks__/@invertase/react-native-apple-authentication.ts` and `ls app/__mocks__/@react-native-google-signin/google-signin.ts` both succeed.
4. `wc -l app/src/{services,stores,screens,components,config}/__tests__/*.{ts,tsx} 2>/dev/null | tail -1` shows ≥10 files counted.
5. `grep -l "describe.skip" app/src/**/*.test.{ts,tsx} 2>/dev/null | wc -l` returns 9 (every stub except `version.test.ts`).
6. `cd app && npm test -- --testPathPattern=version.test` exits 1 — confirmed intentional red waiting on Wave 4.
7. `.planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md` exists with all BLOCKING + manual UAT sections.
</verification>

<success_criteria>
Wave 0 is complete when:
- [ ] Both manual mocks exist and load without error in a Jest environment
- [ ] All 10 stub test files exist; 9 use `describe.skip(...)`, 1 (`version.test.ts`) is intentionally red
- [ ] `npm test --testPathIgnorePatterns=version.test` exits 0
- [ ] `05-HUMAN-UAT.md` contains the Wave-1 BLOCKING prereqs gate and the Wave-4 manual smoke checklist
- [ ] Operator now has a single document to confirm prereqs against before Wave 1 starts
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-sso-pro-cta/05-00-SUMMARY.md` capturing:
- File list (mocks + test stubs + UAT doc)
- Initial Jest run output (`npm test --testPathIgnorePatterns=version.test`)
- Confirmation that Wave 1 cannot start until operator fills the BLOCKING prereqs in 05-HUMAN-UAT.md Part 1
</output>
