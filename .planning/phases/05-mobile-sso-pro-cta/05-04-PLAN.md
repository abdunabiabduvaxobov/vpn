---
phase: 05-mobile-sso-pro-cta
plan: 04
type: execute
wave: 4
depends_on: [05-03-PLAN.md]
files_modified:
  - app/package.json
  - app/src/config/version.ts
  - app/android/app/build.gradle
  - app/ios/VpnApp.xcodeproj/project.pbxproj
  - .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md
autonomous: false
requirements: [APP-07]

must_haves:
  truths:
    - "app/package.json version === '2.2.0'"
    - "app/src/config/version.ts APP_VERSION export === '2.2.0'"
    - "app/android/app/build.gradle versionName 2.2.0 + versionCode 13 (bumped from 12)"
    - "app/ios/VpnApp.xcodeproj/project.pbxproj MARKETING_VERSION = 2.2.0 + CURRENT_PROJECT_VERSION = 2 (bumped from 1)"
    - "All four version sources are in lockstep — no drift between package.json and any native version field"
    - "Wave-0 version.test now exits 0 (the intentional red is closed)"
    - "Full Jest suite exits 0 — every test green including version.test"
    - "`cd app && npx tsc --noEmit` exits 0"
    - "`cd app/android && ./gradlew bundleRelease` produces a signed .aab at app/android/app/build/outputs/bundle/release/app-release.aab"
    - "Operator has smoke-tested the .aab on their physical Android device per 05-HUMAN-UAT.md Part 2 and signed off"
  artifacts:
    - path: "app/package.json"
      provides: "Bumped version field"
      contains: "\"version\": \"2.2.0\""
    - path: "app/src/config/version.ts"
      provides: "Bumped APP_VERSION constant (X-App-Version request header)"
      contains: "APP_VERSION = '2.2.0'"
    - path: "app/android/app/build.gradle"
      provides: "Bumped versionName + versionCode for Play Internal Track release"
      contains: "versionName \"2.2.0\""
    - path: "app/ios/VpnApp.xcodeproj/project.pbxproj"
      provides: "Bumped MARKETING_VERSION + CURRENT_PROJECT_VERSION for iOS release"
      contains: "MARKETING_VERSION = 2.2.0"
    - path: "app/android/app/build/outputs/bundle/release/app-release.aab"
      provides: "Signed Android App Bundle for Play Internal upload (operator UAT artifact)"
    - path: ".planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md"
      provides: "Operator-signed UAT checklist Part 2"
      contains: "Phase 5 complete"
  key_links:
    - from: "app/package.json (version field)"
      to: "app/src/config/version.ts (APP_VERSION constant)"
      via: "Both must be 2.2.0; api.ts L19 sends APP_VERSION as X-App-Version header"
      pattern: "2.2.0"
    - from: "app/android/app/build.gradle versionName"
      to: "app/package.json version"
      via: "Both must be 2.2.0 (Play Console + npm/RN tooling alignment)"
      pattern: "2.2.0"
---

<objective>
Close the version drift between the four version sources (package.json, version.ts, Android build.gradle, iOS pbxproj), turn Wave-0's intentionally-red `version.test` GREEN, produce a signed Android `.aab` via `./gradlew bundleRelease`, and run the operator-led manual UAT smoke per 05-HUMAN-UAT.md Part 2 (D-19 verification path). This plan is `autonomous: false` because it ends in a `checkpoint:human-verify` for the operator to install the signed build on their Android device and check off the manual UAT items.

Purpose: APP-07's literal wording requires TestFlight + Play Internal Track uploads, but D-18 deliberately deviates — Phase 5's bar is "code complete + signed local Android build + smoke-tested on operator's device". Uploads land in a later end-of-milestone phase. The version bumps still happen here (D-17) so the codebase is in a shippable state for that later release phase.

Output: 4 version files bumped + signed `.aab` produced + 05-HUMAN-UAT.md Part 2 ticked off by operator + Phase 5 complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md
@.planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md
@.planning/phases/05-mobile-sso-pro-cta/05-VALIDATION.md
@.planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md
@.planning/phases/05-mobile-sso-pro-cta/05-03-SUMMARY.md
@app/package.json
@app/src/config/version.ts
@app/android/app/build.gradle
@app/ios/VpnApp.xcodeproj/project.pbxproj

<interfaces>
<!-- Current version state (pre-bump): -->

From app/package.json:
```
"version": "2.1.0"
```

From app/src/config/version.ts:
```typescript
// Must match the version in app/package.json and Android versionName.
export const APP_VERSION = '2.1.0'; // (or similar — task verifies and bumps to '2.2.0')
```

From app/android/app/build.gradle:
```
versionCode 12
versionName "2.1.0"
```

From app/ios/VpnApp.xcodeproj/project.pbxproj (lines around 260-300):
```
CURRENT_PROJECT_VERSION = 1;
MARKETING_VERSION = 1.0;
```
(Note: iOS MARKETING_VERSION is 1.0 currently — bumping to 2.2.0 catches up to the cross-platform version per D-17.)

Target version state (post-bump per D-17):
- app/package.json: 2.2.0
- app/src/config/version.ts: APP_VERSION = '2.2.0'
- app/android/app/build.gradle: versionName "2.2.0" + versionCode 13
- app/ios/VpnApp.xcodeproj/project.pbxproj: MARKETING_VERSION = 2.2.0 + CURRENT_PROJECT_VERSION = 2

Wave-0 test that turns green:
- app/src/config/__tests__/version.test.ts asserts `APP_VERSION === '2.2.0'` — currently RED, becomes GREEN after Task 1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bump all four version sources to 2.2.0 + verify Wave-0 version.test turns green + full suite passes</name>
  <files>app/package.json, app/src/config/version.ts, app/android/app/build.gradle, app/ios/VpnApp.xcodeproj/project.pbxproj</files>
  <read_first>
    - app/package.json (current version field — confirm "2.1.0" or similar pre-bump)
    - app/src/config/version.ts (current APP_VERSION constant)
    - app/android/app/build.gradle (current versionCode 12 + versionName "2.1.0" per code_context inspection)
    - app/ios/VpnApp.xcodeproj/project.pbxproj (current MARKETING_VERSION = 1.0 + CURRENT_PROJECT_VERSION = 1 at lines 260 + 268 + 290 + 297 per inspection — Debug + Release configs)
    - app/src/config/__tests__/version.test.ts (Wave-0 stub asserting APP_VERSION === '2.2.0' — currently RED)
    - .planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md (D-17 all four version sources updated together)
  </read_first>
  <action>
    Make the four version edits below. Each is a single targeted modification — do NOT touch any other field.

    **Edit 1: `app/package.json`** — change the existing `"version"` field value to `"2.2.0"`:

    ```json
    "version": "2.2.0",
    ```

    **Edit 2: `app/src/config/version.ts`** — change the `APP_VERSION` constant export to `'2.2.0'`. The file structure should be (preserve any existing comments):

    ```typescript
    // Single source of truth for the client's app version.
    // Must match the version in app/package.json and Android versionName.
    // Backend reads this from the X-App-Version request header and rejects
    // any request whose version is missing or below MIN_APP_VERSION.
    export const APP_VERSION = '2.2.0';
    ```

    **Edit 3: `app/android/app/build.gradle`** — bump both `versionCode` AND `versionName`. Find the existing lines 85 + 86 (per code_context inspection):

    ```
    versionCode 12
    versionName "2.1.0"
    ```

    Replace with:

    ```
    versionCode 13
    versionName "2.2.0"
    ```

    DO NOT change any other lines in build.gradle — applicationId, minSdkVersion, targetSdkVersion, dependencies all remain untouched.

    **Edit 4: `app/ios/VpnApp.xcodeproj/project.pbxproj`** — bump BOTH `MARKETING_VERSION` AND `CURRENT_PROJECT_VERSION` in BOTH Debug AND Release build config blocks. There are 4 lines total per inspection:

    Find lines around 260, 268, 290, 297 (these are the four locations — two for `CURRENT_PROJECT_VERSION = 1;` and two for `MARKETING_VERSION = 1.0;`):

    ```
    CURRENT_PROJECT_VERSION = 1;
    MARKETING_VERSION = 1.0;
    ```

    Replace each occurrence with:

    ```
    CURRENT_PROJECT_VERSION = 2;
    MARKETING_VERSION = 2.2.0;
    ```

    All 4 lines (2 × CURRENT_PROJECT_VERSION + 2 × MARKETING_VERSION) must be updated. If the test/extension targets in the same pbxproj have their own MARKETING_VERSION/CURRENT_PROJECT_VERSION lines, update those too to maintain parity.

    After all 4 edits, run the version test and full suite:

    ```bash
    cd app && npm test -- --testPathPattern=version.test
    cd app && npm test
    cd app && npx tsc --noEmit
    ```

    Each command must exit 0. The version.test that was Wave-0's intentional red now turns GREEN.
  </action>
  <verify>
    <automated>cd app && grep -q '"version": "2.2.0"' package.json && grep -q "APP_VERSION = '2.2.0'" src/config/version.ts && grep -q 'versionName "2.2.0"' android/app/build.gradle && grep -q "versionCode 13" android/app/build.gradle && grep -q "MARKETING_VERSION = 2.2.0" ios/VpnApp.xcodeproj/project.pbxproj && grep -q "CURRENT_PROJECT_VERSION = 2" ios/VpnApp.xcodeproj/project.pbxproj && ! grep -qE 'versionName\s+"2\.1\.0"' android/app/build.gradle && ! grep -q "MARKETING_VERSION = 1.0" ios/VpnApp.xcodeproj/project.pbxproj && npm test -- --testPathPattern=version.test 2>&1 | grep -E "Tests:.*1 passed" && npm test 2>&1 | grep -E "Tests:.*passed" | grep -v "failed" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"version": "2.2.0"' app/package.json` exits 0
    - `grep "APP_VERSION = '2.2.0'" app/src/config/version.ts` exits 0
    - `grep 'versionName "2.2.0"' app/android/app/build.gradle` exits 0
    - `grep "versionCode 13" app/android/app/build.gradle` exits 0
    - `grep "MARKETING_VERSION = 2.2.0" app/ios/VpnApp.xcodeproj/project.pbxproj` returns ≥2 matches (Debug + Release)
    - `grep "CURRENT_PROJECT_VERSION = 2" app/ios/VpnApp.xcodeproj/project.pbxproj` returns ≥2 matches
    - `grep "MARKETING_VERSION = 1.0" app/ios/VpnApp.xcodeproj/project.pbxproj` exits 1 (old value fully removed)
    - `grep 'versionName "2.1.0"' app/android/app/build.gradle` exits 1 (old value removed)
    - `cd app && npm test -- --testPathPattern=version.test` exits 0 (the intentional red is now green)
    - `cd app && npm test` exits 0 (full suite green)
    - `cd app && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All four version sources bumped to 2.2.0 in lockstep; Wave-0 version.test now green; full test suite + type-check pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run signed Android release build (./gradlew bundleRelease) + verify .aab artifact</name>
  <files>app/android/app/build/outputs/bundle/release/app-release.aab</files>
  <read_first>
    - app/android/app/build.gradle (verify signingConfigs section — operator needs to confirm release keystore is set up per 05-HUMAN-UAT.md Part 1; if not, use a phase-internal keystore documented in HUMAN-UAT)
    - .planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md (D-19 — signed .aab is the SC#5 evidence path)
    - .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md (operator-confirmed release keystore location and credentials)
  </read_first>
  <action>
    Confirm the Android release keystore is configured. Open `app/android/app/build.gradle` and verify there's a `signingConfigs.release` block (or `signingConfigs.debug` being aliased to release for phase-internal smoke). If absent, generate a phase-internal release keystore per D-21:

    ```bash
    cd app/android/app
    # Only run if release keystore is missing — operator should already have one or have provisioned a phase-internal keystore per 05-HUMAN-UAT.md.
    # keytool -genkey -v -keystore phase5-release.keystore -alias phase5release -keyalg RSA -keysize 2048 -validity 10000
    ```

    Then add the keystore config to `build.gradle` if not already present. The location and credentials must match what the operator documented in 05-HUMAN-UAT.md.

    Run the release build:

    ```bash
    cd app/android
    ./gradlew clean
    ./gradlew bundleRelease
    ```

    Expected output:
    - `BUILD SUCCESSFUL` from Gradle
    - Artifact at `app/android/app/build/outputs/bundle/release/app-release.aab`

    Verify the artifact:

    ```bash
    test -f app/android/app/build/outputs/bundle/release/app-release.aab && \
      ls -la app/android/app/build/outputs/bundle/release/app-release.aab
    ```

    Extract the APK from the .aab for `adb install` smoke (operator UAT path) — only if `bundletool` is available:

    ```bash
    # Optional — operator may use Android Studio's Build → Generate Signed Bundle/APK menu instead.
    # bundletool build-apks --bundle=app-release.aab --output=app-release.apks --mode=universal --ks=<keystore> --ks-pass=pass:<storepass> --key-pass=pass:<keypass> --ks-key-alias=<alias>
    ```

    If `bundletool` is not available or operator prefers, fall back to `./gradlew assembleRelease` to produce an APK directly:

    ```bash
    cd app/android
    ./gradlew assembleRelease
    # Produces app/android/app/build/outputs/apk/release/app-release.apk
    ```

    Document the artifact path and SHA-256 checksum in 05-HUMAN-UAT.md Part 2 ("Build signed `.aab`" checkbox).

    DO NOT attempt to upload to Play Console — D-18 explicitly defers that to the end-of-milestone release phase.
  </action>
  <verify>
    <automated>cd app/android && test -f app/build/outputs/bundle/release/app-release.aab && ls -la app/build/outputs/bundle/release/app-release.aab | grep -qE "[0-9]+M|[0-9]{7,}" && (sha256sum app/build/outputs/bundle/release/app-release.aab 2>/dev/null || shasum -a 256 app/build/outputs/bundle/release/app-release.aab)</automated>
  </verify>
  <acceptance_criteria>
    - `app/android/app/build/outputs/bundle/release/app-release.aab` exists
    - File size is non-trivial (≥1MB — sanity check that the build didn't produce an empty artifact)
    - SHA-256 checksum recorded in 05-HUMAN-UAT.md (operator can verify against their device-install)
    - Build log shows `BUILD SUCCESSFUL` (Gradle output)
    - No upload to Play Console attempted (D-18 explicit defer)
  </acceptance_criteria>
  <done>Signed Android .aab produced at the expected path; SHA-256 recorded; operator can proceed to manual install + smoke.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Operator manual UAT — install signed .aab on Android device + verify 6 smoke items + sign off</name>
  <files>.planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md (Part 2 manual UAT checklist — every item must be ticked)
    - .planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md (D-19 — 5 smoke items: Google sign-in, guest→Google promotion, PaymentScreen layout, interstitial→browser handoff, deep-link receive)
  </read_first>
  <what-built>
    Task 1 bumped all four version sources to 2.2.0; Task 2 produced a signed `.aab` at `app/android/app/build/outputs/bundle/release/app-release.aab`. The build is ready for operator smoke-test on their physical Android device per D-19.
  </what-built>
  <how-to-verify>
    Operator MUST:

    1. **Install the .aab on the physical Android device**:
       - Either extract APK via `bundletool` and `adb install` it, OR
       - Run `./gradlew assembleRelease` and `adb install app/android/app/build/outputs/apk/release/app-release.apk`.
       - Confirm: app version on device reads `2.2.0` (Settings → Apps → Rise VPN → version).

    2. **Verify the 6 smoke items** from 05-HUMAN-UAT.md Part 2 → "Android smoke":
       - [ ] **Build installed**: app launches; Home shows the connect button.
       - [ ] **Google sign-in works**: navigate Account → "Sign in to sync Pro" → Google → complete Google sheet → land back on Home → AccountScreen shows the linked Google account (the "Sign in to sync Pro" card has disappeared per D-03).
       - [ ] **Guest → Google preserves users.id**: verify in admin panel that `SELECT id, auth_provider FROM users WHERE email = '<operator-email>'` returns ONE row with the ORIGINAL guest's `users.id` (not a new row) and `auth_provider = 'google'`.
       - [ ] **PaymentScreen informational layout**: navigate to Payment → confirm (a) NO `$` or `€` or "/mo" price text anywhere, (b) exactly ONE primary CTA reading "Upgrade to Pro at risevpn.com", (c) tertiary "Already paid? Refresh" text link below the CTA.
       - [ ] **Interstitial → browser handoff**: tap the CTA → confirm "You're leaving the app" sheet appears with Continue + Cancel → tap Continue → confirm Chrome opens to `https://risevpn.com/en/pricing?return=app` (or `/ru/` if device language is Russian).
       - [ ] **Deep-link receive**: in Chrome address bar type `vpnapp://payment/success?invoiceId=test123` → confirm app foregrounds and the "Activating your Pro subscription…" modal appears with a spinner. (Polling will fail with 404 since `test123` isn't a real invoice — modal stays polling until the 30s timeout, then switches to "We're processing your payment…" state with Refresh + Telegram + Close buttons.)

    3. **Sign off** by editing 05-HUMAN-UAT.md Part 2: convert every `- [ ]` to `- [x]` for the smoke items above, and fill in the "Operator confirms Part 2 UAT passed" date line at the bottom.

    4. **Report back** in this checkpoint with either "approved" + the date or a list of failures.

    If any smoke item fails, ABORT — do not mark the phase complete. Failure mode handling:
    - Google sign-in 401: re-verify Web Client ID + SHA-1 registration per 05-HUMAN-UAT.md Part 1.
    - Modal stuck on activating: check `adb logcat | grep -i 'invoice\|polling'` for backend connectivity.
    - Browser doesn't open: re-check `Linking.openURL` permissions + intent-filter on AndroidManifest.
    - Deep link doesn't open app: re-verify intent-filter has `<category android:name="android.intent.category.BROWSABLE" />`.
  </how-to-verify>
  <resume-signal>
    Reply with "approved" and the smoke-test date when all 6 items pass, OR "ABORT — <failing item> — <observation>" if any item fails. Once approved, executor (a) verifies 05-HUMAN-UAT.md has all Part 2 boxes ticked, (b) creates `.planning/phases/05-mobile-sso-pro-cta/05-04-SUMMARY.md`, (c) updates STATE.md to mark Phase 5 complete.
  </resume-signal>
  <acceptance_criteria>
    - User responds with "approved" + smoke-test date
    - 05-HUMAN-UAT.md Part 2 Android smoke section has all 6 boxes `- [x]`
    - 05-HUMAN-UAT.md "Operator confirms Part 2 UAT passed — date: ___" line is filled
    - 05-HUMAN-UAT.md "Phase 5 complete — date: ___" line is filled
    - No failing smoke items reported
  </acceptance_criteria>
  <action>This is a checkpoint:human-verify — pause for operator smoke-test on physical Android device. Display the 6-item smoke checklist from `<how-to-verify>` and the signed `.aab` location (`app/android/app/build/outputs/bundle/release/app-release.aab`) + recorded SHA-256 to the operator. Wait for "approved" + date OR an "ABORT — &lt;item&gt; — &lt;observation&gt;" reply. On approval: edit `.planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md` Part 2 — convert every `- [ ]` in the Android smoke section to `- [x]`, fill the "Operator confirms Part 2 UAT passed — date" line and the "Phase 5 complete — date" line with the operator-provided date. Then create the phase SUMMARY and update STATE.md per the `<output>` block.</action>
  <verify>
    <automated>grep -E "^\s*-\s*\[x\]" .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md | wc -l | grep -qE "^[ ]*[1-9][0-9]" && grep -E "Phase 5 complete — date: 2[0-9]{3}-[0-9]{2}-[0-9]{2}" .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md</automated>
  </verify>
  <done>Operator signed off on the 6 Android smoke items; 05-HUMAN-UAT.md Part 2 fully ticked + sign-off dates recorded; Phase 5 closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → signed .aab → Android device | Build artifact is produced locally and installed via adb; no third-party upload service in the loop this phase |
| device-installed app → backend | Backend `X-App-Version` gate (`MIN_APP_VERSION` env) must be 2.2.0 or lower at smoke time — operator confirmed in 05-HUMAN-UAT.md Part 1 |
| Apple/Google sign-in (live) → backend | Smoke exercises the real Google sign-in sheet → real backend `/auth/google` → real users table row (operator's own account); Apple is not exercised on Android |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| Version drift | Configuration / tampering | 4 version sources | mitigate | Task 1 acceptance criteria verify all 4 are in lockstep AND old values are absent. Wave-0 version.test acts as a regression guard for future bumps. |
| Signed .aab tampered before operator install | Tampering | `app-release.aab` artifact | mitigate | SHA-256 checksum recorded in 05-HUMAN-UAT.md. Operator can verify checksum before `adb install`. |
| Smoke-test against wrong backend env | Configuration error | `api.ts` BASE_URL | accept | api.ts auto-switches between dev and production based on `__DEV__`. Release build (`./gradlew bundleRelease`) sets `__DEV__=false` → hits production. Operator UAT runs against production backend; backend `MIN_APP_VERSION` coordination is captured in 05-HUMAN-UAT.md Part 1. |
| Operator-created users.id orphan during guest→Google smoke | Data integrity | backend in-place promotion (Phase 2 D-06) | mitigate | Smoke item explicitly checks admin panel for `users.id` preservation. If two rows appear, the operator reports failure and the team investigates whether D-06 in-place promotion path was correctly invoked from the mobile side (axios attached the guest token? api.ts `_skipAuthRefresh` short-circuit fired?). |
| `MIN_APP_VERSION` server-side rejection | DoS (smoke-test only) | backend version gate | mitigate | Operator confirmed in 05-HUMAN-UAT.md Part 1 that backend `MIN_APP_VERSION` will be bumped to 2.2.0 AT THE SAME TIME as the mobile release (not before). The 2.2.0 build sends `X-App-Version: 2.2.0` which backend accepts. |
| T-1..T-7 (all phase threats) | various | (covered by Waves 1-3) | mitigate | Phase 5 plans 00-03 collectively address every T-N. Wave 4 produces no new attack surface — only build + smoke. The smoke checklist VERIFIES the threats are mitigated end-to-end (deep-link spoofing test: typing the URL in Chrome and observing the polling modal exercises T-1; PaymentScreen no-price check exercises App Store compliance posture). |
</threat_model>

<verification>
After all 3 tasks complete:

1. All 4 version files bumped to 2.2.0 (Task 1).
2. Full Jest suite green: `cd app && npm test` exits 0 (Wave-0 version.test now green).
3. `cd app && npx tsc --noEmit` exits 0.
4. Signed `.aab` exists at `app/android/app/build/outputs/bundle/release/app-release.aab`.
5. 05-HUMAN-UAT.md Part 2 fully ticked off by operator.
6. Operator signed off "Phase 5 complete — date: <YYYY-MM-DD>".
</verification>

<success_criteria>
Phase 5 is complete when:
- [ ] Task 1: All 4 version sources at 2.2.0; Wave-0 version.test green; full suite + tsc green
- [ ] Task 2: Signed Android `.aab` produced; SHA-256 recorded
- [ ] Task 3: Operator smoke-tested on physical device; 6 items verified; signed off
- [ ] 05-HUMAN-UAT.md Part 2 fully ticked
- [ ] No upload to TestFlight or Play Internal attempted (D-18 deferred to end-of-milestone phase)
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-sso-pro-cta/05-04-SUMMARY.md` capturing:
- 4-file version diff summary
- Signed `.aab` path + SHA-256
- Operator UAT sign-off date
- Full Jest output (all green including version.test)
- Confirmation that uploads to TestFlight/Play Internal are deferred to end-of-milestone release phase
- Phase 5 closeout: SC#1 (Apple → Home), SC#2 (guest→SSO preserves id), SC#3 (informational PaymentScreen), SC#4 (deep-link → modal), SC#5 (2.2.0 + smoked) — note SC#5 is DELIBERATELY DEVIATED per D-18 (local smoke instead of TestFlight upload).

Also update `.planning/STATE.md`:
- Mark Phase 5 as `[x]` in the Phases list
- Update Performance Metrics: phases complete +1, plans complete +5
- Update Current Position to next phase
</output>
