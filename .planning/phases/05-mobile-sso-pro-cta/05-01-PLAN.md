---
phase: 05-mobile-sso-pro-cta
plan: 01
type: execute
wave: 1
depends_on: [05-00-PLAN.md]
files_modified:
  - app/package.json
  - app/package-lock.json
  - app/ios/Podfile.lock
  - app/ios/VpnApp/Info.plist
  - app/ios/VpnApp/VpnApp.entitlements
  - app/ios/VpnApp/AppDelegate.swift
  - app/ios/VpnApp.xcodeproj/project.pbxproj
  - app/android/app/src/main/AndroidManifest.xml
  - app/android/app/src/main/res/values/strings.xml
autonomous: false
requirements: [APP-01, APP-02, APP-06]

must_haves:
  truths:
    - "Operator has confirmed all BLOCKING prereqs in 05-HUMAN-UAT.md Part 1 (Apple Bundle ID, Google iOS/Android/Web Client IDs, Android keystore SHA-1)"
    - "Both new SSO npm packages are installed at EXACT pinned versions (2.5.1 + 16.1.2) per T-6 supply-chain mitigation"
    - "iOS Info.plist declares CFBundleURLTypes containing the 'vpnapp' scheme AND the Google reversed-client-id"
    - "iOS VpnApp.entitlements contains com.apple.developer.applesignin key set to ['Default']"
    - "iOS AppDelegate.swift implements application(_:open:options:) forwarding to RCTLinkingManager (and GIDSignIn handler before it)"
    - "iOS project.pbxproj PRODUCT_BUNDLE_IDENTIFIER replaced with real value (no longer RN template placeholder)"
    - "Android AndroidManifest.xml MainActivity has intent-filter for vpnapp scheme with BROWSABLE category"
    - "Android strings.xml has server_client_id string resource set to the Web OAuth Client ID"
    - "npm audit --omit=dev reports zero high/critical vulnerabilities for the two new SSO packages"
  artifacts:
    - path: "app/package.json"
      provides: "Pinned SSO library versions in dependencies block"
      contains: "@invertase/react-native-apple-authentication"
    - path: "app/ios/VpnApp/Info.plist"
      provides: "CFBundleURLTypes for vpnapp + Google reversed-client-id"
      contains: "CFBundleURLSchemes"
    - path: "app/ios/VpnApp/VpnApp.entitlements"
      provides: "Sign in with Apple capability"
      contains: "com.apple.developer.applesignin"
    - path: "app/ios/VpnApp/AppDelegate.swift"
      provides: "URL handler delegating to GIDSignIn + RCTLinkingManager"
      contains: "application(_ app: UIApplication"
    - path: "app/android/app/src/main/AndroidManifest.xml"
      provides: "Deep-link intent-filter on MainActivity"
      contains: "android:scheme=\"vpnapp\""
    - path: "app/android/app/src/main/res/values/strings.xml"
      provides: "Google Web OAuth Client ID resource"
      contains: "server_client_id"
  key_links:
    - from: "app/ios/VpnApp/Info.plist"
      to: "@react-native-google-signin/google-signin native iOS pod"
      via: "CFBundleURLSchemes entry containing com.googleusercontent.apps.<IOS_OAUTH_CLIENT_ID>"
      pattern: "com\\.googleusercontent\\.apps"
    - from: "app/ios/VpnApp/AppDelegate.swift"
      to: "Linking JS module via RCTLinkingManager"
      via: "application(_:open:options:) implementation"
      pattern: "RCTLinkingManager\\.application"
    - from: "app/android/app/src/main/AndroidManifest.xml MainActivity"
      to: "React Native Linking module (vpnapp scheme)"
      via: "<intent-filter><data android:scheme=\"vpnapp\" /></intent-filter>"
      pattern: "android:scheme=\"vpnapp\""
---

<objective>
Land all Phase 5 native-side infrastructure: install the two new SSO npm packages at pinned versions, fix the iOS Bundle ID (currently the RN template placeholder per RESEARCH.md Open Q #1), add iOS URL schemes + Sign-in-with-Apple entitlement + AppDelegate URL handler, add Android deep-link intent-filter + Google Web Client ID string resource, and run `pod install`. This plan is `autonomous: false` because it begins with a `checkpoint:human-action` for the operator to fill in the BLOCKING prereqs values from 05-HUMAN-UAT.md.

Purpose: Wave 2 services (Apple/Google sign-in wrappers) and Wave 3 UI cannot function without the native registration. iOS Sign-in-with-Apple fails with error code 1000 if Bundle ID is the template placeholder. Android Google sign-in returns `null` idToken if SHA-1 isn't registered. Deep links never reach JS without the intent-filter / CFBundleURLTypes.

Output: 9 files modified, `cd app && npm install` clean, `cd app/ios && pod install` clean, `npm audit --omit=dev` reports no high/critical for the two new packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md
@.planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md
@.planning/phases/05-mobile-sso-pro-cta/05-UI-SPEC.md
@.planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md
@app/package.json
@app/ios/VpnApp/Info.plist
@app/ios/VpnApp/VpnApp.entitlements
@app/ios/VpnApp/AppDelegate.swift
@app/ios/VpnApp.xcodeproj/project.pbxproj
@app/android/app/src/main/AndroidManifest.xml

<interfaces>
<!-- Existing iOS Bundle ID is the RN template placeholder (RESEARCH.md Open Q #1) -->

From app/ios/VpnApp.xcodeproj/project.pbxproj (lines 274 + 303):
```
PRODUCT_BUNDLE_IDENTIFIER = "org.reactjs.native.example.$(PRODUCT_NAME:rfc1034identifier)";
```

From app/ios/VpnApp/VpnApp.entitlements (current, missing Sign-in-with-Apple):
```xml
<key>com.apple.developer.networking.networkextension</key>
<array><string>packet-tunnel-provider</string></array>
<key>com.apple.security.application-groups</key>
<array><string>group.com.vpnapp.shared</string></array>
```

From app/android/app/src/main/AndroidManifest.xml (existing MainActivity has singleTask):
```xml
<activity android:name=".MainActivity" android:launchMode="singleTask" ...>
```

From app/android/app/build.gradle:
```
applicationId 'com.vpnapp'
versionCode 12
versionName "2.1.0"
```
(versionCode/versionName bumps land in Plan 04, NOT here.)

Pinned versions to install (T-6 supply-chain — exact, no caret):
- `@invertase/react-native-apple-authentication@2.5.1` (published 2026-03-31, verified in RESEARCH.md npm view)
- `@react-native-google-signin/google-signin@16.1.2` (published 2026-02-28, verified in RESEARCH.md npm view)
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: BLOCKING — Operator confirms all Wave-1 prereqs in 05-HUMAN-UAT.md Part 1</name>
  <files>.planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md (the document the operator must fill in)
    - .planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md (D-21 BLOCKING gate definition)
    - .planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md (§"Risks & Open Questions" item #1 — iOS Bundle ID)
  </read_first>
  <what-built>Wave 0 produced 05-HUMAN-UAT.md Part 1 — the BLOCKING checklist of operator prereqs. Wave 1 cannot proceed without these values.</what-built>
  <how-to-verify>
    Operator MUST provide values for ALL items in 05-HUMAN-UAT.md Part 1 "Required this phase":

    1. **Apple Bundle ID** (e.g., `com.vpnapp`) — confirmed registered in Apple Developer Portal with "Sign in with Apple" capability enabled.
    2. **Apple Service ID** — confirmed registered.
    3. **Google OAuth Web Client ID** — the audience backend validates (already exists from Phase 2 landing).
    4. **Google OAuth iOS Client ID** — new this phase, tied to the Apple Bundle ID.
    5. **Google OAuth Android Client ID** — new this phase, tied to `com.vpnapp` + debug keystore SHA-1.
    6. **Android debug keystore SHA-1** — extract via `keytool -list -v -keystore app/android/app/debug.keystore -alias androiddebugkey -storepass android -keypass android | grep SHA1`.
    7. **Server MIN_APP_VERSION** — confirm operator has scheduled bump to `2.2.0` simultaneous with mobile release.

    Operator should reply with each value (or "confirmed" + the value, e.g., "Apple Bundle ID: com.vpnapp"). The values get pasted into 05-HUMAN-UAT.md Part 1 and consumed by subsequent tasks.

    If ANY required item cannot be provided, ABORT this plan. Wave 1 cannot proceed.
  </how-to-verify>
  <resume-signal>
    Reply with the 6 values OR reply "ABORT — missing: <list>" if any required item cannot be obtained. After reply, the executor pastes the values into 05-HUMAN-UAT.md Part 1 and proceeds.
  </resume-signal>
  <acceptance_criteria>
    - User responds with concrete values for all 6 required items
    - 05-HUMAN-UAT.md Part 1 has every "Required this phase" checkbox filled with operator-provided values (no placeholder text remaining)
    - `grep -E "^\\s*-\\s*\\[\\s*\\]" .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md | head -10` shows the "Required this phase" boxes converted to `- [x]`
  </acceptance_criteria>
  <action>This is a checkpoint:human-action — pause for operator input. Display the 7-item BLOCKING checklist from `<how-to-verify>` to the operator. When the operator replies with values (or ABORT), paste each value into the corresponding `____________________` placeholder in `.planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md` Part 1 and convert `- [ ]` to `- [x]` for each filled item. Then proceed to Task 2. If the operator replies "ABORT — missing: ...", halt the plan and report which item blocks Wave 1.</action>
  <verify>
    <automated>grep -E "^\s*-\s*\[x\]" .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md | head -6 | wc -l | grep -qE "^[ ]*[6-9]|[1-9][0-9]"</automated>
  </verify>
  <done>Operator-provided values pasted into 05-HUMAN-UAT.md Part 1; every "Required this phase" checkbox is `- [x]`; Wave 1 unblocked.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Install both SSO packages with pinned versions + run pod install + verify audit clean</name>
  <files>app/package.json, app/package-lock.json, app/ios/Podfile.lock</files>
  <read_first>
    - app/package.json (verify neither package is currently in dependencies; check existing react-native version 0.84.1 + react 19.2.x)
    - app/ios/Podfile (verify `use_react_native!` autolink is on so the two new pods get picked up after npm install)
    - .planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md (§"Standard Stack" — exact versions 2.5.1 + 16.1.2 + `--save-exact` requirement per T-6)
    - .planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md (D-22 T-6: pin exact, commit lockfile, audit clean)
  </read_first>
  <action>
    From `app/` directory, run:

    ```bash
    cd app
    npm install --save-exact \
      @invertase/react-native-apple-authentication@2.5.1 \
      @react-native-google-signin/google-signin@16.1.2
    ```

    Verify `app/package.json` `dependencies` block now contains:
    - `"@invertase/react-native-apple-authentication": "2.5.1"` (NO leading `^` or `~`)
    - `"@react-native-google-signin/google-signin": "16.1.2"` (NO leading `^` or `~`)

    Then run pod install for iOS auto-linking:

    ```bash
    cd app/ios
    pod install
    cd ..
    ```

    Verify the post-install output names the two new pods:
    - `RNAppleAuthentication` (auto-linked from `@invertase/react-native-apple-authentication`)
    - `GoogleSignIn` (auto-linked from `@react-native-google-signin/google-signin`)

    Then run security audit:

    ```bash
    cd app
    npm audit --omit=dev --json > /tmp/audit-phase5.json
    ```

    Parse the JSON: confirm `vulnerabilities` block reports `high: 0` and `critical: 0` for both new packages. If any `high` or `critical` finding appears for either package, ABORT and document the CVE — do not proceed.

    Commit lockfile changes: `app/package-lock.json` AND `app/ios/Podfile.lock` MUST land in this task's commit. Both are required per T-6 supply-chain mitigation.

    DO NOT modify any other file. Native-side configuration (Info.plist, entitlements, AppDelegate, AndroidManifest, strings.xml) happens in Tasks 3-7 below.
  </action>
  <verify>
    <automated>cd app && grep -q '"@invertase/react-native-apple-authentication": "2.5.1"' package.json && grep -q '"@react-native-google-signin/google-signin": "16.1.2"' package.json && grep -vE '"@invertase/react-native-apple-authentication": "\^|~' package.json && test -f package-lock.json && test -f ios/Podfile.lock && grep -q "RNAppleAuthentication" ios/Podfile.lock && grep -q "GoogleSignIn" ios/Podfile.lock && (npm audit --omit=dev --audit-level=high 2>&1 | grep -qE "found 0 vulnerabilities|0 high|0 critical" || npm audit --omit=dev --audit-level=high --json | python3 -c "import sys,json; a=json.load(sys.stdin); v=a.get('metadata',{}).get('vulnerabilities',{}); sys.exit(0 if v.get('high',0)==0 and v.get('critical',0)==0 else 1)")</automated>
  </verify>
  <acceptance_criteria>
    - `cd app && grep '"@invertase/react-native-apple-authentication": "2.5.1"' package.json` exits 0 (EXACT version, no caret)
    - `cd app && grep '"@react-native-google-signin/google-signin": "16.1.2"' package.json` exits 0 (EXACT version, no caret)
    - `app/package-lock.json` modified (git diff shows new entries for both packages)
    - `app/ios/Podfile.lock` contains `RNAppleAuthentication` (grep matches)
    - `app/ios/Podfile.lock` contains `GoogleSignIn` (grep matches)
    - `cd app && npm audit --omit=dev --audit-level=high` exits 0 OR JSON output shows `high: 0, critical: 0`
  </acceptance_criteria>
  <done>Both packages installed at EXACT pinned versions; pod install completed with both pods linked; security audit clean of high/critical findings.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update iOS PRODUCT_BUNDLE_IDENTIFIER to operator-confirmed real Bundle ID</name>
  <files>app/ios/VpnApp.xcodeproj/project.pbxproj</files>
  <read_first>
    - app/ios/VpnApp.xcodeproj/project.pbxproj (lines 270-310 area — Debug + Release build config blocks)
    - .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md (the operator-confirmed Apple Bundle ID value)
    - .planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md (§"Risks & Open Questions" item #1)
    - .planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md (D-21 prerequisite)
  </read_first>
  <action>
    Read the Apple Bundle ID value the operator confirmed in 05-HUMAN-UAT.md Part 1 (likely `com.vpnapp` to match Android `applicationId`).

    In `app/ios/VpnApp.xcodeproj/project.pbxproj`, find BOTH occurrences of:
    ```
    PRODUCT_BUNDLE_IDENTIFIER = "org.reactjs.native.example.$(PRODUCT_NAME:rfc1034identifier)";
    ```
    (Lines 274 and 303 — one for Debug, one for Release.)

    Replace BOTH with the operator-confirmed value, e.g.:
    ```
    PRODUCT_BUNDLE_IDENTIFIER = com.vpnapp;
    ```

    DO NOT change `CURRENT_PROJECT_VERSION` or `MARKETING_VERSION` — those bump in Plan 04. Only the two `PRODUCT_BUNDLE_IDENTIFIER` lines change here.

    DO NOT touch the `VpnAppTests` or `VpnAppNetworkExtension` target's bundle IDs unless they're explicitly listed in 05-HUMAN-UAT.md — they typically inherit a `.tests` or `.NetworkExtension` suffix from the main bundle ID and that pattern should be preserved per Apple Developer Portal registration. If the test/extension targets currently use the placeholder, replace `.example.$(PRODUCT_NAME:rfc1034identifier)` with the same root (e.g., `com.vpnapp.tests`) keeping any existing `.tests`/`.extension` suffix.
  </action>
  <verify>
    <automated>cd app && ! grep -q "org.reactjs.native.example" ios/VpnApp.xcodeproj/project.pbxproj && grep -E "^\s*PRODUCT_BUNDLE_IDENTIFIER\s*=\s*[a-z0-9.]+;" ios/VpnApp.xcodeproj/project.pbxproj | wc -l | grep -qE "^[ ]*[2-9]"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "org.reactjs.native.example" app/ios/VpnApp.xcodeproj/project.pbxproj` exits 1 (placeholder fully removed)
    - `grep -E "^\s*PRODUCT_BUNDLE_IDENTIFIER\s*=" app/ios/VpnApp.xcodeproj/project.pbxproj` returns ≥2 lines (Debug + Release main target, plus any test/extension target IDs)
    - The new Bundle ID matches the value in 05-HUMAN-UAT.md Part 1 "Apple Bundle ID"
  </acceptance_criteria>
  <done>iOS Bundle ID is the operator-confirmed real value (e.g., `com.vpnapp`) for BOTH Debug and Release configurations.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Add Sign-in-with-Apple entitlement + CFBundleURLTypes to iOS Info.plist and entitlements file</name>
  <files>app/ios/VpnApp/Info.plist, app/ios/VpnApp/VpnApp.entitlements</files>
  <read_first>
    - app/ios/VpnApp/Info.plist (current — confirm CFBundleURLTypes does NOT yet exist)
    - app/ios/VpnApp/VpnApp.entitlements (current — confirm com.apple.developer.applesignin does NOT yet exist; the two existing keys are com.apple.developer.networking.networkextension and com.apple.security.application-groups)
    - .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md (operator-provided Google iOS OAuth Client ID — used in the reversed-client-id below)
    - .planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md (§"Code Examples" iOS Info.plist + entitlements blocks)
    - .planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md (D-21 Apple capability registration)
  </read_first>
  <action>
    **Edit `app/ios/VpnApp/VpnApp.entitlements`** — add the Sign-in-with-Apple capability INSIDE the existing top-level `<dict>` (alongside the existing two keys):

    ```xml
    <key>com.apple.developer.applesignin</key>
    <array>
      <string>Default</string>
    </array>
    ```

    Final file should look like:

    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
    <plist version="1.0">
    <dict>
      <key>com.apple.developer.applesignin</key>
      <array>
        <string>Default</string>
      </array>
      <key>com.apple.developer.networking.networkextension</key>
      <array><string>packet-tunnel-provider</string></array>
      <key>com.apple.security.application-groups</key>
      <array><string>group.com.vpnapp.shared</string></array>
    </dict>
    </plist>
    ```

    **Edit `app/ios/VpnApp/Info.plist`** — add `CFBundleURLTypes` as a new top-level key INSIDE the root `<dict>` (place it after the existing CFBundleName or near the bottom before `</dict>` — must be a top-level key of the root dict). Replace `<IOS_OAUTH_CLIENT_ID>` with the operator-confirmed Google iOS Client ID from 05-HUMAN-UAT.md (the value WITHOUT the `.apps.googleusercontent.com` suffix, e.g., if the full client ID is `12345-abc.apps.googleusercontent.com`, use `12345-abc` here — the full reversed-client-id form is `com.googleusercontent.apps.12345-abc`):

    ```xml
    <key>CFBundleURLTypes</key>
    <array>
      <!-- Custom scheme for payment-return deep link -->
      <dict>
        <key>CFBundleURLName</key>
        <string>com.vpnapp.payment</string>
        <key>CFBundleURLSchemes</key>
        <array>
          <string>vpnapp</string>
        </array>
      </dict>
      <!-- Google Sign-In reversed iOS OAuth client ID -->
      <dict>
        <key>CFBundleURLName</key>
        <string>com.googleusercontent.apps.<IOS_OAUTH_CLIENT_ID></string>
        <key>CFBundleURLSchemes</key>
        <array>
          <string>com.googleusercontent.apps.<IOS_OAUTH_CLIENT_ID></string>
        </array>
      </dict>
    </array>
    ```

    Critical: the `<IOS_OAUTH_CLIENT_ID>` placeholder MUST be replaced with the literal operator-provided iOS Client ID (without the `.apps.googleusercontent.com` suffix) in BOTH the CFBundleURLName AND CFBundleURLSchemes string values. Failure to replace this placeholder will cause Google Sign-In to fail silently on iOS.

    Validate Info.plist syntax: `plutil -lint app/ios/VpnApp/Info.plist` exits 0. Validate entitlements: `plutil -lint app/ios/VpnApp/VpnApp.entitlements` exits 0.
  </action>
  <verify>
    <automated>cd app && plutil -lint ios/VpnApp/Info.plist 2>&1 | grep -q "OK" && plutil -lint ios/VpnApp/VpnApp.entitlements 2>&1 | grep -q "OK" && grep -q "com.apple.developer.applesignin" ios/VpnApp/VpnApp.entitlements && grep -q "CFBundleURLTypes" ios/VpnApp/Info.plist && grep -q "vpnapp" ios/VpnApp/Info.plist && grep -q "com.googleusercontent.apps" ios/VpnApp/Info.plist && ! grep -q "<IOS_OAUTH_CLIENT_ID>" ios/VpnApp/Info.plist</automated>
  </verify>
  <acceptance_criteria>
    - `plutil -lint app/ios/VpnApp/Info.plist` exits 0
    - `plutil -lint app/ios/VpnApp/VpnApp.entitlements` exits 0
    - `grep "com.apple.developer.applesignin" app/ios/VpnApp/VpnApp.entitlements` exits 0
    - `grep "<string>vpnapp</string>" app/ios/VpnApp/Info.plist` exits 0
    - `grep "com.googleusercontent.apps" app/ios/VpnApp/Info.plist` exits 0
    - `grep "<IOS_OAUTH_CLIENT_ID>" app/ios/VpnApp/Info.plist` exits 1 (placeholder fully replaced)
    - Entitlements still preserves the existing `com.apple.developer.networking.networkextension` and `com.apple.security.application-groups` keys
  </acceptance_criteria>
  <done>Info.plist registers vpnapp:// and Google reversed-client-id schemes; entitlements file adds Sign-in-with-Apple alongside existing entries.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Add application(_:open:options:) URL handler to AppDelegate.swift</name>
  <files>app/ios/VpnApp/AppDelegate.swift</files>
  <read_first>
    - app/ios/VpnApp/AppDelegate.swift (existing — verify `application(_:open:options:)` does NOT yet exist; verify `import` block at top)
    - .planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md (§"Code Examples" iOS AppDelegate.swift URL handler block)
  </read_first>
  <action>
    Open `app/ios/VpnApp/AppDelegate.swift`. Add `import GoogleSignIn` to the imports block at the top of the file (if not already present). The existing imports likely already include `React_RCTAppDelegate` — preserve those.

    Inside the AppDelegate class, AFTER the existing `didFinishLaunchingWithOptions` method (or wherever the class has its other delegate methods), add this method:

    ```swift
    override func application(
      _ app: UIApplication,
      open url: URL,
      options: [UIApplication.OpenURLOptionsKey: Any] = [:]
    ) -> Bool {
      // First give Google Sign-In a chance to handle its callback URL scheme
      if GIDSignIn.sharedInstance.handle(url) {
        return true
      }
      // Otherwise forward to React Native's Linking module (catches vpnapp://)
      return RCTLinkingManager.application(app, open: url, options: options)
    }
    ```

    If `RCTLinkingManager` is not yet imported, also add `#import <React/RCTLinkingManager.h>` to the bridging header (typically `app/ios/VpnApp/VpnApp-Bridging-Header.h`). If the bridging header doesn't exist, use Swift-import: at top of AppDelegate.swift add `import React`.

    After editing, verify Xcode can parse the file: `xcrun swift-frontend -parse app/ios/VpnApp/AppDelegate.swift -sdk $(xcrun --sdk iphoneos --show-sdk-path) -target arm64-apple-ios14.0 2>&1 | tail -5` — this is best-effort (full Xcode required to actually compile); fall back to `grep` checks below if swift-frontend isn't available.

    If `override` keyword is rejected by the compiler because the method already exists in a different form (e.g., the project uses a non-React_RCTAppDelegate base), drop `override` and ensure the method is declared with the same signature.
  </action>
  <verify>
    <automated>cd app && grep -q "func application" ios/VpnApp/AppDelegate.swift && grep -q "GIDSignIn.sharedInstance.handle(url)" ios/VpnApp/AppDelegate.swift && grep -q "RCTLinkingManager.application" ios/VpnApp/AppDelegate.swift && grep -q "import GoogleSignIn" ios/VpnApp/AppDelegate.swift</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import GoogleSignIn" app/ios/VpnApp/AppDelegate.swift` exits 0
    - `grep "func application" app/ios/VpnApp/AppDelegate.swift` exits 0
    - `grep "GIDSignIn.sharedInstance.handle(url)" app/ios/VpnApp/AppDelegate.swift` exits 0 (Google handler called first)
    - `grep "RCTLinkingManager.application(app, open: url, options: options)" app/ios/VpnApp/AppDelegate.swift` exits 0 (RN handler called for non-Google URLs)
  </acceptance_criteria>
  <done>AppDelegate.swift forwards URL opens to GIDSignIn first, then to RCTLinkingManager — both Google Sign-In callback and vpnapp:// deep links reach their handlers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Add Android deep-link intent-filter to MainActivity and Google Web Client ID string resource</name>
  <files>app/android/app/src/main/AndroidManifest.xml, app/android/app/src/main/res/values/strings.xml</files>
  <read_first>
    - app/android/app/src/main/AndroidManifest.xml (existing — confirm MainActivity has `android:launchMode="singleTask"` on line 26 per code_context inspection; confirm NO existing intent-filter for vpnapp; identify the existing MAIN/LAUNCHER intent-filter on MainActivity which must stay untouched)
    - app/android/app/src/main/res/values/strings.xml (create if missing; if exists, append; verify it has the standard `<resources>` root)
    - .planning/phases/05-mobile-sso-pro-cta/05-HUMAN-UAT.md (operator-provided Google OAuth Web Client ID — the value pasted into server_client_id)
    - .planning/phases/05-mobile-sso-pro-cta/05-RESEARCH.md (§"Code Examples" Android sections — intent-filter with BROWSABLE category, strings.xml resource)
    - .planning/phases/05-mobile-sso-pro-cta/05-CONTEXT.md (Pitfall 3: intent-filter MUST include BROWSABLE category)
  </read_first>
  <action>
    **Edit `app/android/app/src/main/AndroidManifest.xml`** — inside the existing `<activity android:name=".MainActivity" ...>` element, AFTER the existing MAIN/LAUNCHER intent-filter (do NOT replace it), add a new intent-filter:

    ```xml
    <intent-filter android:autoVerify="false">
      <action android:name="android.intent.action.VIEW" />
      <category android:name="android.intent.category.DEFAULT" />
      <category android:name="android.intent.category.BROWSABLE" />
      <data android:scheme="vpnapp" android:host="payment" />
    </intent-filter>
    ```

    CRITICAL: the `BROWSABLE` category is REQUIRED. Without it, Chrome on Android refuses to navigate to `vpnapp://` from a top-level link tap (Pitfall 3 in RESEARCH.md). The `android:host="payment"` restricts the filter to URLs of the form `vpnapp://payment/...` (matches the locked URL pattern `vpnapp://payment/success?invoiceId=X`).

    The existing `android:launchMode="singleTask"` on MainActivity (line 26) is REQUIRED for `onNewIntent` to be delivered to the same instance when a warm app receives the deep link — leave it intact.

    **Create or edit `app/android/app/src/main/res/values/strings.xml`** — add a `<string name="server_client_id">...</string>` resource set to the operator-confirmed Google Web OAuth Client ID from 05-HUMAN-UAT.md:

    ```xml
    <?xml version="1.0" encoding="utf-8"?>
    <resources>
      <string name="app_name">Rise VPN</string>
      <!-- Google Sign-In Web OAuth Client ID. Passed to GoogleSignin.configure({webClientId})
           in JS at app boot; this string resource exists for native consumers that may
           emerge later. The value is the same Web Client ID Phase 2 issued for the landing
           site (audience the backend validates). -->
      <string name="server_client_id">YOUR_WEB_OAUTH_CLIENT_ID.apps.googleusercontent.com</string>
    </resources>
    ```

    Replace `YOUR_WEB_OAUTH_CLIENT_ID.apps.googleusercontent.com` with the operator-confirmed value from 05-HUMAN-UAT.md.

    If `strings.xml` already exists, preserve every existing `<string>` entry and only append the new `server_client_id` entry inside the existing `<resources>` block.

    Validate XML well-formedness: `xmllint --noout app/android/app/src/main/AndroidManifest.xml` and `xmllint --noout app/android/app/src/main/res/values/strings.xml` both exit 0.
  </action>
  <verify>
    <automated>cd app && xmllint --noout android/app/src/main/AndroidManifest.xml && xmllint --noout android/app/src/main/res/values/strings.xml && grep -q 'android:scheme="vpnapp"' android/app/src/main/AndroidManifest.xml && grep -q 'android:host="payment"' android/app/src/main/AndroidManifest.xml && grep -q "android.intent.category.BROWSABLE" android/app/src/main/AndroidManifest.xml && grep -q "server_client_id" android/app/src/main/res/values/strings.xml && grep -q 'android:launchMode="singleTask"' android/app/src/main/AndroidManifest.xml && ! grep -q "YOUR_WEB_OAUTH_CLIENT_ID" android/app/src/main/res/values/strings.xml</automated>
  </verify>
  <acceptance_criteria>
    - `xmllint --noout app/android/app/src/main/AndroidManifest.xml` exits 0 (well-formed XML)
    - `xmllint --noout app/android/app/src/main/res/values/strings.xml` exits 0
    - `grep 'android:scheme="vpnapp"' app/android/app/src/main/AndroidManifest.xml` exits 0
    - `grep 'android:host="payment"' app/android/app/src/main/AndroidManifest.xml` exits 0
    - `grep "android.intent.category.BROWSABLE" app/android/app/src/main/AndroidManifest.xml` exits 0
    - `grep 'android:launchMode="singleTask"' app/android/app/src/main/AndroidManifest.xml` STILL exits 0 (preserved)
    - `grep "server_client_id" app/android/app/src/main/res/values/strings.xml` exits 0
    - `grep "YOUR_WEB_OAUTH_CLIENT_ID" app/android/app/src/main/res/values/strings.xml` exits 1 (placeholder fully replaced)
    - The existing MAIN/LAUNCHER intent-filter on MainActivity is preserved (a `grep "android.intent.action.MAIN"` returns ≥1)
  </acceptance_criteria>
  <done>Android intent-filter for `vpnapp://payment/*` is registered with BROWSABLE; strings.xml has the Google Web Client ID resource; MainActivity's existing launcher behavior is preserved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → planning-artifacts | Operator-provided Apple Bundle ID, Google OAuth client IDs, SHA-1 are pasted into 05-HUMAN-UAT.md and consumed by Tasks 3-6 |
| npm registry → app/node_modules | New SSO packages downloaded from registry; pinned versions + lockfile + audit are the integrity gate |
| OS URL routing → app | Custom scheme `vpnapp://` is owned by the OS; another installed app could claim it (T-8 from RESEARCH.md §Threat Model) |
| Web → app | Browser tab can fire `vpnapp://payment/success?invoiceId=X` — this is the deep-link spoofing surface (T-1 from CONTEXT.md D-22) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6 (CONTEXT.md D-22) | Tampering / Elevation | New npm packages `@invertase/react-native-apple-authentication` + `@react-native-google-signin/google-signin` | mitigate | Pin EXACT versions via `npm install --save-exact` (Task 2); commit `app/package-lock.json` AND `app/ios/Podfile.lock`; run `npm audit --omit=dev` and abort if any `high`/`critical` finding. Both libraries are named in ADR-007 §15 row 7 (approved list). |
| T-1 (Deep-link spoofing) | Spoofing | Android intent-filter + iOS CFBundleURLTypes for `vpnapp://` | mitigate (partial) | Native registration alone does NOT defend against spoofing — defense lives in Wave 3's polling layer which treats `invoiceId` from the URL as untrusted and verifies status via `GET /invoices/:id`. Wave 1 contribution: scope the Android intent-filter to `android:host="payment"` so only `vpnapp://payment/*` URLs (NOT arbitrary `vpnapp://attacker/...`) reach the app. |
| T-3 (Apple auth-code leakage) | Information disclosure | iOS entitlements / Sign-in-with-Apple capability | accept | Wave 1 only enables the capability; the `authorizationCode` is captured by Wave 2 services and forwarded to backend without persistence. No Wave 1 surface logs the code. |
| T-5 (Token storage on rooted device) | Spoofing / Elevation | (no new persistence surface in Wave 1) | not-applicable | Wave 1 adds no new persistence; AsyncStorage continues per ADR §12.6. |
| T-8 (URL-scheme collision) | Spoofing | Android `vpnapp://` registration | accept | Per RESEARCH.md §Threat Model, if another installed app claims `vpnapp` the OS shows a chooser. Risk is low for a niche scheme; mitigation (Universal Links) is deferred per CONTEXT.md `<deferred>`. |
| Bundle-ID placeholder leaving production | Elevation / Spoofing | iOS `PRODUCT_BUNDLE_IDENTIFIER` | mitigate | Task 3 replaces the RN template placeholder `org.reactjs.native.example.*` with the operator-confirmed real value. Without this, Sign-in-with-Apple cannot work AND the app could collide with any other RN-template app in TestFlight. |
| Wave-1 BLOCKING gate bypass | Bypass of explicit constraint | Task 1 checkpoint | mitigate | `autonomous: false` on the plan; Task 1 is `checkpoint:human-action gate="blocking"`. Execution halts until operator confirms all 6 required values. |
</threat_model>

<verification>
After all 6 tasks complete:

1. `cd app && grep '"@invertase/react-native-apple-authentication": "2.5.1"' package.json` exits 0
2. `cd app && grep '"@react-native-google-signin/google-signin": "16.1.2"' package.json` exits 0
3. `cd app && npm audit --omit=dev --audit-level=high` exits 0
4. `grep "org.reactjs.native.example" app/ios/VpnApp.xcodeproj/project.pbxproj` exits 1 (placeholder fully removed)
5. `plutil -lint app/ios/VpnApp/Info.plist` and `plutil -lint app/ios/VpnApp/VpnApp.entitlements` both exit 0
6. `grep "com.apple.developer.applesignin" app/ios/VpnApp/VpnApp.entitlements` exits 0
7. `grep "<string>vpnapp</string>" app/ios/VpnApp/Info.plist` exits 0
8. `grep "GIDSignIn.sharedInstance.handle(url)" app/ios/VpnApp/AppDelegate.swift` exits 0
9. `xmllint --noout app/android/app/src/main/AndroidManifest.xml` exits 0
10. `grep 'android:scheme="vpnapp"' app/android/app/src/main/AndroidManifest.xml && grep "BROWSABLE" app/android/app/src/main/AndroidManifest.xml` exit 0
11. `grep "server_client_id" app/android/app/src/main/res/values/strings.xml` exits 0
12. Both lockfiles (`app/package-lock.json` + `app/ios/Podfile.lock`) modified and committed
</verification>

<success_criteria>
Wave 1 is complete when:
- [ ] Task 1 BLOCKING checkpoint passed — operator confirmed all 6 prereqs
- [ ] Both SSO packages installed at EXACT pinned versions (2.5.1 + 16.1.2), `npm audit` clean
- [ ] iOS Bundle ID replaced with real value in both Debug + Release configs
- [ ] iOS Info.plist registers `vpnapp://` + Google reversed-client-id; entitlements adds Sign-in-with-Apple
- [ ] iOS AppDelegate forwards URLs to GIDSignIn + RCTLinkingManager
- [ ] Android AndroidManifest has BROWSABLE intent-filter for `vpnapp://payment/*` on MainActivity
- [ ] Android strings.xml has `server_client_id` resource set to real Web Client ID
- [ ] Native side compiles cleanly (`pod install` succeeded, XML lints pass)
</success_criteria>

<output>
After completion, create `.planning/phases/05-mobile-sso-pro-cta/05-01-SUMMARY.md` capturing:
- Operator-confirmed prereq values (excluding any that are sensitive — record where they live in 05-HUMAN-UAT.md instead)
- Installed package versions + lockfile diff summary
- `npm audit --omit=dev` clean output
- Confirmation that Wave 2 can now run with the native infrastructure in place
</output>
