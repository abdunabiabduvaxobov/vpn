---
phase: 08-cleanup-hardening
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - server/api/internal/handler/servers_vless_test.go
  - server/api/internal/handler/auth_refresh_device_test.go
  - server/api/internal/handler/auth_opaque_refresh_test.go
  - server/api/internal/handler/security_headers_test.go
  - server/api/internal/handler/health_test.go
  - server/api/internal/handler/admin_search_test.go
  - server/api/internal/handler/admin_audit_diff_test.go
  - server/api/internal/handler/debug_error_limit_test.go
  - server/api/internal/handler/servers_order_test.go
  - server/api/internal/handler/bcrypt_cost_test.go
  - server/api/internal/middleware/ratelimit_failclosed_test.go
  - server/api/internal/bot/recovery_private_test.go
  - server/api/internal/logger/logger_redact_test.go
  - server/tunnel/internal/server_reload_test.go
  - app/src/stores/vpnStore.test.ts
  - test/wire-vless/README.md
  - test/wire-vless/docker-compose.yml
  - docs/manual-verification/08-keychain-asyncstorage.md
autonomous: true
requirements:
  - HARD-01
  - HARD-02
  - HARD-03
  - HARD-04
  - HARD-05
  - HARD-06
  - HARD-07
  - HARD-08
  - HARD-09
  - HARD-10
  - HARD-11
  - HARD-12
  - HARD-13
  - HARD-14
  - HARD-15
  - HARD-16
  - HARD-17

must_haves:
  truths:
    - "Every HARD-NN requirement has a RED (failing) or compiling-skip automated test that pins its target behavior before implementation"
    - "The wire-level VLESS rejection harness exists and can present an arbitrary UUID to a real xray tunnel"
    - "The zap-redaction observer test, device-bind refresh test, and waitForDisconnected jest test exist and fail RED"
    - "A manual-verification doc for SC#5 (Xcode Keychain / AsyncStorage absence) exists"
  artifacts:
    - path: "server/api/internal/logger/logger_redact_test.go"
      provides: "RED test for HARD-10 zap redaction via zaptest/observer"
      contains: "observer"
    - path: "server/api/internal/handler/auth_refresh_device_test.go"
      provides: "RED test for HARD-04 device-bind 401"
      contains: "device"
    - path: "server/tunnel/internal/server_reload_test.go"
      provides: "RED test for HARD-02 tunnel ReloadClients"
      contains: "ReloadClients"
    - path: "app/src/stores/vpnStore.test.ts"
      provides: "RED test for HARD-15 waitForDisconnected"
      contains: "waitForDisconnected"
    - path: "test/wire-vless/docker-compose.yml"
      provides: "wire-level VLESS rejection harness for HARD-02 SC#2"
      contains: "xray"
    - path: "docs/manual-verification/08-keychain-asyncstorage.md"
      provides: "SC#5 manual procedure"
      contains: "Keychain"
  key_links:
    - from: "Wave 0 test files"
      to: "Wave 1-3 implementation plans"
      via: "RED tests that flip GREEN when each HARD-NN is implemented"
      pattern: "GREEN when HARD"
---

<objective>
Establish the Nyquist test infrastructure for Phase 8 listed in 08-VALIDATION.md "Wave 0 Requirements". Every later wave verifies against tests created here. This plan writes RED (failing or compiling-skip-pending) tests and harnesses ONLY — it does NOT implement any HARD-NN behavior. Implementation plans (Waves 1-3) flip these tests GREEN.

Purpose: Without Wave 0, later tasks would self-verify against tests they wrote in the same context window, defeating feedback sampling. RED-first pins the target behavior independently.
Output: 13 Go test files, 1 jest test, the wire-level VLESS docker harness, and the SC#5 manual-verification doc.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cleanup-hardening/08-VALIDATION.md
@.planning/phases/08-cleanup-hardening/08-RESEARCH.md

<interfaces>
<!-- Verified anchors (08-RESEARCH.md Anchor Verification). Tests target these. -->
From server/api/internal/handler/auth.go:
  - generateTokens(userID, tier, role, fullName, planID, jwtSecret) returns tokens with .RefreshToken (currently a JWT; HARD-03 makes it 43-char base64url)
  - RefreshToken handler at auth.go:227; hashes inbound token (sha256), FindSessionByTokenHash at auth.go:241
  - storeRefreshSession call sites: auth.go:107, 288, 414, 550 (all must thread device_id/issue_ip in HARD-04)
From server/api/internal/handler/servers.go:
  - ListServersCached (servers.go:122) — HARD-14 ordering layers here, per-request, NOT cached
  - GetServerConfig (servers.go:226), UserID: cfg.TunnelVLESSUUID at servers.go:293 — HARD-02 replaces with per-user UUID
From server/api/internal/repository/admin_repo.go:
  - ListUsers(ctx, db, page, limit, search) — ILIKE %x% at admin_repo.go:30 (HARD-06 target)
From server/api/internal/middleware/ratelimit.go:
  - LinkAttemptLimit (ratelimit.go:98), fail-open return c.Next() at 108 (HARD-12 -> 503)
From server/api/internal/bot/recovery.go:
  - handleUpdate (recovery.go:144), msg.IsCommand() at 162 (HARD-05 gate before this)
From server/api/internal/handler/health.go:
  - Health() returns "go_version": runtime.Version() at health.go:28 (HARD-17 removes)
From server/tunnel/internal/server.go:
  - buildXRayConfig() (server.go:185); Config.Clients []string (config.go:18); core.New().Start()/Close() (HARD-02 ReloadClients)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Go API RED tests for the tightly-specified items + refresh/device/vless tests</name>
  <read_first>
    - .planning/phases/08-cleanup-hardening/08-VALIDATION.md (Per-Task Verification Map — exact assertions)
    - .planning/phases/08-cleanup-hardening/08-RESEARCH.md sections 3 and 4 (verified anchors + assertion shapes)
    - server/api/internal/handler/auth.go (generateTokens, RefreshToken, storeRefreshSession)
    - server/api/internal/handler/health.go (Health)
    - server/api/internal/repository/admin_repo.go (ListUsers ILIKE)
    - server/api/internal/middleware/ratelimit.go (LinkAttemptLimit)
    - server/api/internal/handler/admin.go (AdminUpdateUser, AdminListUsers)
    - server/api/internal/middleware/audit.go (AuditLog, model.AuditDetails)
    - server/api/internal/handler/migrations_test.go (existing testcontainers/miniredis harness to mirror)
  </read_first>
  <action>
Create the following Go RED tests. Each MUST compile and FAIL, or compile and `t.Skip("GREEN when HARD-NN ... lands")`. Never a silent PASS. Mirror the testcontainers-postgres + miniredis harness used in migrations_test.go / webhook_lava_test.go.

1. `auth_refresh_device_test.go` (HARD-04, SC#4): issue a session bound to device_id="A"; call RefreshToken with device_id="B" -> assert HTTP 401; with device_id="A" -> assert 200; an IP mismatch is allowed (no 401). Binding columns do not exist yet -> use a compiling `t.Skip("GREEN when HARD-04 migration 025 + RefreshToken binding land")`.

2. `auth_opaque_refresh_test.go` (HARD-03): call generateTokens, assert the refresh field matches `^[A-Za-z0-9_-]{43}$` and contains NO `.`. Expect RED now (current token is a dot-separated JWT).

3. `health_test.go` (HARD-17): httptest GET /health, decode JSON, assert no `go_version` key. RED now.

4. `admin_search_test.go` (HARD-06): AdminListUsers with search="ab" (len<3) returns 400; the SQL generated by ListUsers for a valid search has no leading `%` (capture via gorm DryRun session or SQL logger). RED now.

5. `admin_audit_diff_test.go` (HARD-07): change a user's role via AdminUpdateUser, query latest audit row, assert details JSONB contains `role` with `before`/`after` keys. RED now.

6. `ratelimit_failclosed_test.go` (HARD-12, in internal/middleware): build LinkAttemptLimit with a miniredis then `.Close()` it (outage); assert 503 (not 200/Next). RED now.

7. `debug_error_limit_test.go` (HARD-13): fire 6 rapid requests to /debug/error from one IP; assert the 6th returns 429. RED now.

8. `servers_order_test.go` (HARD-14): order a server list for userA twice -> identical; for userB -> different; both are permutations of the same set (no drops/adds). RED now.

9. `recovery_private_test.go` (HARD-05, in internal/bot): feed handleUpdate an Update with Message.Chat.Type="group"; assert no reply is sent (spy/fake the reply sink). RED now.

10. `bcrypt_cost_test.go` (HARD-11): generate a hash via the production admin-password path; assert `bcrypt.Cost(hash) == 12`. RED now (DefaultCost=10).

11. `servers_vless_test.go` (HARD-02 API side): two users same plan -> `/servers/:id/config` returns DIFFERENT UserID UUIDs; after a plan change the user's UUID rotates and the prior identity row is is_active=false; `GET /internal/servers/:id/vless-clients` returns the active set. Use `t.Skip("GREEN when migration 026 + per-user UUID + active-set endpoint land")` if it cannot compile.

12. `security_headers_test.go` (HARD-08): httptest a request through the admin route group; assert response carries `Strict-Transport-Security`, `X-Content-Type-Options: nosniff`, `Content-Security-Policy`. Use compiling skip if the headers middleware does not exist yet, else RED.

For tests that cannot compile against current code, use a compiling `t.Skip("GREEN when HARD-NN ...")` — keep `go build ./...` green.
  </action>
  <verify>
    <automated>cd server/api && go build ./... && go test ./internal/handler/... ./internal/middleware/... ./internal/bot/... -run 'Refresh|Health|AdminSearch|AuditDiff|FailClosed|DebugError|ServerOrder|RecoveryPrivate|BcryptCost|Vless|SecurityHeaders|Opaque' 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd server/api && go build ./...` exits 0 (suite compiles; skips allowed)
    - Each new `_test.go` file exists and contains at least one `func Test...`
    - Running the new tests shows FAIL or SKIP, never silent PASS
    - `grep -rl "GREEN when HARD" server/api/internal | wc -l` returns >= 4 (the skip-gated files)
    - No new test imports `stripe-go`
  </acceptance_criteria>
  <done>12 Go RED/skip tests exist, the api module compiles, and each new test fails or skips (none falsely passes).</done>
</task>

<task type="auto">
  <name>Task 2: Logger-redaction RED test, tunnel-reload RED test, jest waitForDisconnected RED test</name>
  <read_first>
    - server/api/cmd/main.go (logger via zap.NewProduction() at main.go:61)
    - .planning/phases/08-cleanup-hardening/08-RESEARCH.md section 4.6 (zaptest/observer + regexes)
    - server/tunnel/internal/server.go (buildXRayConfig, core.New/Start/Close)
    - server/tunnel/internal/config.go (Config.Clients)
    - app/src/stores/vpnStore.ts (busy-wait at vpnStore.ts:78-93)
    - app/jest.config.js (existing jest setup)
  </read_first>
  <action>
1. `server/api/internal/logger/logger_redact_test.go` (HARD-10, SC#6): create dir `server/api/internal/logger/` if absent. Test constructs a logger wrapped by the not-yet-existing `logger.NewRedactingLogger(...)` writing into a `go.uber.org/zap/zaptest/observer` in-memory core. Log `zap.String("token", "<header.payload.signature, each segment >=20 base64url chars>")` and `zap.String("x", "<a 43-char base64url string>")`. Assert observed entries contain `[REDACTED]` and NOT the original secrets. The test file declares the contract regexes: JWT `^[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}\.[A-Za-z0-9_-]{10,}$` and base64url-32 `^[A-Za-z0-9_-]{32,}$`. Use compiling `t.Skip("GREEN when logger.NewRedactingLogger lands (HARD-10)")` if the func is absent.

2. `server/tunnel/internal/server_reload_test.go` (HARD-02 tunnel side): assert a not-yet-existing `(*TunnelServer).ReloadClients(uuids []string) error` rebuilds config from the new UUID set and restarts the instance. Gate with `t.Skip("GREEN when ReloadClients lands (HARD-02)")` and document the intended signature in a comment. If a pure config-builder is exposable, add a buildable assertion that the generated config client list equals the input UUID set (RED/real now).

3. `app/src/stores/vpnStore.test.ts` (HARD-15): jest test for a not-yet-existing `waitForDisconnected(timeoutMs)`: resolves when store state goes `'disconnecting'` -> `'disconnected'`; resolves at the timeout cap when it never transitions (no infinite hang, no busy-wait). Import from where HARD-15 will export it; if absent, RED on import is acceptable (drives implementation).
  </action>
  <verify>
    <automated>cd server/api && go build ./internal/logger/... 2>&1 | tail -3; cd /Users/abdunabi/Desktop/vpn/server/tunnel && go build ./... && go test ./internal/... -run Reload 2>&1 | tail -5; cd /Users/abdunabi/Desktop/vpn/app && npx jest src/stores/vpnStore.test.ts 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/internal/logger/logger_redact_test.go` exists and references `observer` and `[REDACTED]`
    - `server/tunnel/internal/server_reload_test.go` exists and references `ReloadClients`
    - `app/src/stores/vpnStore.test.ts` exists and references `waitForDisconnected`
    - All three fail or skip RED; tunnel + api modules still `go build` clean
  </acceptance_criteria>
  <done>The three cross-surface RED tests exist, build cleanly (Go), and fail/skip pending implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Wire-level VLESS rejection harness + SC#5 manual-verification doc</name>
  <read_first>
    - server/tunnel/internal/server.go (REALITY + WS inbound config, Config.Clients)
    - server/tunnel/internal/config.go (config shape, ports, REALITY keys)
    - .planning/phases/08-cleanup-hardening/08-RESEARCH.md section 2.4 (wire rejection) and section 6.4 (SC#5 procedure)
    - .planning/phases/08-cleanup-hardening/08-VALIDATION.md (Manual-Only Verifications table)
  </read_first>
  <action>
1. `test/wire-vless/docker-compose.yml` + `test/wire-vless/README.md` (HARD-02 SC#2 wire harness): a docker-compose that runs the RiseVPN tunnel (xray) with a known active-UUID set, plus a scripted xray VLESS client container that can present an ARBITRARY UUID. README documents the procedure: (a) start the tunnel with active UUID set {U1}; (b) connect a client presenting U1 -> handshake SUCCEEDS; (c) connect a client presenting a foreign/revoked UUID U2 -> VLESS session is REJECTED (REALITY falls back to the impersonated dest; no tunnel established). This harness is run manually at the phase gate (it is the heaviest infra; it does not run in unit `go test`). Provide concrete commands (the xray client config JSON with the test UUID, the expected failure signature). Do NOT require it to pass yet — until HARD-02 lands the tunnel admits the shared UUID. Mark in README which step proves SC#2.

2. `docs/manual-verification/08-keychain-asyncstorage.md` (HARD-16 / SC#5): step-by-step manual procedure from 08-VALIDATION.md "Manual-Only Verifications": build app to iOS simulator; sign in; Keychain Access (or Xcode device container) shows the react-native-keychain service entry; inspect the app's RCTAsyncLocalStorage manifest and confirm NO `auth-tokens` key; Android: confirm EncryptedSharedPreferences xml exists and AsyncStorage has no token. Include the exact keychain service name to look for and the AsyncStorage manifest path.
  </action>
  <verify>
    <automated>test -f test/wire-vless/docker-compose.yml && test -f test/wire-vless/README.md && test -f docs/manual-verification/08-keychain-asyncstorage.md && grep -qi xray test/wire-vless/docker-compose.yml && grep -qi keychain docs/manual-verification/08-keychain-asyncstorage.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test/wire-vless/docker-compose.yml` exists and references `xray`
    - `test/wire-vless/README.md` documents the present-foreign-UUID -> rejection procedure with concrete commands
    - `docs/manual-verification/08-keychain-asyncstorage.md` exists, references `Keychain` and `AsyncStorage`, and lists the iOS + Android steps
  </acceptance_criteria>
  <done>The wire-VLESS harness scaffold and the SC#5 manual doc exist with concrete, runnable procedures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| test harness -> tunnel | The wire-VLESS harness presents untrusted/foreign UUIDs to the tunnel to prove rejection |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-00 | Tampering | test fixtures leaking into prod build | accept | Tests live under `_test.go` / `test/` and are excluded from the prod binary; the wire harness is a separate docker-compose not wired into deploy |
</threat_model>

<verification>
- `cd server/api && go build ./...` exits 0
- `cd server/tunnel && go build ./...` exits 0
- New Go tests fail or skip (never silent PASS); at least 4 carry a "GREEN when HARD" skip message
- jest test file present and RED
- Wire harness + manual doc present
</verification>

<success_criteria>
Every HARD-NN listed in 08-VALIDATION.md "Wave 0 Requirements" has a corresponding RED/skip test or harness, both Go modules compile, and the SC#5 manual doc exists.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cleanup-hardening/08-01-SUMMARY.md`.
</output>
