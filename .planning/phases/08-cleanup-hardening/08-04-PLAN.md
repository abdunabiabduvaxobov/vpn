---
phase: 08-cleanup-hardening
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-03"]
files_modified:
  - server/api/migrations/025_session_device_binding.sql
  - server/api/internal/model/user.go
  - server/api/internal/handler/auth.go
  - server/api/internal/repository/session_repo.go
autonomous: true
requirements:
  - HARD-03
  - HARD-04

must_haves:
  truths:
    - "Refresh tokens are 32-byte opaque URL-safe base64 strings (43 chars), not JWTs"
    - "A refresh presented with a different device_id than the one bound at issue is rejected with 401"
    - "An IP change at refresh is logged as a security event but the refresh is still allowed"
    - "At deploy, all pre-existing JWT-format refresh sessions are invalidated, forcing one re-login"
  artifacts:
    - path: "server/api/migrations/025_session_device_binding.sql"
      provides: "sessions.device_id + issue_ip columns + index + clean-break DELETE (HARD-04, D-09)"
      contains: "device_id"
    - path: "server/api/internal/handler/auth.go"
      provides: "opaque refresh mint in generateTokens + device/IP binding in RefreshToken (HARD-03, HARD-04)"
      contains: "base64.RawURLEncoding"
    - path: "server/api/internal/repository/session_repo.go"
      provides: "session create/lookup carrying device_id + issue_ip"
      contains: "DeviceID"
  key_links:
    - from: "generateTokens"
      to: "opaque crypto/rand 32-byte token"
      via: "replace refreshClaims JWT mint with base64.RawURLEncoding(rand 32)"
      pattern: "rand.Read|RawURLEncoding"
    - from: "RefreshToken handler"
      to: "session.DeviceID comparison"
      via: "hard 401 on device_id mismatch; soft log on IP mismatch"
      pattern: "DeviceID"
---

<objective>
Replace JWT refresh tokens with opaque 32-byte random strings (HARD-03 / S1-2) and bind refresh sessions to device_id (hard) + issue-IP (soft) (HARD-04 / S1-7). At deploy, clear all sessions for a clean-break single re-login (D-09) — this re-login is coordinated with the mobile Keychain move (plan 08-09).

Purpose: Remove the forgeable-by-JWT_SECRET-holder refresh envelope and stop a stolen refresh token from being replayed on a different device.
Output: Migration 025, model + repo + handler changes, GREEN Wave 0 device-bind and opaque-refresh tests.

NOTE: This plan only changes the refresh MINT in generateTokens and the RefreshToken binding. It must NOT touch the bcrypt line at auth.go:201 (owned by plan 08-03, already merged in Wave 1) or generateTokens' access-token JWT (unchanged).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cleanup-hardening/08-RESEARCH.md
@.planning/phases/08-cleanup-hardening/08-CONTEXT.md

<interfaces>
From server/api/internal/recovery/start_token.go (REUSE this pattern — D-08):
  - crypto/rand + base64.RawURLEncoding opaque-token generation (24 bytes -> 32 chars; HARD-03 wants 32 bytes -> 43 chars)
From server/api/internal/handler/auth.go:
  - generateTokens at auth.go:601; refresh JWT mint (refreshClaims{sub,type:"refresh",iat,exp}) at auth.go:621-633 -> REPLACE with opaque string
  - RefreshToken handler at auth.go:227; sha256(req.RefreshToken) -> FindSessionByTokenHash at auth.go:241; rotation tx at auth.go:264-303
  - storeRefreshSession(ctx, db, userID, refreshToken) call sites: auth.go:107, 288, 414, 550 — must now also receive deviceID + issueIP
From server/api/internal/model/user.go:
  - model.Session{ID, UserID, RefreshTokenHash, DeviceInfo, CreatedAt, ExpiresAt} at user.go:60-67 — add DeviceID, IssueIP
From server/api/migrations/001_initial.sql:
  - sessions table at 001_initial.sql:18-25; refresh_token_hash is VARCHAR(64) SHA-256 hex (UNCHANGED by token-format change)
Migration numbering: highest existing is 024. This plan claims 025. (Plan 08-07 claims 026 for vless identities.)
Wave 0 tests to flip GREEN: auth_opaque_refresh_test.go (HARD-03), auth_refresh_device_test.go (HARD-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration 025 (device_id + issue_ip + clean-break) and model.Session fields</name>
  <read_first>
    - server/api/migrations/001_initial.sql (sessions table at 001_initial.sql:18-25)
    - server/api/migrations/024_admin_panel_overhaul.sql (latest migration — confirm 025 is free; mirror its DDL/format conventions)
    - server/api/internal/model/user.go (model.Session at user.go:60-67)
    - server/api/internal/handler/migrations_test.go (migration test harness)
    - .planning/phases/08-cleanup-hardening/08-RESEARCH.md section 3.3
  </read_first>
  <action>
Create `server/api/migrations/025_session_device_binding.sql`:
```sql
ALTER TABLE sessions
    ADD COLUMN IF NOT EXISTS device_id VARCHAR(255),  -- bound at issue; hard-checked on refresh (D-10)
    ADD COLUMN IF NOT EXISTS issue_ip  VARCHAR(45);   -- bound at issue; soft-checked, log-only (D-10, IPv6-safe)
CREATE INDEX IF NOT EXISTS idx_sessions_device_id ON sessions(device_id);

-- D-09 clean-break cutover: all existing JWT-format refresh sessions become invalid at deploy,
-- forcing exactly one re-login (coordinated with mobile Keychain move, plan 08-09).
DELETE FROM sessions;
```
Follow the migration runner conventions used by 024 (file naming, transaction wrapping if the runner expects it).

In `model.Session` (user.go:60-67), add `DeviceID string` (gorm column `device_id`) and `IssueIP string` (gorm column `issue_ip`).
  </action>
  <verify>
    <automated>cd server/api && go build ./... && go test ./internal/handler/... -run Migrations 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/migrations/025_session_device_binding.sql` exists with `device_id`, `issue_ip`, the index, and `DELETE FROM sessions`
    - `model.Session` has `DeviceID` and `IssueIP` fields with correct gorm tags
    - migrations test harness applies 025 cleanly (`go test -run Migrations` green)
  </acceptance_criteria>
  <done>Migration 025 adds binding columns + clean-break wipe; model.Session carries the new fields.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Opaque refresh mint (HARD-03) + device/IP binding on issue and refresh (HARD-04)</name>
  <read_first>
    - server/api/internal/recovery/start_token.go (opaque-token pattern to mirror — D-08)
    - server/api/internal/handler/auth.go (generateTokens at 601/621-633; RefreshToken at 227; storeRefreshSession + 4 call sites at 107/288/414/550)
    - server/api/internal/repository/session_repo.go (FindSessionByTokenHash, storeRefreshSession, session create)
    - server/api/internal/handler/auth_opaque_refresh_test.go (Wave 0 RED — HARD-03 contract)
    - server/api/internal/handler/auth_refresh_device_test.go (Wave 0 RED — HARD-04 contract)
    - .planning/phases/08-cleanup-hardening/08-RESEARCH.md sections 3.1, 3.2, 3.3
    - docs/audit/SECURITY-AUDIT.md (S1-2, S1-7)
  </read_first>
  <behavior>
    - generateTokens returns a refresh token matching `^[A-Za-z0-9_-]{43}$` with no `.` (opaque, not JWT)
    - A session issued with device_id="A": refresh with device_id="B" -> 401; refresh with device_id="A" -> 200 + new rotated token
    - Refresh with a different IP than issue -> allowed (200) but a security warn is logged
    - The rotated session row carries forward device_id (and preserves the original issue_ip)
  </behavior>
  <action>
HARD-03 (S1-2): in `generateTokens` (auth.go), replace the refreshClaims/jwt.NewWithClaims block (auth.go:621-633) that mints a JWT refresh token with an opaque 32-byte random string (mirror recovery/start_token.go):
```go
raw := make([]byte, 32)
if _, err := rand.Read(raw); err != nil {
    return nil, fmt.Errorf("generating refresh token: %w", err)
}
refreshString := base64.RawURLEncoding.EncodeToString(raw) // 43 chars, opaque
```
Leave the ACCESS-token JWT mint untouched. `storeRefreshSession` already SHA-256-hashes whatever string it receives — the hash column (VARCHAR(64)) is unaffected.

HARD-04 (S1-7): thread `deviceID, issueIP` through session creation:
- Extend `storeRefreshSession` (session_repo.go) to accept `deviceID, issueIP string` and persist them on the new row. Update ALL 4 call sites (auth.go:107, 288, 414, 550): `device_id` comes from the request body / device fingerprint; `issueIP` from `c.IP()`.
- In `RefreshToken` (auth.go:227): parse `device_id` from the refresh request body. After `FindSessionByTokenHash`:
  - `if session.DeviceID != "" && session.DeviceID != req.DeviceID { return 401 }` — hard reject (D-10). Plain string compare (device_id is an OS identifier, not a secret).
  - `if session.IssueIP != c.IP() { logger.Warn("refresh ip mismatch", zap.String("user_id", ...), zap.String("issue_ip", session.IssueIP), zap.String("current_ip", c.IP())) }` then CONTINUE (D-10 soft — mobile roams).
- In the rotation tx (auth.go:264-303), the new session row must carry forward `device_id` and the ORIGINAL `issue_ip` (preserve the anomaly baseline; document the choice).

Because migration 025 wipes sessions, no NULL-device legacy rows survive — every post-cutover session has device_id populated.
  </action>
  <verify>
    <automated>cd server/api && go test ./internal/handler/... -run 'Opaque|Refresh|Device' 2>&1 | tail -15 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "base64.RawURLEncoding" server/api/internal/handler/auth.go` present in generateTokens; `grep -n "refreshClaims\|type.*refresh.*jwt" server/api/internal/handler/auth.go` returns 0 (JWT refresh mint gone)
    - Refresh field matches `^[A-Za-z0-9_-]{43}$` (Wave 0 auth_opaque_refresh_test GREEN)
    - device_id mismatch -> 401; same device -> 200; IP mismatch -> 200 + warn (Wave 0 auth_refresh_device_test GREEN)
    - All 4 storeRefreshSession call sites pass deviceID + issueIP; rotation preserves them
    - The ACCESS-token JWT and the auth.go:201 bcrypt line are unchanged
  </acceptance_criteria>
  <done>Refresh tokens are opaque; refresh is device-bound (hard) + IP-aware (soft); both Wave 0 tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client -> /auth/refresh | untrusted refresh token + device_id cross into session lookup |
| JWT_SECRET holder | anyone with the signing key could previously forge a refresh JWT |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Spoofing / Tampering | auth.go generateTokens (audit S1-2) | mitigate | Refresh token is opaque crypto/rand base64url — no signature to forge; JWT_SECRET no longer mints valid refresh tokens (HARD-03) |
| T-08-04 | Spoofing (replay) | auth.go RefreshToken (audit S1-7) | mitigate | Session bound to device_id at issue; hard 401 on mismatch blocks stolen-token replay on a new device (HARD-04) |
| T-08-04b | Repudiation / detection gap | refresh IP change | accept (log) | IP mismatch logged as a security event but allowed — mobile roaming makes IP-reject cause false logouts (D-10); anomaly is recorded for detection |
| T-08-03b | Session fixation | leftover JWT sessions post-cutover | mitigate | Migration 025 `DELETE FROM sessions` forces a clean-break single re-login (D-09) |
</threat_model>

<verification>
- `go test ./internal/handler/... -run 'Opaque|Refresh|Device'` GREEN
- `go test ./internal/handler/... -run Migrations` GREEN
- `grep -n base64.RawURLEncoding server/api/internal/handler/auth.go` present; no refresh JWT mint remains
- `go build ./...` exits 0
</verification>

<success_criteria>
HARD-03 + HARD-04 implemented; opaque-refresh and device-bind Wave 0 tests pass; sessions cleared at deploy for a single coordinated re-login.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cleanup-hardening/08-04-SUMMARY.md`.
</output>
