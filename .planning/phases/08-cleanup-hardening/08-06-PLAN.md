---
phase: 08-cleanup-hardening
plan: 06
type: execute
wave: 1
depends_on: ["08-01"]
files_modified:
  - app/src/hooks/useVpnConnection.ts
  - app/src/hooks/useVpnLifecycle.ts
  - app/src/hooks/useProtocolFallback.ts
  - app/src/hooks/useConnectionStats.ts
  - app/src/stores/vpnStore.ts
autonomous: true
requirements:
  - HARD-15

must_haves:
  truths:
    - "vpnStore.connect no longer busy-waits (no 100ms setTimeout polling loop); it awaits an event-driven waitForDisconnected"
    - "useVpnConnection is decomposed into smaller cohesive hooks with unchanged call-site behavior"
    - "The connect/disconnect/protocol-fallback flow is behavior-preserving (no functional change)"
  artifacts:
    - path: "app/src/stores/vpnStore.ts"
      provides: "event-driven waitForDisconnected replacing the busy-wait (HARD-15)"
      contains: "waitForDisconnected"
    - path: "app/src/hooks/useVpnConnection.ts"
      provides: "thin composition over extracted sub-hooks (HARD-15)"
      contains: "useVpn"
  key_links:
    - from: "vpnStore.connect"
      to: "waitForDisconnected"
      via: "store.subscribe one-shot promise resolving on state change"
      pattern: "subscribe"
---

<objective>
Refactor the 591-line `useVpnConnection` hook into smaller cohesive hooks and replace the `vpnStore.connect` busy-wait (100ms poll for up to 3s) with an event-driven wait (HARD-15 / CODE-REVIEW APP-M-04, APP-H-04, APP-H-03). This is a BEHAVIOR-PRESERVING refactor — no functional change to the connect flow; the only observable improvement is removing the busy-wait. Independent of the backend cutover; runs in parallel.

Purpose: Reduce a 591-line hook's complexity and eliminate a CPU-burning poll loop in the connect path.
Output: Decomposed hooks, an event-driven `waitForDisconnected`, and a GREEN jest test from Wave 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cleanup-hardening/08-RESEARCH.md
@docs/audit/CODE-REVIEW.md

<interfaces>
From app/src/stores/vpnStore.ts:
  - connect() busy-wait at vpnStore.ts:78-93: polls get().connectionState === 'disconnecting' every 100ms up to 3s via `await new Promise(r => setTimeout(r, 100))`
  - connectionState states include 'disconnecting' and 'disconnected'
  - zustand store exposes .getState() and .subscribe(listener)
From app/src/hooks/useVpnConnection.ts (591 lines):
  - tryNextProtocol at useVpnConnection.ts:161-225 (APP-H-03: direct setState bypassing disconnect cleanup)
  - ~10 effects covering lifecycle, protocol fallback, stats/timers, reconnect
From app/src/stores/vpnStore.test.ts (Wave 0 RED test): contract for waitForDisconnected(timeoutMs)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Event-driven waitForDisconnected replacing the busy-wait</name>
  <read_first>
    - app/src/stores/vpnStore.ts (connect busy-wait at vpnStore.ts:78-93; store shape)
    - app/src/stores/vpnStore.test.ts (Wave 0 RED test — the contract)
    - .planning/phases/08-cleanup-hardening/08-RESEARCH.md section 5 (waitForDisconnected snippet)
    - docs/audit/CODE-REVIEW.md (APP-H-04)
  </read_first>
  <action>
Add an event-driven `waitForDisconnected(timeoutMs = 3000): Promise<void>` (export it from vpnStore.ts so the Wave 0 test can import it). Implementation per research §5:
```ts
export function waitForDisconnected(timeoutMs = 3000): Promise<void> {
  return new Promise((resolve) => {
    if (useVpnStore.getState().connectionState !== 'disconnecting') return resolve();
    const unsub = useVpnStore.subscribe((s) => {
      if (s.connectionState !== 'disconnecting') { unsub(); resolve(); }
    });
    setTimeout(() => { unsub(); resolve(); }, timeoutMs); // safety cap, preserves old 3s behavior
  });
}
```
Replace the busy-wait block in `connect()` (vpnStore.ts:78-93) with `await waitForDisconnected();`. Preserve the SAME observable behavior: same 3s cap, same force-to-disconnected fallback after the cap. No other change to connect()'s logic.
  </action>
  <verify>
    <automated>cd app && npx jest src/stores/vpnStore.test.ts 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "waitForDisconnected" app/src/stores/vpnStore.ts` shows the exported helper and its use inside connect()
    - `grep -n "setTimeout(r, 100)\|setTimeout(resolve, 100)" app/src/stores/vpnStore.ts` returns 0 hits (busy-wait removed)
    - Wave 0 `vpnStore.test.ts` is GREEN (resolves on state change; resolves at cap when no transition)
  </acceptance_criteria>
  <done>The connect path awaits an event-driven wait; the busy-wait poll is gone; the Wave 0 jest test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose useVpnConnection into cohesive sub-hooks (behavior-preserving)</name>
  <read_first>
    - app/src/hooks/useVpnConnection.ts (full file — all ~10 effects + tryNextProtocol at 161-225)
    - app/src/stores/vpnStore.ts (now exposes waitForDisconnected from Task 1)
    - .planning/phases/08-cleanup-hardening/08-RESEARCH.md section 5 (decomposition recommendation)
    - docs/audit/CODE-REVIEW.md (APP-M-04, APP-H-03)
    - app/src/screens (any screen that imports useVpnConnection — confirm call sites stay unchanged)
  </read_first>
  <action>
Split `useVpnConnection` along its effects into cohesive hooks, keeping `useVpnConnection` as a THIN composition that calls the sub-hooks so all existing call sites are unchanged (same exported API/return shape):
- `app/src/hooks/useVpnLifecycle.ts` — connect/disconnect orchestration (uses waitForDisconnected from Task 1).
- `app/src/hooks/useProtocolFallback.ts` — the `tryNextProtocol` chain. FIX APP-H-03: route protocol switches through the proper disconnect cleanup (await waitForDisconnected) instead of direct setState that bypasses cleanup. This is the one allowed behavior change (a bug fix the audit calls for) — keep it minimal and documented.
- `app/src/hooks/useConnectionStats.ts` — timers / byte counters.
(Reconnect/backoff may stay in lifecycle or get its own hook — decomposition shape is Claude's discretion per D-23; the constraint is behavior-preservation + the APP-H-03 cleanup fix.)
`useVpnConnection.ts` becomes a small wrapper composing these and returning the same object its callers already consume. Do NOT change any screen/component that imports it.
  </action>
  <verify>
    <automated>cd app && npx tsc --noEmit 2>&1 | tail -15 && npx jest 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `app/src/hooks/useVpnLifecycle.ts`, `useProtocolFallback.ts`, `useConnectionStats.ts` exist
    - `useVpnConnection.ts` line count is materially reduced (was 591) and re-exports the same public API (no caller edits)
    - `npx tsc --noEmit` passes (no type errors); `npx jest` suite green
    - `useProtocolFallback` routes protocol switches through disconnect cleanup (no direct setState bypass) — APP-H-03 addressed
  </acceptance_criteria>
  <done>useVpnConnection is a thin composition over cohesive hooks; types compile; the jest suite is green; call sites unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| (none new) | Behavior-preserving client refactor; no new trust boundary introduced |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Denial of service (client) | vpnStore.connect busy-wait (CODE-REVIEW APP-H-04) | mitigate | Replace 100ms poll loop with an event-driven wait, removing the CPU-spinning busy-wait in the connect path (HARD-15) |
| T-08-15b | Tampering (state) | useProtocolFallback tryNextProtocol (APP-H-03) | mitigate | Route protocol switches through proper disconnect cleanup instead of direct setState bypass |
</threat_model>

<verification>
- `npx jest src/stores/vpnStore.test.ts` GREEN (waitForDisconnected)
- `npx tsc --noEmit` passes; full `npx jest` green
- No busy-wait remains in vpnStore.connect
- Manual device smoke (connect/disconnect/reconnect/protocol-fallback) deferred to the phase-gate manual checklist (08-VALIDATION Manual-Only)
</verification>

<success_criteria>
HARD-15: useVpnConnection decomposed, busy-wait replaced with event-driven wait, behavior preserved, Wave 0 jest test green.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cleanup-hardening/08-06-SUMMARY.md`.
</output>
