---
phase: 08-cleanup-hardening
plan: 05
type: execute
wave: 2
depends_on: ["08-01", "08-03"]
files_modified:
  - server/api/go.mod
  - server/api/go.sum
  - server/api/internal/handler/admin_test.go
  - server/api/internal/handler/payment_test.go
  - server/api/internal/handler/stripe_removal_test.go
  - server/api/internal/middleware/version.go
  - server/api/internal/model/subscription.go
  - server/api/cmd/main.go
autonomous: true
requirements:
  - HARD-01

must_haves:
  truths:
    - "grep -rn stripe server/ --include=*.go returns zero hits"
    - "stripe-go is absent from go.mod and go.sum"
    - "No test imports stripe-go"
    - "subscriptions.stripe_id is verified absent (already dropped by migration 020) — no new migration needed"
    - "A durable Go test (TestNoStripeReferences) fails the suite if any stripe reference reappears in a .go file — the SC#1 literal gate survives as a regression fence, not a one-shot grep"
  artifacts:
    - path: "server/api/go.mod"
      provides: "go.mod without stripe-go (HARD-01)"
      contains: "module"
    - path: "server/api/internal/handler/admin_test.go"
      provides: "test fixture without stripe_id column (HARD-01, D-03)"
      contains: "func Test"
    - path: "server/api/internal/handler/stripe_removal_test.go"
      provides: "durable grep-stripe regression fence (TestNoStripeReferences, SC#1 literal gate)"
      contains: "TestNoStripeReferences"
  key_links:
    - from: "go.mod"
      to: "go.sum"
      via: "go mod tidy purges stripe-go after the require line is deleted"
      pattern: "stripe"
    - from: "stripe_removal_test.go"
      to: "server/ .go tree"
      via: "exec.Command grep over .go files asserts zero stripe references at every go test run"
      pattern: "exec.Command.*grep|TestNoStripeReferences"
---

<objective>
Remove all residual Stripe code and the `stripe-go` dependency (HARD-01 / SC#1), and install a DURABLE regression fence so a stripe reference cannot silently creep back. The `subscriptions.stripe_id` column was already dropped by Phase 3's migration `020_lava_payments.sql:85` and is asserted absent by `migrations_test.go:203-207` — so this plan VERIFIES (no redundant migration, D-01 discretion = verify-only) and cleans up the 5 residual `.go` hits + the dep, then locks the result with a Go test that re-runs the grep at every `go test`.

Purpose: Close SC#1 (`grep -rn stripe server/` == 0) PERMANENTLY (a one-shot execution-time grep would only prove it at execution time; the VALIDATION.md Wave 0 contract requires a durable grep-stripe assertion that survives as a regression check) and shrink the dependency surface (one fewer dep for govulncheck to scan).
Output: go.mod/go.sum without stripe-go, Stripe test fixtures deleted, stale comments fixed, grep-stripe gate green, and a `TestNoStripeReferences` Go test that fails if any stripe reference reappears.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-cleanup-hardening/08-RESEARCH.md
@.planning/phases/08-cleanup-hardening/08-CONTEXT.md

<interfaces>
Residual stripe hits in .go (08-RESEARCH §1, verified — exactly 5, none live handler code):
  - server/api/cmd/main.go:102 — a WARN string mentioning stripe in OptionalEnvWarnings (cosmetic)
  - server/api/internal/handler/migrations_test.go:203-207 — the D-01 assertion that stripe_id is dropped (KEEP — this is the SC#1 evidence)
  - server/api/internal/handler/admin_test.go:345 — `stripe_id TEXT` in a test-fixture CREATE TABLE (D-03: delete)
  - server/api/internal/middleware/version.go:84 — a code comment referencing /webhook/stripe (stale)
  - server/api/internal/model/subscription.go:14 — a doc comment ("dropped stripe_id (migration 020)")
Dependency: server/api/go.mod:15 has `github.com/stripe/stripe-go/v81 v81.4.0` (genuinely unimported in .go now -> `go mod tidy` removes cleanly)
Note: main.go is also edited by plan 08-03 (Wave 1, already merged) — this plan only touches the line-102 WARN string.
IMPORTANT for the durable fence (Task 2): migrations_test.go:203-207 KEEPS the *word* "stripe_id" as the column-absence assertion — that is one legitimate remaining reference. The fence must therefore allow `migrations_test.go` (and its own `stripe_removal_test.go` file, which contains the literal "stripe" search term) and assert ZERO stripe references everywhere else under server/. The reword in Task 1 step 4 removes "stripe" from subscription.go/version.go/main.go so only migrations_test.go's intentional assertion remains — confirm the fence's allowlist matches exactly the files that legitimately mention stripe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete stripe-go dep + Stripe test fixtures + stale comments; verify column dropped</name>
  <read_first>
    - server/api/go.mod (line 15 stripe require)
    - server/api/internal/handler/admin_test.go (stripe_id fixture at admin_test.go:345)
    - server/api/internal/handler/payment_test.go (any stripe-named cases / stripe-go imports — D-03 delete; confirm webhook_lava_test.go is the live coverage)
    - server/api/internal/handler/migrations_test.go (KEEP the stripe_id-absence assertion at 203-207)
    - server/api/internal/middleware/version.go (stale comment at version.go:84)
    - server/api/internal/model/subscription.go (doc comment at subscription.go:14)
    - server/api/cmd/main.go (WARN string at main.go:102)
    - server/api/migrations/020_lava_payments.sql (line 85 DROP COLUMN — confirms column already dropped)
    - .planning/phases/08-cleanup-hardening/08-RESEARCH.md section 1 (verify-only recommendation)
  </read_first>
  <action>
1. Remove `github.com/stripe/stripe-go/v81 v81.4.0` from `server/api/go.mod` (line 15) and run `go mod tidy` (from server/api) to purge it from `go.sum`. The package is unimported in `.go` files, so tidy removes it cleanly.
2. Delete the `stripe_id TEXT` line from the `admin_test.go:345` fixture (D-03). Delete any stripe-named test cases / stripe-go imports in `payment_test.go` outright — do NOT port to lava (the lava path already has `webhook_lava_test.go`). After this, NO test imports `stripe-go`.
3. KEEP `migrations_test.go:203-207` (the stripe_id-absence assertion) — it is the SC#1 evidence that the column is dropped. Do NOT add a redundant `DROP COLUMN` migration (D-01 verify-only). This is the ONE intentional remaining "stripe" reference (the column name in the absence assertion) — Task 2's fence allowlists this file.
4. Fix the stale comments/strings so the grep is clean: `version.go:84` (remove the /webhook/stripe comment), `subscription.go:14` (reword the doc comment to not contain the word "stripe", e.g. "legacy provider column dropped in migration 020"), and the `main.go:102` WARN string (remove the stripe mention). After this step, the ONLY remaining stripe reference under server/ in production+test .go (other than the new fence file) is migrations_test.go's column-absence assertion.
  </action>
  <verify>
    <automated>cd server/api && grep -rn stripe . --include='*.go'; grep -n stripe go.mod go.sum; go build ./... && go test ./internal/handler/... -run 'Migrations|Admin|Payment|Webhook' 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn stripe server/ --include='*.go'` returns only the migrations_test.go column-absence assertion line(s) (intentional) — zero hits in production code and zero in any other test
    - `grep -n stripe server/api/go.mod server/api/go.sum` returns 0 hits
    - No `_test.go` imports `github.com/stripe/stripe-go`
    - `migrations_test.go` still asserts `stripe_id` column absence and that test is GREEN
    - `go build ./...` exits 0; handler test suite green
  </acceptance_criteria>
  <done>Stripe is fully gone from .go and go.mod/go.sum; the column-dropped assertion still passes; no new migration added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Durable grep-stripe regression fence (TestNoStripeReferences, SC#1 literal gate)</name>
  <read_first>
    - server/api/internal/handler/migrations_test.go (the ONE legitimately-remaining stripe reference: the stripe_id column-absence assertion at 203-207 — must be allowlisted, not flagged)
    - server/api/internal/handler/webhook_lava_test.go (test-style conventions: package name `handler`, table-test idiom)
    - any existing `_test.go` in server/api/internal/handler that walks files / uses `os`/`exec` (mirror its style if present)
    - .planning/phases/08-cleanup-hardening/08-CONTEXT.md D-02/D-03 (lock: grep-stripe == 0, no test imports stripe-go)
    - .planning/phases/08-cleanup-hardening/08-VALIDATION.md "## Wave 0 Requirements" SC#1 note (the durable-fence requirement this task satisfies)
  </read_first>
  <behavior>
    - With the tree clean (Task 1 done): `go test ./internal/handler/... -run TestNoStripeReferences` PASSES (zero non-allowlisted stripe references).
    - If a `.go` file under server/ is given a `stripe` reference (e.g. re-adding a `stripe_id` column to a fixture, or a `stripe-go` import), the test FAILS with a message naming the offending file:line.
    - The test does NOT false-positive on its own search term inside `stripe_removal_test.go`, nor on the intentional `stripe_id` column-absence assertion in `migrations_test.go` (both allowlisted).
    - Matching is case-insensitive and scoped to `.go` files under the server root.
  </behavior>
  <action>
Create `server/api/internal/handler/stripe_removal_test.go` (package `handler`) with `TestNoStripeReferences`:
1. Resolve the server root robustly: walk up from the test's working dir (or use a relative path) to the `server/` directory — e.g. `serverRoot := "../../.."` from `server/api/internal/handler` resolves to `server/api`; to cover BOTH modules' .go under `server/`, point the grep at the repo `server/` dir (resolve via walking up until a dir named `server` is found, or accept `../../../..` -> repo `server`). Compute it once and `t.Fatalf` if it cannot be resolved (do NOT silently pass).
2. Run the grep via `exec.Command`:
   ```go
   cmd := exec.Command("grep", "-rniI", "--include=*.go", "stripe", serverRoot)
   out, _ := cmd.CombinedOutput() // grep exits 1 (no match) => err non-nil but out empty => that's the PASS case
   ```
   `-i` = case-insensitive (per the revision brief), `-I` skips binary files, `--include=*.go` scopes to Go source.
3. Parse `out` line-by-line. Build an allowlist set of files that legitimately contain the word "stripe":
   - the test file itself (`stripe_removal_test.go`) — it contains the literal search term
   - `migrations_test.go` — the intentional `stripe_id` column-absence assertion (D-01 SC#1 evidence)
   Skip any matched line whose path ends in one of the allowlisted basenames. Collect every other matched line.
4. If the collected (non-allowlisted) match count > 0: `t.Fatalf("SC#1 regression: %d stripe reference(s) must be zero:\n%s", n, strings.Join(offending, "\n"))`. Otherwise the test passes.
5. If `grep` is unavailable on the runner (`exec.LookPath("grep")` fails), `t.Skip` with a clear message rather than failing — but document that CI (ubuntu-latest) always has grep, so the gate is live in CI.
Keep the allowlist a small explicit slice with a comment explaining WHY each file is allowed, so a future reader knows the fence is intentional and not a loophole.
  </action>
  <verify>
    <automated>cd server/api && go test ./internal/handler/... -run TestNoStripeReferences -v 2>&1 | tail -15 && go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/internal/handler/stripe_removal_test.go` exists, package `handler`, defines `TestNoStripeReferences`
    - The test runs as part of `go test ./internal/handler/...` and PASSES on the clean tree
    - The assertion uses `exec.Command("grep", ...)` over `.go` files under the server root, case-insensitive, and `t.Fatal`s when the non-allowlisted match count > 0
    - Allowlist covers exactly `stripe_removal_test.go` and `migrations_test.go` (the only legitimate stripe mentions); proven by temporarily adding a `stripe_id` line to a fixture and watching the test go RED (revert after)
    - `go build ./...` exits 0
  </acceptance_criteria>
  <done>A durable Go test fails the suite if any stripe reference reappears under server/, making SC#1 a permanent regression fence rather than a one-shot execution-time grep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| build supply chain | an unused dependency still widens the attack/vuln surface scanned in CI |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering / Elevation | unused stripe-go dependency | mitigate | Remove the dep so it cannot become a vuln vector and so govulncheck (HARD-09) has a smaller surface (HARD-01) |
| T-08-01b | Information disclosure | stale stripe references in comments/strings | accept | Cosmetic cleanup; no live behavior — closing the grep also removes any misleading operator-facing WARN |
| T-08-01c | Tampering | stripe code/dep silently reintroduced in a later PR | mitigate | `TestNoStripeReferences` durable fence fails the suite (and CI) if any stripe reference reappears under server/ — SC#1 stays closed over time |
</threat_model>

<verification>
- `grep -rn stripe server/ --include='*.go'` == 0 (excluding the intentional migrations_test.go column-absence assertion)
- `grep -n stripe server/api/go.mod server/api/go.sum` == 0
- `go test ./internal/handler/... -run TestNoStripeReferences` GREEN on the clean tree; RED when a stripe reference is reintroduced
- `go build ./...` and the migrations/admin/payment/webhook tests green
</verification>

<success_criteria>
HARD-01 / SC#1: zero stripe hits in .go, stripe-go absent from go.mod, stripe_id verified dropped (no new migration), and a durable Go test fence (`TestNoStripeReferences`) keeps SC#1 closed against regression.
</success_criteria>

<output>
After completion, create `.planning/phases/08-cleanup-hardening/08-05-SUMMARY.md`.
</output>
