---
phase: 06-performance-scalability
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - server/api/migrations/perf_indexes_test.go
  - server/api/internal/cache/servers_cache_test.go
  - server/api/internal/cache/user_cache_test.go
  - server/api/internal/cache/heartbeat_cache_test.go
  - server/api/internal/config/scheduler_gate_test.go
  - server/api/internal/repository/ctx_cancel_test.go
  - server/api/internal/handler/servers_cache_test.go
  - server/api/internal/scheduler/scheduler_perf_test.go
autonomous: true
requirements: [PERF-01, PERF-02, PERF-04, PERF-05, PERF-06, PERF-07, PERF-08]
nyquist_compliant: true

must_haves:
  truths:
    - "Each new test file compiles and runs (RED — failing or skipped against not-yet-built code is expected)"
    - "The seven Wave 0 test files named in 06-VALIDATION.md exist"
    - "Tests reference the exact symbols the implementation waves will create (GetServersCache, GetUserCache, FlushHeartbeats, ShouldRunScheduler, idx_connections_heartbeat_active, idx_connections_connected_at)"
  artifacts:
    - path: "server/api/migrations/perf_indexes_test.go"
      provides: "EXPLAIN ANALYZE assertions for PERF-05/PERF-08 (testcontainers)"
      contains: "idx_connections_heartbeat_active"
    - path: "server/api/internal/cache/servers_cache_test.go"
      provides: "miniredis cache-aside/fail-open/bust for cache:servers:active"
      contains: "cache:servers:active"
    - path: "server/api/internal/cache/user_cache_test.go"
      provides: "miniredis cache-aside/fail-open/bust for user:<id>"
      contains: "user:"
    - path: "server/api/internal/cache/heartbeat_cache_test.go"
      provides: "pipeline write + dirty-set; FlushHeartbeats collapses N→1"
      contains: "hb:dirty"
    - path: "server/api/internal/config/scheduler_gate_test.go"
      provides: "ShouldRunScheduler truth table"
      contains: "ShouldRunScheduler"
    - path: "server/api/internal/repository/ctx_cancel_test.go"
      provides: "pg_stat_activity ctx-cancel assertion"
      contains: "pg_stat_activity"
    - path: "server/api/internal/handler/servers_cache_test.go"
      provides: "zero-servers-SELECT on cache hit"
      contains: "TestServersCacheNoSelect"
  key_links:
    - from: "all Wave 0 test files"
      to: "the implementation symbols of plans 02-07"
      via: "compile-time references"
      pattern: "GetServersCache|GetUserCache|FlushHeartbeats|ShouldRunScheduler|idx_connections_heartbeat_active"
---

<objective>
Establish the Wave 0 test infrastructure that every implementation wave depends on. This plan creates the seven test files listed in 06-VALIDATION.md so that each downstream task has a concrete `<automated>` command to satisfy. Tests are written RED-first: they reference symbols (cache functions, the `ShouldRunScheduler` helper, the new index names, `FlushHeartbeats`) that do not exist yet, so they will fail to compile or fail at runtime until the implementation waves land them.

Purpose: Nyquist compliance. The phase's definition-of-done is automated assertions (D-09), not a live load test. Without these test files first, the implementation tasks have nothing to verify against.
Output: Seven Go test files (testcontainers + miniredis based). No production code is touched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-performance-scalability/06-VALIDATION.md
@.planning/phases/06-performance-scalability/06-RESEARCH.md

<interfaces>
<!-- Templates to mirror. Read these before writing the new test files. -->

Existing test templates already in-tree:
- server/api/migrations/migrations_test.go — testcontainers Postgres 16 setup; `splitSQLStatements` special-cases CONCURRENTLY (lines 36-67). Reuse its container bootstrap for perf_indexes_test.go and ctx_cancel_test.go.
- server/api/internal/cache/plans_cache_test.go — miniredis pattern for cache-aside tests. Mirror for servers/user/heartbeat cache tests.
- server/api/internal/config/config_test.go — env-var table-test pattern. Mirror for scheduler_gate_test.go.

Symbols the implementation waves WILL create (reference these now so tests fail until built):
- cache.GetServersCache(ctx, client) (string, error) / SetServersCache / BustServersCache  [plan 04]
- cache.GetUserCache(ctx, client, userID) / SetUserCache / BustUserCache  [plan 05]
- cache.TouchHeartbeat(ctx, client, connID) error  [plan 06]
- cache.FlushHeartbeats(ctx, client, db) (int, error)  [plan 06]
- config.ShouldRunScheduler(envValue string) bool  [plan 06]
- index idx_connections_heartbeat_active ON connections(last_heartbeat_at) WHERE disconnected_at IS NULL  [plan 03]
- index idx_connections_connected_at ON connections(connected_at)  [plan 03]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cache + config unit-test scaffolds (miniredis, no Docker)</name>
  <read_first>
    - server/api/internal/cache/plans_cache_test.go (miniredis pattern to mirror exactly)
    - server/api/internal/config/config_test.go (env table-test pattern)
    - .planning/phases/06-performance-scalability/06-RESEARCH.md (Pattern 1 cache-aside code; D-04 heartbeat pipeline; ShouldRunScheduler truth table in the D-09b confirmation section)
  </read_first>
  <files>server/api/internal/cache/servers_cache_test.go, server/api/internal/cache/user_cache_test.go, server/api/internal/cache/heartbeat_cache_test.go, server/api/internal/config/scheduler_gate_test.go</files>
  <action>
Create four unit-test files using miniredis (`github.com/alicebob/miniredis/v2`, already in go.mod) and Go's `testing`. These reference not-yet-built symbols on purpose (RED).

`servers_cache_test.go` (package `cache`):
- `TestServersCacheRoundTrip`: start miniredis, build a `*redis.Client` pointed at it, call `SetServersCache(ctx, client, `[{"id":"s1"}]`)`, then `GetServersCache(ctx, client)` returns that exact string.
- `TestServersCacheMissReturnsEmpty`: Get on empty Redis returns `""`, nil error (miss != error).
- `TestServersCacheFailOpen`: close the miniredis (`mr.Close()`), assert `GetServersCache` returns `""`, nil error (fail-open — never propagates a Redis error).
- `TestBustServersCache`: Set, then `BustServersCache(ctx, client)`, then Get returns `""`. Assert the key constant is `cache:servers:active`.

`user_cache_test.go` (package `cache`):
- `TestUserCacheRoundTrip`: `SetUserCache(ctx, client, "uid-1", "pro")` then `GetUserCache(ctx, client, "uid-1")` returns the tier `"pro"` (and a found=true signal — match whatever signature plan 05 uses; recommend `(tier string, found bool, err error)`).
- `TestUserCacheMiss`: Get on unknown id returns found=false (fall through to DB → authoritative).
- `TestUserCacheFailOpen`: close miniredis, Get returns found=false, nil err.
- `TestBustUserCache`: Set "uid-1", `BustUserCache(ctx, client, "uid-1")`, Get returns found=false. Assert key shape is `user:uid-1`.
- `TestUserCacheTTL`: after Set, assert miniredis reports a TTL on key `user:uid-1` of ≤ 5s (use `mr.TTL("user:uid-1")`).

`heartbeat_cache_test.go` (package `cache`):
- `TestTouchHeartbeatWritesKeyAndDirtySet`: `TouchHeartbeat(ctx, client, "conn-1")`; assert miniredis has key `hb:conn-1` (value = a unix ts), TTL ~600s (`mr.TTL`), AND `conn-1` is a member of set `hb:dirty` (`mr.SMembers("hb:dirty")`).
- `TestFlushHeartbeatsCollapsesNto1`: TouchHeartbeat for 50 distinct conn ids; then call `FlushHeartbeats(ctx, client, db)` ONCE and assert it returns count=50 and drains `hb:dirty` to empty (the PERF-02 (e) "N writes → 1 flush" assertion). NOTE: this needs a real/sqlite-or-testcontainer db for the UPDATE — if a DB isn't wired in -short mode, assert at minimum that `SMEMBERS hb:dirty` had 50 members before flush and the helper is called once. Mark the DB-dependent portion to run without `-short`.
- `TestTouchHeartbeatFailOpen`: nil client → no panic, returns nil (heartbeat is non-critical).

`scheduler_gate_test.go` (package `config`):
- `TestShouldRunScheduler`: table test for `ShouldRunScheduler(envValue string) bool` with cases: `""`→true, `"true"`→true, `"TRUE"`→true, `"1"`→true, `"false"`→false, `"0"`→false, `"no"`→false (decide false/falsey set; document the rule — recommend: any value other than the falsey set {"false","0","no"} is true, default true). This is the load-bearing PERF-06 (d) assertion.
  </action>
  <verify>
    <automated>cd server/api && go vet ./internal/cache/... ./internal/config/... 2>&1 | head; echo "EXPECT: compile errors referencing undefined GetServersCache/GetUserCache/TouchHeartbeat/FlushHeartbeats/ShouldRunScheduler — RED is correct at Wave 0"</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/internal/cache/servers_cache_test.go` exists and contains the string `cache:servers:active`
    - `server/api/internal/cache/user_cache_test.go` exists and contains `mr.TTL(` and `user:`
    - `server/api/internal/cache/heartbeat_cache_test.go` exists and contains both `hb:dirty` and `FlushHeartbeats`
    - `server/api/internal/config/scheduler_gate_test.go` exists and contains `ShouldRunScheduler` with at least the cases `""`, `"true"`, `"false"`
    - `grep -rn "miniredis" server/api/internal/cache/servers_cache_test.go` returns a hit
  </acceptance_criteria>
  <done>Four unit-test files exist, reference the not-yet-built symbols, and `go vet` fails ONLY on undefined symbols (proving the tests are wired to the right names) — no syntax errors of the test code itself.</done>
</task>

<task type="auto">
  <name>Task 2: testcontainers integration-test scaffolds (indexes, ctx-cancel, zero-SELECT)</name>
  <read_first>
    - server/api/migrations/migrations_test.go (testcontainers Postgres 16 bootstrap + splitSQLStatements for CONCURRENTLY)
    - server/api/internal/repository/connection_repo.go (CleanupStaleConnections predicate at :113-126 — the query the EXPLAIN will run; admin analytics date-bucket query is in admin_repo.go :230/:284/:438)
    - .planning/phases/06-performance-scalability/06-VALIDATION.md (the five D-09 assertions; test-map rows for PERF-05/07/08/01)
    - .planning/phases/06-performance-scalability/06-RESEARCH.md (Fork 2 DDL; Fork 1 pgx ctx-cancel; PERF-01 zero-SELECT via gorm logger capture)
  </read_first>
  <files>server/api/migrations/perf_indexes_test.go, server/api/internal/repository/ctx_cancel_test.go, server/api/internal/handler/servers_cache_test.go, server/api/internal/scheduler/scheduler_perf_test.go</files>
  <action>
Create four integration-test files. These spin a real Postgres 16 via testcontainers (reuse the `migrations_test.go` bootstrap) and/or miniredis. They are RED until the indexes/cache/flush land. Guard each behind `if testing.Short() { t.Skip(...) }` so the quick suite stays fast.

`perf_indexes_test.go` (package `migrations`, `//go:build` consistent with migrations_test.go):
- `TestPerfIndexes`: start a fresh Postgres 16 testcontainer, run ALL migrations 001..023 (the harness already runs the directory). Then:
  - (PERF-05) seed ~1000 connections rows (mix of disconnected_at NULL/non-NULL), `ANALYZE connections`, then run `EXPLAIN (FORMAT JSON) <the COALESCE-dropped stale-sweep query: SELECT id FROM connections WHERE disconnected_at IS NULL AND last_heartbeat_at < now() - interval '3 minutes'>` and assert the plan JSON contains `idx_connections_heartbeat_active` (Index Scan / Bitmap Index Scan, NOT Seq Scan).
  - (PERF-08) run `EXPLAIN (FORMAT JSON)` on the analytics query `SELECT date_trunc('day', connected_at), count(*) FROM connections WHERE connected_at >= now() - interval '30 days' GROUP BY 1` and assert `idx_connections_connected_at` appears in the plan.
- This is the D-09 (a) assertion. RED until migration 022/023 exist.

`ctx_cancel_test.go` (package `repository`):
- `TestCtxCancelAbortsQuery`: testcontainer Postgres; open a gorm DB; in a goroutine run `db.WithContext(ctx).Exec("SELECT pg_sleep(30)")` with a cancellable ctx; from the main goroutine poll `SELECT count(*) FROM pg_stat_activity WHERE query LIKE '%pg_sleep%' AND state='active'` until it's 1, then `cancel()`, then poll again and assert the row disappears within ~2s. This is D-09 (c) — proves pgx v5 sends a CancelRequest. RED is acceptable now (it will pass once ctx is threaded, but the raw `db.WithContext(ctx)` form already works — write it so it's GREEN-able immediately and serves as the PERF-07 proof).

`servers_cache_test.go` (package `handler`):
- `TestServersCacheNoSelect`: wire a gorm DB with a custom logger that records every SQL statement (use gorm's `logger.Interface` capturing into a slice, or the gorm `Session` callback). Seed servers, prime `cache:servers:active` via `SetServersCache`, then invoke the `ListServers` handler path (or the cache-read branch) and assert ZERO statements containing `vpn_servers` (or `servers`) SELECT were logged on the cache-hit. This is D-09 (b). RED until plan 04 adds the cache read to the handler. Also add `TestServersCacheBustOnAdminWrite` skeleton asserting an admin server-write calls `BustServersCache` (can be a focused unit test on the handler once plan 04 lands).

`scheduler_perf_test.go` (package `scheduler`):
- `TestHeartbeatFlushTicker`: skeleton asserting that a 10s flush goroutine exists and `FlushHeartbeats` collapses N dirty ids into 1 bulk UPDATE (miniredis + testcontainer or sqlite). This is D-09 (e).
- `TestSchedulerGateNoJobs`: skeleton — with `RUN_SCHEDULER=false` wiring, advancing past cleanupInterval fires no cleanup side-effect (assert via a sentinel: a stale connection row is NOT disconnected). This is D-09 (d), belt-and-suspenders to the unit test in scheduler_gate_test.go.
- `TestWeeklyPruneCadence`: skeleton asserting the 90-day connection prune runs on a weekly (not per-minute) tick. RED until plan 06.
  </action>
  <verify>
    <automated>cd server/api && go vet ./migrations/... ./internal/repository/... ./internal/handler/... ./internal/scheduler/... 2>&1 | head -30; echo "EXPECT: undefined-symbol / not-yet-implemented references — RED is correct at Wave 0"</automated>
  </verify>
  <acceptance_criteria>
    - `server/api/migrations/perf_indexes_test.go` exists and contains both `idx_connections_heartbeat_active` and `idx_connections_connected_at` and `EXPLAIN`
    - `server/api/internal/repository/ctx_cancel_test.go` exists and contains `pg_stat_activity` and `pg_sleep`
    - `server/api/internal/handler/servers_cache_test.go` exists and contains `TestServersCacheNoSelect`
    - `server/api/internal/scheduler/scheduler_perf_test.go` exists and contains `FlushHeartbeats` and a RUN_SCHEDULER no-op test
    - every integration test starts with a `testing.Short()` skip guard: `grep -c "testing.Short()" server/api/migrations/perf_indexes_test.go` ≥ 1
  </acceptance_criteria>
  <done>Four integration-test files exist, reuse the migrations_test.go testcontainer bootstrap, guard with `-short` skips, and reference the exact index names + symbols the implementation waves will build. The five D-09 assertions each have a concrete test home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → testcontainer Postgres | local-only ephemeral DB; no production data |
| test harness → miniredis | in-process fake; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-W0-01 | Tampering | test files referencing prod cache keys | accept | Tests are read-only against ephemeral fakes; no prod Redis/DB touched. Key constants must match production exactly (`cache:servers:active`, `user:<id>`, `hb:dirty`) so the tests actually validate the prod paths — verified in acceptance criteria. |
| T-06-W0-02 | DoS | testcontainer resource use in CI | accept | `-short` skip guards keep the quick suite Docker-free; full suite runs testcontainers only on wave-merge/phase-gate. |

No HIGH-severity threats: this plan ships test code only, no production surface changes.
</threat_model>

<verification>
- `cd server/api && go vet ./...` surfaces only undefined-symbol errors pointing at the not-yet-built implementation symbols (RED-first is the intended Wave 0 state).
- All seven 06-VALIDATION.md Wave 0 files exist (note: heartbeat + scheduler flush share `scheduler_perf_test.go` + `heartbeat_cache_test.go`).
- No production `.go` file under `internal/` (non-test) or `cmd/` is modified.
</verification>

<success_criteria>
- The seven test files named in 06-VALIDATION.md exist (cache×3, config gate, perf_indexes, ctx_cancel, handler servers_cache) plus scheduler_perf_test.go for the flush/gate/prune assertions.
- Each of the five D-09 phase-gate assertions has a concrete test function home.
- Tests reference exact production symbols + key shapes + index names so downstream tasks have unambiguous targets.
- RED state confirmed: failures are undefined-symbol / not-implemented, never test-code syntax errors.
</success_criteria>

<output>
After completion, create `.planning/phases/06-performance-scalability/06-00-SUMMARY.md`
</output>
