---
phase: 06-performance-scalability
plan: 06
type: execute
wave: 5
depends_on: [00, 03, 04, 05]
files_modified:
  - server/api/internal/repository/user_repo.go
  - server/api/internal/repository/connection_repo.go
  - server/api/internal/repository/server_repo.go
  - server/api/internal/repository/plan_repo.go
  - server/api/internal/repository/session_repo.go
  - server/api/internal/repository/admin_repo.go
  - server/api/internal/repository/expiry_repo.go
  - server/api/internal/repository/recovery_repo.go
  - server/api/internal/repository/webhook_event_repo.go
  - server/api/internal/repository/subscription_repo.go
  - server/api/internal/repository/device_repo.go
  - server/api/internal/repository/link_code_repo.go
  - server/api/internal/repository/audit_repo.go
  - server/api/internal/repository/ctx_cancel_test.go
  - server/api/internal/handler/
  - server/api/internal/middleware/auth.go
  - server/api/internal/scheduler/scheduler.go
  - server/api/internal/bot/recovery.go
autonomous: true
requirements: [PERF-07]
nyquist_compliant: true

must_haves:
  truths:
    - "Every exported repository function takes ctx context.Context as its FIRST parameter and calls db.WithContext(ctx) once internally"
    - "Handlers pass c.Context(); scheduler passes context.Background() (or a per-pass timeout); the bot passes botCtx"
    - "Killing a long-running client request via ctx cancellation aborts the underlying DB query (the row disappears from pg_stat_activity)"
    - "The whole module builds and the full test suite passes after the mechanical threading"
  artifacts:
    - path: "server/api/internal/repository/user_repo.go"
      provides: "ctx-first repo signatures + db.WithContext(ctx)"
      contains: "ctx context.Context"
    - path: "server/api/internal/repository/ctx_cancel_test.go"
      provides: "pg_stat_activity ctx-cancel assertion (D-09 (c))"
      contains: "pg_stat_activity"
  key_links:
    - from: "handler call sites"
      to: "repo functions"
      via: "c.Context() threaded as first arg"
      pattern: "repository\\.[A-Za-z]+\\(c.Context\\(\\)"
    - from: "db.WithContext(ctx)"
      to: "pgx cancel-request on client disconnect"
      via: "ctx cancellation aborts the in-flight query"
      pattern: "db.WithContext\\(ctx\\)"
---

<objective>
Implement PERF-07 (D-08): thread `ctx context.Context` as the FIRST parameter through every exported repository function and call `db.WithContext(ctx)` once inside each body, then update every call site to pass a ctx (handlers: `c.Context()`; scheduler: `context.Background()` / per-pass timeout; bot: `botCtx`). This is the mechanical, compiler-guided refactor research Fork 1 resolved — the driver is pgx v5, so a cancelled ctx genuinely sends a Postgres CancelRequest and aborts the in-flight query.

This plan is sequenced LAST (Wave 5, depends on 03/04/05) precisely because it touches every repo file AND every handler/scheduler/bot call site — many of which the earlier plans also edit. Running it last means it absorbs the final signatures (the new cache busts, the RETURNING-id downgrades, the heartbeat changes, the c.Locals sweep) instead of colliding with them.

Purpose: stuck queries become time-bounded by client disconnect — closes the pool-exhaustion DoS surface (audit §4.1). Proven by the Wave 0 ctx-cancel test (D-09 (c)).
Output: ctx-first signatures across all repos + updated call sites; the build green; the full suite green; ctx_cancel_test.go GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-performance-scalability/06-RESEARCH.md
@server/api/internal/repository/user_repo.go

<interfaces>
Before / After (research Pattern 2):
  BEFORE: func FindUserByID(db *gorm.DB, id string) (*model.User, error) { result := db.First(&user, "id = ?", id) ... }
  AFTER:  func FindUserByID(ctx context.Context, db *gorm.DB, id string) (*model.User, error) { result := db.WithContext(ctx).First(&user, "id = ?", id) ... }
  BEFORE call: repository.FindUserByID(db, userID)
  AFTER call:  repository.FindUserByID(c.Context(), db, userID)

Caller ctx sources:
  - handlers -> c.Context()  (already used at auth.go:83 for IsTokenBlacklisted)
  - scheduler -> context.Background() (recommend context.WithTimeout per cleanup pass so a wedged query can't hang the ticker)
  - bot/recovery.go -> botCtx (in scope; bot goroutine)

Transactions: db.WithContext(ctx).Transaction(func(tx){...}) — the tx inherits the statement ctx; add .WithContext(ctx) to the OUTER call (SetUserPlan, PerformRestore, ReplacePlanServers, SoftDeletePlan, ReplaceOffer).

Full repo inventory (research Fork 1 table) — every exported query function in:
  user_repo, connection_repo, server_repo, plan_repo, session_repo, admin_repo, expiry_repo, recovery_repo, webhook_event_repo, subscription_repo, device_repo, link_code_repo, audit_repo.

pgx v5 (go.mod jackc/pgx/v5 v5.5.5) sends a CancelRequest on ctx cancel — the basis for the D-09 (c) assertion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thread ctx as first param through ALL repository functions</name>
  <read_first>
    - every file in server/api/internal/repository/ (the exported functions to change — use the Fork 1 inventory table as the checklist)
    - .planning/phases/06-performance-scalability/06-RESEARCH.md (Fork 1: exact before/after; the transaction note; the per-file inventory)
    - server/api/internal/repository/connection_repo.go (note FlushHeartbeats from plan 05 already uses db.WithContext(ctx) — leave it; CleanupStaleConnections from plan 02 needs ctx added)
  </read_first>
  <files>server/api/internal/repository/user_repo.go, server/api/internal/repository/connection_repo.go, server/api/internal/repository/server_repo.go, server/api/internal/repository/plan_repo.go, server/api/internal/repository/session_repo.go, server/api/internal/repository/admin_repo.go, server/api/internal/repository/expiry_repo.go, server/api/internal/repository/recovery_repo.go, server/api/internal/repository/webhook_event_repo.go, server/api/internal/repository/subscription_repo.go, server/api/internal/repository/device_repo.go, server/api/internal/repository/link_code_repo.go, server/api/internal/repository/audit_repo.go</files>
  <action>
For EVERY exported function in each repository file that runs a query, add `ctx context.Context` as the FIRST parameter and replace the first `db.<op>` with `db.WithContext(ctx).<op>`. Import "context" where missing.

Rules:
- First param order: always ctx FIRST, then db, then the rest: func Xxx(ctx context.Context, db *gorm.DB, ...).
- Call db.WithContext(ctx) ONCE at the top of the body (or chain it onto the first statement) — do not scatter it.
- For functions using db.Transaction(...): change to db.WithContext(ctx).Transaction(func(tx *gorm.DB) error {...}) so the tx inherits ctx. Apply to SetUserPlan, PerformRestore, ReplacePlanServers, SoftDeletePlan, ReplaceOffer and any other transactional function.
- Functions already updated by earlier plans: FlushHeartbeats (plan 05) already takes ctx + uses WithContext — leave as-is. DowngradeExpiredSubscriptions / DowngradeExpiredPlans (plan 04, now return []string) — add ctx as first param + WithContext on their UPDATE...RETURNING.
- Do NOT change behavior, return shapes, or SQL — only add ctx + WithContext.

Work file-by-file; after each file, run go build to catch call-site breakage early (the compiler is your guide — every un-updated caller is a build error, which Task 2 fixes).
  </action>
  <verify>
    <automated>cd server/api && go build ./internal/repository/... 2>&1 | tail -10; echo "(repo package compiles; call-site errors in other packages are expected until Task 2)"</automated>
  </verify>
  <acceptance_criteria>
    - every exported query function in the 13 repo files has ctx context.Context as its first parameter: grep -rn "func [A-Z][A-Za-z]*(db \*gorm.DB" server/api/internal/repository/ returns 0 (no exported func still takes db as the FIRST param)
    - grep -rn "db.WithContext(ctx)" server/api/internal/repository/ returns hits across all 13 files
    - transactional functions use db.WithContext(ctx).Transaction: grep -n "WithContext(ctx).Transaction" server/api/internal/repository/*.go returns hits for SetUserPlan / PerformRestore / ReplacePlanServers / SoftDeletePlan / ReplaceOffer
    - cd server/api && go build ./internal/repository/... exits 0 (the repo package itself compiles)
  </acceptance_criteria>
  <done>Every exported repo function takes ctx first and calls db.WithContext(ctx) once; transactions inherit ctx; no SQL/behavior change. The repository package compiles; call-site errors elsewhere are the Task 2 worklist.</done>
</task>

<task type="auto">
  <name>Task 2: Update ALL call sites to pass ctx; build + full suite green; ctx-cancel assertion</name>
  <read_first>
    - every file under server/api/internal/handler/ that calls repository.* (pass c.Context())
    - server/api/internal/middleware/auth.go (FindUserByID + GetUserCache calls — pass c.Context())
    - server/api/internal/scheduler/scheduler.go (cleanup jobs — pass context.Background() or a per-pass context.WithTimeout)
    - server/api/internal/bot/recovery.go (PerformRestore + any repo calls — pass botCtx; if botCtx is not in scope at the call, thread it from Run)
    - server/api/internal/repository/ctx_cancel_test.go (Wave 0 — the pg_stat_activity assertion this must turn GREEN)
    - .planning/phases/06-performance-scalability/06-RESEARCH.md (Pitfall 4 — scheduler/bot have no Fiber ctx; pass Background/botCtx, never nil)
  </read_first>
  <files>server/api/internal/handler/, server/api/internal/middleware/auth.go, server/api/internal/scheduler/scheduler.go, server/api/internal/bot/recovery.go, server/api/internal/repository/ctx_cancel_test.go</files>
  <action>
Fix every compiler error from Task 1 by passing a ctx as the first arg at each call site:
- Handlers: pass c.Context() (e.g. repository.FindUserByID(c.Context(), db, userID)).
- middleware/auth.go: pass c.Context() to FindUserByID and to the cache calls (already use c.Context()).
- scheduler.go: pass context.Background() to each cleanup/downgrade/prune repo call. RECOMMENDED: wrap each cleanup PASS in a context.WithTimeout(context.Background(), 30*time.Second) (defer cancel) so a wedged cleanup query can't hang the ticker (Pitfall 4). The 10s flush goroutine already passes context.Background() to FlushHeartbeats — keep or wrap with a short timeout.
- bot/recovery.go: pass botCtx to PerformRestore and any repo calls. If botCtx isn't in scope at the call site (it's the goroutine ctx from Run), thread it down from Run/the handler struct.

Do NOT pass nil for ctx anywhere (Pitfall 4 — nil ctx panics/ignored).

Finalize the Wave 0 ctx_cancel_test.go so it is GREEN: testcontainer Postgres; run db.WithContext(ctx).Exec("SELECT pg_sleep(30)") in a goroutine with a cancellable ctx; poll pg_stat_activity until the query is active; cancel(); assert the row disappears within ~2s (proves pgx sends CancelRequest). This is D-09 (c).

Run the FULL build + suite. Every miss is a build error — iterate until green.
  </action>
  <verify>
    <automated>cd server/api && go build ./... && go test ./... -short 2>&1 | tail -25; echo "--- ctx-cancel integration (no -short) ---"; go test ./internal/repository/... -run TestCtxCancelAbortsQuery 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - cd server/api && go build ./... exits 0 (entire module compiles with ctx-first repos)
    - no call site passes nil as the ctx arg to a repo function: grep -rn "repository\.[A-Za-z]*(nil," server/api/internal/ returns 0
    - handlers pass c.Context(): grep -rn "repository\.[A-Za-z]*(c.Context()" server/api/internal/handler/ returns multiple hits
    - scheduler passes context.Background()/WithTimeout (not nil): grep -n "context.Background()\|context.WithTimeout" server/api/internal/scheduler/scheduler.go returns a hit
    - go test ./... -short passes (full quick suite green after the mechanical refactor)
    - go test ./internal/repository/... -run TestCtxCancelAbortsQuery passes (ctx cancel removes the query from pg_stat_activity — D-09 (c))
  </acceptance_criteria>
  <done>Every repo call site passes a real ctx (handlers c.Context(), scheduler Background/timeout, bot botCtx); the entire module builds; the full suite is green; the ctx-cancel test proves query abort on disconnect (PERF-07 / D-09 (c)).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client TCP -> Fiber -> GORM -> pgx -> Postgres | a client disconnect must propagate cancellation all the way to the DB query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-CTXLEAK | DoS | unbounded queries outliving their request | mitigate | db.WithContext(c.Context()) threaded through every repo call; a client disconnect cancels c.Context(), pgx v5 sends a Postgres CancelRequest, the query aborts and releases its pool connection. Closes the "open 1000 conns, send slow request, drop TCP, drain the pool" attack (audit §4.1). Asserted by TestCtxCancelAbortsQuery (pg_stat_activity). |
| T-06-CTXNIL | Reliability | scheduler/bot passing nil ctx | mitigate | Pitfall 4 — scheduler passes context.Background()/WithTimeout, bot passes botCtx; a grep guard asserts no nil ctx is passed. |

No HIGH residual: the DoS surface is closed by end-to-end ctx propagation; the assertion proves the abort actually fires.
</threat_model>

<verification>
- cd server/api && go build ./... green (whole module compiles with ctx-first repos).
- go test ./... -short green (full quick suite after the mechanical refactor).
- go test ./internal/repository/... -run TestCtxCancelAbortsQuery green (D-09 (c) — query removed from pg_stat_activity on cancel).
- No nil ctx passed anywhere; scheduler/bot use Background/timeout/botCtx.
</verification>

<success_criteria>
- PERF-07: every GORM call inherits the request ctx via db.WithContext(ctx); no query outlives its request.
- ctx-cancellation aborts the in-flight DB query (D-09 (c)).
- Full module build + suite green after the mechanical threading.
</success_criteria>

<output>
After completion, create `.planning/phases/06-performance-scalability/06-06-SUMMARY.md`
</output>
