---
phase: 06-performance-scalability
plan: 07
type: execute
wave: 6
depends_on: [01, 02, 05, 06]
files_modified:
  - docs/runbooks/06-perf-deploy-runbook.md
  - .planning/phases/06-performance-scalability/06-HUMAN-UAT.md
autonomous: false
requirements: [PERF-03, PERF-05, PERF-08]
nyquist_compliant: true

must_haves:
  truths:
    - "A production runbook documents the manual live-DB index backfill (psql -f CREATE INDEX CONCURRENTLY for 022/023) — because docker-entrypoint-initdb.d only fires on an empty pgdata volume, a normal deploy will NOT apply the new indexes to the live DB"
    - "The runbook documents the data-tier off-host move (set DB_HOST/REDIS_HOST, private interface + firewall, TLS-ready upgrade path) as an operator step (the physical move is out of Phase 6 code scope)"
    - "06-HUMAN-UAT.md captures the deferred ~10k synthetic load test and the live index backfill as operator-gated items, consistent with how Phases 4 and 5 deferred hardware-dependent UAT"
    - "A human checkpoint verifies the runbook is correct and the backfill/load-test are tracked, not silently skipped"
  artifacts:
    - path: "docs/runbooks/06-perf-deploy-runbook.md"
      provides: "live-DB index backfill + off-host data-tier move + Redis/PG bring-up steps"
      contains: "CREATE INDEX CONCURRENTLY"
    - path: ".planning/phases/06-performance-scalability/06-HUMAN-UAT.md"
      provides: "deferred load test + manual backfill UAT gate"
      contains: "backfill"
  key_links:
    - from: "migrations 022/023 (plan 02)"
      to: "the live production DB"
      via: "manual psql -f backfill runbook step (not auto-applied by docker compose up)"
      pattern: "CREATE INDEX CONCURRENTLY"
    - from: "docker-compose.data.yml (plan 01)"
      to: "second-host deploy"
      via: "DB_HOST/REDIS_HOST + private interface runbook"
      pattern: "DB_HOST"
---

<objective>
Capture the phase's critical operational caveat (research Fork 2 / Pitfall 1) as an explicit deliverable: the migration runner (docker-entrypoint-initdb.d) only fires on an EMPTY pgdata volume, so a normal docker compose up against the existing production database will NOT apply the new 022/023 index migrations. The indexes must be backfilled manually via psql -f with CREATE INDEX CONCURRENTLY (safe online). This plan writes the production runbook (index backfill + off-host data-tier move + bring-up order) and the HUMAN-UAT note (deferred ~10k load test + the manual backfill gate), then pauses for a human checkpoint so the operational steps are verified and tracked — never silently false-positived by a fresh-volume CI test.

Purpose: prevent the "passed in CI, missing in prod" failure mode for PERF-05/PERF-08, and scope the physical second-host move (PERF-03) + the deferred load test correctly — consistent with how Phases 4 and 5 deferred hardware-dependent UAT and how D-09 defers the live load test.
Output: a runbook doc, a HUMAN-UAT doc, and a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-performance-scalability/06-RESEARCH.md
@.planning/phases/06-performance-scalability/06-VALIDATION.md
@server/api/migrations/022_add_perf_indexes.sql
@server/api/migrations/023_connections_connected_at_index.sql
@docker-compose.data.yml

<interfaces>
The Manual-Only Verifications table in 06-VALIDATION.md already names the three deferred items: (1) real ~10k load test, (2) physical second-host PG/Redis provisioning, (3) production live-DB index backfill. Mirror those into the runbook + HUMAN-UAT.

Fork 2 backfill commands (use verbatim in the runbook):
  docker exec vpn-postgres psql -U vpnapp -d vpnapp -f /docker-entrypoint-initdb.d/022_add_perf_indexes.sql
  docker exec vpn-postgres psql -U vpnapp -d vpnapp -f /docker-entrypoint-initdb.d/023_connections_connected_at_index.sql
  verify: SELECT indexname FROM pg_indexes WHERE tablename='connections';
  verify usage later: SELECT idx_scan FROM pg_stat_user_indexes WHERE indexrelname='idx_connections_heartbeat_active';

Bring-up (co-located single host) from plan 01:
  docker compose -f docker-compose.data.yml -f docker-compose.prod.yml up -d
Off-host move (operator step): run data.yml on the data host (bind PG/Redis to the private interface + firewall), set DB_HOST/REDIS_HOST in the app host .env, run prod.yml on the app host. Zero image change. TLS-ready: flip sslmode=disable to require + redis:// to rediss:// later, no code change (D-02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the production perf-deploy runbook (index backfill + off-host move)</name>
  <read_first>
    - .planning/phases/06-performance-scalability/06-RESEARCH.md (Fork 2 backfill command + Pitfall 1 empty-volume warning sign; D-01/D-02 off-host move + TLS-ready upgrade)
    - server/api/migrations/022_add_perf_indexes.sql + 023 (the exact filenames + CONCURRENTLY DDL the backfill applies)
    - docker-compose.data.yml + docker-compose.prod.yml (the bring-up commands + DB_HOST/REDIS_HOST parameterization from plan 01)
    - .github/workflows/deploy.yml (how the current deploy runs compose — the runbook must align with it; the deploy copies migrations but relies on initdb semantics)
  </read_first>
  <files>docs/runbooks/06-perf-deploy-runbook.md</files>
  <action>
Create docs/runbooks/06-perf-deploy-runbook.md with these sections:

1. "Why a manual index backfill is required" — explain docker-entrypoint-initdb.d empty-volume semantics (only runs on first init / empty pgdata); a normal docker compose up against the existing live DB does NOT re-run 022/023; the deploy workflow copies migrations every deploy but still relies on initdb, so the new indexes are NOT auto-applied to the existing production database. Warning sign: the EXPLAIN assertion passes in CI (fresh testcontainer) but pg_stat_user_indexes shows idx_scan=0 for the new index in prod.

2. "Live-DB index backfill (PERF-05 / PERF-08)" — the verbatim psql -f commands for 022 and 023; note CONCURRENTLY makes it safe online (no write lock) and IF NOT EXISTS makes it idempotent/re-runnable. Include the verification queries (pg_indexes list + pg_stat_user_indexes idx_scan after some traffic).

3. "Co-located single-host bring-up" — the two-file compose up command (data.yml + prod.yml together); confirm Redis runs with --requirepass + --maxmemory 256mb --maxmemory-policy allkeys-lru and PG mounts the tuned postgresql.conf.

4. "Off-host data-tier move (PERF-03, operator step, OUT of code scope)" — step list: run data.yml on the data host; bind PG/Redis to the private interface (VPC/WireGuard/locked subnet) + firewall so they are not publicly reachable; set DB_HOST + REDIS_HOST in the app host .env; run prod.yml on the app host; zero image change. Note the data-migration (dump/restore or replication) is a separate operator decision. TLS-ready upgrade: flip DATABASE_URL sslmode=disable to sslmode=require (or verify-full) and REDIS_URL redis:// to rediss:// when TLS is enabled — no code change (D-02).

5. "Rollback / safety" — the indexes are additive (DROP INDEX CONCURRENTLY to revert); the compose split is reversible (run both files on one host); the COALESCE-drop (plan 02) is behavior-equivalent.

Keep it operator-readable: numbered steps, copy-pasteable commands, expected output per step.
  </action>
  <verify>
    <automated>grep -l "CREATE INDEX CONCURRENTLY" /Users/abdunabi/Desktop/vpn/docs/runbooks/06-perf-deploy-runbook.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/runbooks/06-perf-deploy-runbook.md exists and contains the verbatim backfill commands (docker exec vpn-postgres psql ... -f /docker-entrypoint-initdb.d/022_add_perf_indexes.sql and the 023 file)
    - it contains the string "CREATE INDEX CONCURRENTLY" and the verification queries (pg_indexes, pg_stat_user_indexes)
    - it documents the empty-volume initdb caveat and the warning sign (CI green / prod idx_scan=0)
    - it documents the off-host move with DB_HOST/REDIS_HOST + private interface + firewall + TLS-ready upgrade path
    - it documents the co-located two-file bring-up command and confirms Redis maxmemory + mounted postgresql.conf
  </acceptance_criteria>
  <done>The runbook documents the manual live-DB index backfill (the empty-volume caveat made operator-actionable), the co-located bring-up, and the off-host data-tier move with the TLS-ready path. PERF-05/PERF-08 production application + PERF-03 physical move are correctly scoped as operator steps.</done>
</task>

<task type="auto">
  <name>Task 2: Write 06-HUMAN-UAT.md (deferred load test + manual backfill gate)</name>
  <read_first>
    - .planning/phases/06-performance-scalability/06-VALIDATION.md (Manual-Only Verifications table — the three deferred items)
    - .planning/phases/04-landing-surfaces/04-HUMAN-UAT.md (the HUMAN-UAT format used by prior phases — mirror its structure)
    - docs/runbooks/06-perf-deploy-runbook.md (Task 1 — cross-reference the backfill steps)
  </read_first>
  <files>.planning/phases/06-performance-scalability/06-HUMAN-UAT.md</files>
  <action>
Create .planning/phases/06-performance-scalability/06-HUMAN-UAT.md mirroring the prior-phase HUMAN-UAT format. Include three deferred/operator-gated items, each with: what it verifies, why it is manual, the exact steps, and a checkbox status (DEFERRED / PENDING-OPERATOR):

1. "Live-DB index backfill (PERF-05 / PERF-08)" — operator runs the psql -f backfill from the runbook against the live DB, then verifies idx_scan increments in pg_stat_user_indexes after traffic. Status: PENDING-OPERATOR (must run on next prod deploy). Cross-reference docs/runbooks/06-perf-deploy-runbook.md.

2. "Real ~10k synthetic connection load test (PERF-02 / phase goal)" — drive k6/vegeta against staging; measure PG write q/s drop (expect ~167/s to ~1/10s), /auth/refresh p99, cleanup latency. Status: DEFERRED to the end-of-milestone release phase (per D-09; phase ships assertion-level proof only). Note the five D-09 automated assertions are the in-phase definition-of-done.

3. "Physical second-host PG/Redis provisioning + private-link/firewall (PERF-03)" — operator provisions the data host, binds PG/Redis to the private interface, sets DB_HOST/REDIS_HOST. Status: DEFERRED (operator's call; Phase 6 ships the parameterized compose split only).

Add a short header noting this is consistent with how Phases 4 and 5 deferred hardware-dependent UAT.
  </action>
  <verify>
    <automated>grep -l "backfill" /Users/abdunabi/Desktop/vpn/.planning/phases/06-performance-scalability/06-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/06-performance-scalability/06-HUMAN-UAT.md exists and contains all three deferred/operator items (live backfill, ~10k load test, second-host provisioning)
    - it contains the word "backfill" and references docs/runbooks/06-perf-deploy-runbook.md
    - it marks the ~10k load test DEFERRED to the release phase and the live backfill PENDING-OPERATOR
    - it notes the five D-09 automated assertions are the in-phase definition-of-done
  </acceptance_criteria>
  <done>06-HUMAN-UAT.md tracks the manual backfill (PENDING-OPERATOR) and the deferred load test + second-host move (DEFERRED) so none is silently skipped; the in-phase automated assertions are documented as the real definition-of-done.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify runbook + deferred-UAT tracker (operational gates)</name>
  <what-built>
The Phase 6 production runbook (docs/runbooks/06-perf-deploy-runbook.md) and the deferred-UAT tracker (06-HUMAN-UAT.md). All code changes (compose split, indexes, /servers cache, user cache, heartbeat→Redis, scheduler gate, ctx propagation) landed in plans 01–06 with automated assertions; this checkpoint confirms the OPERATIONAL gaps are documented and tracked rather than silently passing on a fresh-volume CI run.
  </what-built>
  <how-to-verify>
1. Open docs/runbooks/06-perf-deploy-runbook.md. Confirm the live-DB index backfill commands are correct for your prod host (vpn-postgres container, vpnapp DB) and that the empty-volume caveat is clearly stated.
2. Confirm the off-host move steps (DB_HOST/REDIS_HOST, private interface + firewall, TLS-ready upgrade) match your infra plan.
3. Open .planning/phases/06-performance-scalability/06-HUMAN-UAT.md. Confirm the three deferred/operator items are tracked with the right status (live backfill PENDING-OPERATOR; ~10k load test + second-host move DEFERRED to release).
4. Decide: will you run the live index backfill on the next prod deploy now, or schedule it? Either way it must be tracked — do not let a fresh-volume CI green imply prod has the indexes.
  </how-to-verify>
  <resume-signal>Type "approved" once the runbook + UAT tracker are correct, or describe corrections needed.</resume-signal>
  <files>(no files modified — manual verification checkpoint)</files>
  <action>This is a checkpoint task. Execute the steps in &lt;how-to-verify&gt; above and reply with the &lt;resume-signal&gt; to resume.</action>
  <verify>
    <automated>echo "manual checkpoint — awaiting human approval per how-to-verify"</automated>
  </verify>
  <done>Operator replied "approved" (runbook + deferred-UAT tracker confirmed correct) or described corrections.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator -> live production DB | the manual backfill runs DDL against the live DB; CONCURRENTLY keeps it online-safe |
| documentation -> operational reality | the risk is a gap between "CI green" and "prod applied" — the runbook closes it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-BACKFILL-GAP | DoS (silent perf regression) | live DB missing the new indexes | mitigate | The runbook + HUMAN-UAT make the manual backfill an explicit, tracked operator gate; the warning sign (CI green but pg_stat_user_indexes idx_scan=0) is documented so the gap is detectable. CONCURRENTLY + IF NOT EXISTS make the backfill safe + idempotent. |
| T-06-OFFHOST-EXPOSURE | Information Disclosure | data tier on a public interface during the move | mitigate | The runbook mandates binding PG/Redis to a private interface + firewall and documents the TLS-ready upgrade (D-02) before any cross-host link is exposed. |

No HIGH residual: the operational gaps are documented + human-gated; no new code surface is introduced by this plan.
</threat_model>

<verification>
- docs/runbooks/06-perf-deploy-runbook.md exists with the backfill commands + CONCURRENTLY + DB_HOST off-host steps.
- 06-HUMAN-UAT.md exists with the three deferred/operator items + the D-09 assertion note.
- Human checkpoint approved (runbook correct, backfill/load-test tracked).
</verification>

<success_criteria>
- The empty-volume migration caveat is captured as an explicit, operator-actionable runbook step + a HUMAN-UAT gate (never silently passed by CI).
- PERF-03 physical move + the deferred ~10k load test are correctly scoped as operator/release items.
- Human verifies the operational steps before the phase is considered shippable.
</success_criteria>

<output>
After completion, create `.planning/phases/06-performance-scalability/06-07-SUMMARY.md`
</output>
